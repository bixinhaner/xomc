# Breadcrumb 面包屑

## 概述
显示当前页面在导航层级中的位置。

## 结构
```
首页 > 告警管理 > 当前告警
```

## 样式
- 分隔符: `>` 或 `/`
- 祖先节点: 可点击链接，`primary-600`
- 当前节点: 不可点击，`neutral-800`，`font-semibold`
- 字号: `font-base` (14px)

## 位置
二级栏（Secondary Bar）左侧，高度 40px。
