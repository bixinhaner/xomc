# omcgo-worker ConfigMap
# 来源：omcgo/cmd/worker/etc/config.prod.yaml
# 容器内挂载到 /etc/omcgo/config.yaml
# Worker 无对外端口，仅暴露 :9092 metrics + /healthz + /readyz
apiVersion: v1
kind: ConfigMap
metadata:
  name: omcgo-worker-config
  namespace: omc
  labels:
    app.kubernetes.io/name: omcgo-worker
    app.kubernetes.io/component: config
data:
  config.yaml: |
    db:
      dsn: "postgres://${DB_USER}:${DB_PASSWORD}@${DB_HOST}:5432/${DB_NAME}?sslmode=disable"
      max_conns: 20
      min_conns: 5
      max_conn_lifetime: 30m
      max_conn_idle_time: 5m
      health_check_interval: 30s

    tsdb:
      dsn: "postgres://${DB_USER}:${DB_PASSWORD}@${DB_HOST}:5432/${DB_NAME}?sslmode=disable"
      max_conns: 20
      min_conns: 5
      max_conn_lifetime: 30m
      max_conn_idle_time: 5m
      health_check_interval: 30s

    redis:
      addrs:
        - "${REDIS_HOST}:6379"
      password: "${REDIS_PASSWORD}"
      db: 0
      pool_size: 100

    nats:
      url: "nats://${NATS_HOST}:4222"
      max_reconnect: -1
      reconnect_wait: 2s

    minio:
      endpoint: "${MINIO_HOST}:9000"
      # 【运维必填】浏览器 / 外部 SDK 可达的 MinIO 公网地址（host[:port]，无 scheme），
      # 用于签发 License / 配置备份 / 文件导出等下载的预签名 URL。留空则回退用
      # endpoint(${MINIO_HOST}:9000，k8s ClusterIP) 签发——浏览器无法解析会报
      # ERR_NAME_NOT_RESOLVED（qa-614 #377）。请经 env/Secret 注入 MINIO_PUBLIC_HOST
      # 为对外域名 / SLB VIP / Ingress 地址。
      public_endpoint: "${MINIO_PUBLIC_HOST}"
      access_key: "${MINIO_ACCESS_KEY}"
      secret_key: "${MINIO_SECRET_KEY}"
      use_ssl: false
      buckets:
        pm_files: "pm-files"
        mr_files: "mr-files"
        firmware: "firmware"
        config_backup: "config-backup"
        logs: "logs"
        reports: "reports"
        exchange: "omc-exchange"

    metrics:
      port: 9092

    log:
      level: "info"
      format: "json"
      output_paths:
        - "stdout"

    request_id_prefix: "worker"
