# omcgo-worker Secret（占位）
# ⚠️ 生产环境绝对禁止 commit 实际密钥！本文件仅为模板。
# Worker 不暴露 HTTP 业务端口，凭据仅用于连接 DB/Redis/NATS/MinIO
apiVersion: v1
kind: Secret
metadata:
  name: omcgo-worker-secret
  namespace: omc
  labels:
    app.kubernetes.io/name: omcgo-worker
    app.kubernetes.io/component: secret
type: Opaque
stringData:
  DB_USER: "REPLACE_ME"
  DB_PASSWORD: "REPLACE_ME"
  DB_HOST: "postgres.omc.svc.cluster.local"
  DB_NAME: "omcgo"
  REDIS_HOST: "redis.omc.svc.cluster.local"
  REDIS_PASSWORD: ""
  NATS_HOST: "nats.omc.svc.cluster.local"
  MINIO_HOST: "minio.omc.svc.cluster.local"
  MINIO_ACCESS_KEY: "REPLACE_ME"
  MINIO_SECRET_KEY: "REPLACE_ME"
