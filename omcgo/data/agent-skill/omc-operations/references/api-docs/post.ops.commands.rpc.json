{
  "schemaVersion": "1.0",
  "operationId": "post.ops.commands.rpc",
  "method": "POST",
  "path": "/api/v1/ops/commands/rpc",
  "handler": "github.com/omcgo/omcgo/internal/ops.(*ExtHandler).ExecuteRPC-fm",
  "category": "ops",
  "title": "运维任务 - 创建",
  "summary": "运维任务 - 创建",
  "description": "运维任务：创建新的资源记录 Handler: ExecuteRPC enqueues an ad-hoc RPC command as an OpsTask (T-0102-b).\n\nWire contract (POST /api/v1/ops/commands/rpc):\n\n\trequest  : { device_sn | device_sns[], action, params? }\n\tresponse : 202 Accepted\n\t           { task_id, status, approval_required, stream_url }\n\nBehavior:\n 1. Risk classify by action (classifyRiskForAction) — combined with\n    device count, ApprovalService.EvaluateRiskLevel produces the task's\n    overall risk.\n 2. Persist an OpsTask via the consumer-driven TaskCreator interface.\n    Inline command spec (action + params) lives in OpsTask.Message as\n    a JSON envelope so T-0102-c's RPC dispatcher can read it back.\n 3. If risk is L3 (dangerous), task is created with approval_state=pending\n    and is NOT auto-dispatched — operator must call POST /tasks/:id/approve.\n    Otherwise approval_state=not_required and a fire-and-forget goroutine\n    calls executor.Run to move task → running.\n 4. Audit log + SSE \"command.enqueued\" event published on the per-task\n    channel \"command:\u003ctask_id\u003e\" so the existing CommandStreamSSE handler\n    can deliver realtime updates.",
  "intents": [
    "运维任务 - 创建",
    "运维任务：创建新的资源记录 Handler: ExecuteRPC enqueues an ad-hoc RPC command as an OpsTask (T-0102-b).\n\nWire contract (POST /api/v1/ops/commands/rpc):\n\n\trequest  : { device_sn | device_sns[], action, params? }\n\tresponse : 202 Accepted\n\t           { task_id, status, approval_required, stream_url }\n\nBehavior:\n 1. Risk classify by action (classifyRiskForAction) — combined with\n    device count, ApprovalService.EvaluateRiskLevel produces the task's\n    overall risk.\n 2. Persist an OpsTask via the consumer-driven TaskCreator interface.\n    Inline command spec (action + params) lives in OpsTask.Message as\n    a JSON envelope so T-0102-c's RPC dispatcher can read it back.\n 3. If risk is L3 (dangerous), task is created with approval_state=pending\n    and is NOT auto-dispatched — operator must call POST /tasks/:id/approve.\n    Otherwise approval_state=not_required and a fire-and-forget goroutine\n    calls executor.Run to move task → running.\n 4. Audit log + SSE \"command.enqueued\" event published on the per-task\n    channel \"command:\u003ctask_id\u003e\" so the existing CommandStreamSSE handler\n    can deliver realtime updates.",
    "ops",
    "运维任务",
    "commands",
    "rpc"
  ],
  "tags": [
    "ops",
    "运维任务",
    "commands",
    "rpc"
  ],
  "risk": "low",
  "confirmationRequired": true,
  "idempotent": false,
  "sideEffects": "会修改业务状态或触发任务；执行前确认目标、范围和影响。操作语义：运维任务 - 创建",
  "pathParams": [],
  "queryParams": [],
  "formParams": [],
  "requestBody": {
    "contentType": "application/json",
    "required": true,
    "schema": {
      "properties": {
        "action": {
          "type": "string"
        },
        "device_sn": {
          "type": "string"
        },
        "device_sns": {
          "items": {
            "type": "string"
          },
          "type": "array"
        },
        "params": {
          "additionalProperties": {
            "additionalProperties": true,
            "type": "object"
          },
          "type": "object"
        }
      },
      "required": [
        "action"
      ],
      "type": "object"
    }
  },
  "responses": {
    "2xx": {
      "content": {
        "application/json": {
          "schema": {
            "properties": {
              "data": {
                "description": "该 Handler 返回的真实业务结果；以实际 API JSON 为准。"
              },
              "msg": {
                "example": "ok",
                "type": "string"
              },
              "ret": {
                "const": 1,
                "type": "integer"
              }
            },
            "required": [
              "ret",
              "msg",
              "data"
            ],
            "type": "object"
          }
        }
      },
      "description": "成功。JSON 接口使用 OMC 标准响应信封；文件或 SSE 接口按 Content-Type 返回二进制或事件流。"
    },
    "4xx": {
      "description": "请求参数、身份、权限或 Agent policy 不允许。不要绕过限制。"
    },
    "5xx": {
      "description": "OMC 内部处理失败。不要编造结果或自动重试非幂等写操作。"
    }
  },
  "contractCoverage": {
    "request": "go-handler",
    "response": "standard-envelope"
  },
  "sources": [
    "runtime-route",
    "go-handler"
  ]
}
