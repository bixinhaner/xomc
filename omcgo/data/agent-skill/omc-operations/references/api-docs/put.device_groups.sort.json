{
  "schemaVersion": "1.0",
  "operationId": "put.device_groups.sort",
  "method": "PUT",
  "path": "/api/v1/device-groups/sort",
  "handler": "github.com/omcgo/omcgo/internal/topology.(*Handler).BatchSort-fm",
  "category": "device-groups",
  "title": "设备分组 - 排序",
  "summary": "设备分组 - 排序",
  "description": "设备分组：调整列表或树节点顺序 Handler: BatchSort handles PUT /device-groups/sort.",
  "intents": [
    "设备分组 - 排序",
    "设备分组：调整列表或树节点顺序 Handler: BatchSort handles PUT /device-groups/sort.",
    "device-groups",
    "设备分组",
    "sort",
    "device groups"
  ],
  "tags": [
    "device-groups",
    "设备分组",
    "sort"
  ],
  "risk": "low",
  "confirmationRequired": true,
  "idempotent": true,
  "sideEffects": "会修改业务状态或触发任务；执行前确认目标、范围和影响。操作语义：设备分组 - 排序",
  "pathParams": [],
  "queryParams": [],
  "formParams": [],
  "requestBody": {
    "contentType": "application/json",
    "required": true,
    "schema": {
      "goType": "BatchSortRequest",
      "properties": {
        "items": {
          "items": {
            "goType": "SortItem",
            "properties": {
              "id": {
                "type": "string"
              },
              "sort_order": {
                "goType": "int",
                "type": "integer"
              }
            },
            "required": [
              "id"
            ],
            "type": "object"
          },
          "type": "array"
        }
      },
      "required": [
        "items"
      ],
      "type": "object"
    }
  },
  "responses": {
    "2xx": {
      "content": {
        "application/json": {
          "schema": {
            "properties": {
              "data": {
                "description": "该 Handler 返回的真实业务结果；以实际 API JSON 为准。"
              },
              "msg": {
                "example": "ok",
                "type": "string"
              },
              "ret": {
                "const": 1,
                "type": "integer"
              }
            },
            "required": [
              "ret",
              "msg",
              "data"
            ],
            "type": "object"
          }
        }
      },
      "description": "成功。JSON 接口使用 OMC 标准响应信封；文件或 SSE 接口按 Content-Type 返回二进制或事件流。"
    },
    "4xx": {
      "description": "请求参数、身份、权限或 Agent policy 不允许。不要绕过限制。"
    },
    "5xx": {
      "description": "OMC 内部处理失败。不要编造结果或自动重试非幂等写操作。"
    }
  },
  "contractCoverage": {
    "request": "go-handler",
    "response": "standard-envelope"
  },
  "sources": [
    "runtime-route",
    "go-handler"
  ]
}
