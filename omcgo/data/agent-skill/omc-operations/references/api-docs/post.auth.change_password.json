{
  "schemaVersion": "1.0",
  "operationId": "post.auth.change_password",
  "method": "POST",
  "path": "/api/v1/auth/change-password",
  "handler": "github.com/omcgo/omcgo/internal/admin.(*Handler).ChangePassword-fm",
  "category": "auth",
  "title": "认证 - 修改密码",
  "summary": "认证 - 修改密码",
  "description": "认证：修改当前用户或指定用户密码 Handler: ChangePassword handles POST /api/v1/auth/change-password.\nThe user must be authenticated; it verifies the old password before updating.\n\n旧/新密码均需 RSA-OAEP 加密传输（共用一次 keyID）。",
  "intents": [
    "认证 - 修改密码",
    "认证：修改当前用户或指定用户密码 Handler: ChangePassword handles POST /api/v1/auth/change-password.\nThe user must be authenticated; it verifies the old password before updating.\n\n旧/新密码均需 RSA-OAEP 加密传输（共用一次 keyID）。",
    "auth",
    "change-password",
    "change password"
  ],
  "tags": [
    "auth",
    "change-password"
  ],
  "risk": "low",
  "confirmationRequired": true,
  "idempotent": false,
  "sideEffects": "会修改业务状态或触发任务；执行前确认目标、范围和影响。操作语义：认证 - 修改密码",
  "pathParams": [],
  "queryParams": [],
  "formParams": [],
  "requestBody": {
    "contentType": "application/json",
    "required": true,
    "schema": {
      "goType": "ChangePasswordHTTPRequest",
      "properties": {
        "encrypted_new_password": {
          "type": "string"
        },
        "encrypted_old_password": {
          "type": "string"
        },
        "key_id": {
          "type": "string"
        },
        "new_password": {
          "description": "T-0120 plaintext fallback",
          "type": "string"
        },
        "old_password": {
          "description": "T-0120 plaintext fallback",
          "type": "string"
        }
      },
      "type": "object"
    }
  },
  "responses": {
    "2xx": {
      "content": {
        "application/json": {
          "schema": {
            "properties": {
              "data": {
                "description": "该 Handler 返回的真实业务结果；以实际 API JSON 为准。"
              },
              "msg": {
                "example": "ok",
                "type": "string"
              },
              "ret": {
                "const": 1,
                "type": "integer"
              }
            },
            "required": [
              "ret",
              "msg",
              "data"
            ],
            "type": "object"
          }
        }
      },
      "description": "成功。JSON 接口使用 OMC 标准响应信封；文件或 SSE 接口按 Content-Type 返回二进制或事件流。"
    },
    "4xx": {
      "description": "请求参数、身份、权限或 Agent policy 不允许。不要绕过限制。"
    },
    "5xx": {
      "description": "OMC 内部处理失败。不要编造结果或自动重试非幂等写操作。"
    }
  },
  "contractCoverage": {
    "request": "go-handler",
    "response": "standard-envelope"
  },
  "sources": [
    "runtime-route",
    "go-handler"
  ]
}
