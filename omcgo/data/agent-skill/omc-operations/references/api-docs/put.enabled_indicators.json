{
  "schemaVersion": "1.0",
  "operationId": "put.enabled_indicators",
  "method": "PUT",
  "path": "/api/v1/enabled-indicators",
  "handler": "github.com/omcgo/omcgo/internal/pm/indicator.(*RESTHandler).SetEnabled-fm",
  "category": "enabled-indicators",
  "title": "enabled-indicators - 批量更新",
  "summary": "enabled-indicators - 批量更新",
  "description": "enabled-indicators：批量更新资源数据",
  "intents": [
    "enabled-indicators - 批量更新",
    "enabled-indicators：批量更新资源数据",
    "enabled-indicators",
    "enabled indicators"
  ],
  "tags": [
    "enabled-indicators"
  ],
  "risk": "low",
  "confirmationRequired": true,
  "idempotent": true,
  "sideEffects": "会修改业务状态或触发任务；执行前确认目标、范围和影响。操作语义：enabled-indicators - 批量更新",
  "pathParams": [],
  "queryParams": [
    {
      "name": "operatorCode",
      "in": "query",
      "required": false,
      "type": "string"
    }
  ],
  "formParams": [],
  "requestBody": {
    "contentType": "application/json",
    "required": true,
    "schema": {
      "goType": "setEnabledReq",
      "properties": {
        "enable": {
          "type": "boolean"
        },
        "indicator_ids": {
          "items": {
            "type": "string"
          },
          "type": "array"
        }
      },
      "required": [
        "indicator_ids"
      ],
      "type": "object"
    }
  },
  "responses": {
    "2xx": {
      "content": {
        "application/json": {
          "schema": {
            "properties": {
              "data": {
                "description": "该 Handler 返回的真实业务结果；以实际 API JSON 为准。"
              },
              "msg": {
                "example": "ok",
                "type": "string"
              },
              "ret": {
                "const": 1,
                "type": "integer"
              }
            },
            "required": [
              "ret",
              "msg",
              "data"
            ],
            "type": "object"
          }
        }
      },
      "description": "成功。JSON 接口使用 OMC 标准响应信封；文件或 SSE 接口按 Content-Type 返回二进制或事件流。"
    },
    "4xx": {
      "description": "请求参数、身份、权限或 Agent policy 不允许。不要绕过限制。"
    },
    "5xx": {
      "description": "OMC 内部处理失败。不要编造结果或自动重试非幂等写操作。"
    }
  },
  "contractCoverage": {
    "request": "go-handler",
    "response": "standard-envelope"
  },
  "sources": [
    "runtime-route",
    "go-handler"
  ]
}
