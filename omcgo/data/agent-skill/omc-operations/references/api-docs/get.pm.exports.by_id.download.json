{
  "schemaVersion": "1.0",
  "operationId": "get.pm.exports.by_id.download",
  "method": "GET",
  "path": "/api/v1/pm/exports/:id/download",
  "handler": "github.com/omcgo/omcgo/internal/pm/export.(*Handler).Download-fm",
  "category": "pm",
  "title": "性能 PM - 下载",
  "summary": "性能 PM - 下载",
  "description": "性能 PM：下载文件或导出结果 Handler: Download GET /pm/exports/:id/download — 默认同源流式下载 KPI 导出 CSV。\n\n兼容旧调用：?mode=url 仍返回对象存储签名 GET 链接。\n\n文件未就绪（非 succeeded 或 file_path 为空）返 4xx，不 500 崩溃。",
  "intents": [
    "性能 PM - 下载",
    "性能 PM：下载文件或导出结果 Handler: Download GET /pm/exports/:id/download — 默认同源流式下载 KPI 导出 CSV。\n\n兼容旧调用：?mode=url 仍返回对象存储签名 GET 链接。\n\n文件未就绪（非 succeeded 或 file_path 为空）返 4xx，不 500 崩溃。",
    "pm",
    "性能",
    "exports",
    "download"
  ],
  "tags": [
    "pm",
    "性能",
    "exports",
    "download"
  ],
  "risk": "read",
  "confirmationRequired": false,
  "idempotent": true,
  "sideEffects": "无；该操作只读取当前用户可见数据。",
  "pathParams": [
    {
      "name": "id",
      "in": "path",
      "required": true,
      "type": "string",
      "description": "替换 URL 路径中的 :id"
    }
  ],
  "queryParams": [
    {
      "name": "mode",
      "in": "query",
      "required": false,
      "type": "string"
    }
  ],
  "formParams": [],
  "responses": {
    "2xx": {
      "content": {
        "application/json": {
          "schema": {
            "properties": {
              "data": {
                "description": "该 Handler 返回的真实业务结果；以实际 API JSON 为准。"
              },
              "msg": {
                "example": "ok",
                "type": "string"
              },
              "ret": {
                "const": 1,
                "type": "integer"
              }
            },
            "required": [
              "ret",
              "msg",
              "data"
            ],
            "type": "object"
          }
        }
      },
      "description": "成功。JSON 接口使用 OMC 标准响应信封；文件或 SSE 接口按 Content-Type 返回二进制或事件流。"
    },
    "4xx": {
      "description": "请求参数、身份、权限或 Agent policy 不允许。不要绕过限制。"
    },
    "5xx": {
      "description": "OMC 内部处理失败。不要编造结果或自动重试非幂等写操作。"
    }
  },
  "contractCoverage": {
    "request": "go-handler",
    "response": "standard-envelope"
  },
  "emptyResult": "空数组或 total=0 表示当前用户权限和过滤条件下没有可见记录；除非接口另有明确语义，不要改用其他接口猜测数据。",
  "sources": [
    "runtime-route",
    "go-handler"
  ]
}
