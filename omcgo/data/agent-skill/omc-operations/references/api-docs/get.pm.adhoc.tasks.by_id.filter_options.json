{
  "schemaVersion": "1.0",
  "operationId": "get.pm.adhoc.tasks.by_id.filter_options",
  "method": "GET",
  "path": "/api/v1/pm/adhoc/tasks/:id/filter-options",
  "handler": "github.com/omcgo/omcgo/internal/pm/adhoc.(*Handler).FilterOptions-fm",
  "category": "pm",
  "title": "性能 PM - filteroptions",
  "summary": "性能 PM - filteroptions",
  "description": "性能 PM：处理filteroptions相关数据 Handler: FilterOptions GET /pm/adhoc/tasks/:id/filter-options\n\n按选中任务的聚合维度，列出\"该任务实际聚合了哪些产品/设备组/频段\"的权威子集清单（走 SELECT DISTINCT，\n与结果分页/上限解耦，不被截断）。前端据此渲染对应维度的多选筛选框。\n维度为 device/aggregate_group/network 时返回空 options（前端不渲染筛选框）。",
  "intents": [
    "性能 PM - filteroptions",
    "性能 PM：处理filteroptions相关数据 Handler: FilterOptions GET /pm/adhoc/tasks/:id/filter-options\n\n按选中任务的聚合维度，列出\"该任务实际聚合了哪些产品/设备组/频段\"的权威子集清单（走 SELECT DISTINCT，\n与结果分页/上限解耦，不被截断）。前端据此渲染对应维度的多选筛选框。\n维度为 device/aggregate_group/network 时返回空 options（前端不渲染筛选框）。",
    "pm",
    "性能",
    "adhoc",
    "tasks",
    "filter-options",
    "filter options"
  ],
  "tags": [
    "pm",
    "性能",
    "adhoc",
    "tasks",
    "filter-options"
  ],
  "risk": "read",
  "confirmationRequired": false,
  "idempotent": true,
  "sideEffects": "无；该操作只读取当前用户可见数据。",
  "pathParams": [
    {
      "name": "id",
      "in": "path",
      "required": true,
      "type": "string",
      "description": "替换 URL 路径中的 :id"
    }
  ],
  "queryParams": [],
  "formParams": [],
  "responses": {
    "2xx": {
      "content": {
        "application/json": {
          "schema": {
            "properties": {
              "data": {
                "description": "该 Handler 返回的真实业务结果；以实际 API JSON 为准。"
              },
              "msg": {
                "example": "ok",
                "type": "string"
              },
              "ret": {
                "const": 1,
                "type": "integer"
              }
            },
            "required": [
              "ret",
              "msg",
              "data"
            ],
            "type": "object"
          }
        }
      },
      "description": "成功。JSON 接口使用 OMC 标准响应信封；文件或 SSE 接口按 Content-Type 返回二进制或事件流。"
    },
    "4xx": {
      "description": "请求参数、身份、权限或 Agent policy 不允许。不要绕过限制。"
    },
    "5xx": {
      "description": "OMC 内部处理失败。不要编造结果或自动重试非幂等写操作。"
    }
  },
  "contractCoverage": {
    "request": "path-only",
    "response": "standard-envelope"
  },
  "emptyResult": "空数组或 total=0 表示当前用户权限和过滤条件下没有可见记录；除非接口另有明确语义，不要改用其他接口猜测数据。",
  "sources": [
    "runtime-route",
    "go-handler"
  ]
}
