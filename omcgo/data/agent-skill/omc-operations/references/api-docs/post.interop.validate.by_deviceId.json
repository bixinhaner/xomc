{
  "schemaVersion": "1.0",
  "operationId": "post.interop.validate.by_deviceId",
  "method": "POST",
  "path": "/api/v1/interop/validate/:deviceId",
  "handler": "github.com/omcgo/omcgo/internal/interop.(*Handler).ValidateDevice-fm",
  "category": "interop",
  "title": "互操作 - 校验",
  "summary": "互操作 - 校验",
  "description": "互操作：校验请求参数或配置内容 Handler: ValidateDevice compares a device's actual parameters against its data model definition.",
  "intents": [
    "互操作 - 校验",
    "互操作：校验请求参数或配置内容 Handler: ValidateDevice compares a device's actual parameters against its data model definition.",
    "interop"
  ],
  "tags": [
    "interop"
  ],
  "risk": "low",
  "confirmationRequired": true,
  "idempotent": false,
  "sideEffects": "会修改业务状态或触发任务；执行前确认目标、范围和影响。操作语义：互操作 - 校验",
  "pathParams": [
    {
      "name": "deviceId",
      "in": "path",
      "required": true,
      "type": "string",
      "description": "替换 URL 路径中的 :deviceId"
    }
  ],
  "queryParams": [
    {
      "name": "carrier",
      "in": "query",
      "required": false,
      "type": "string"
    },
    {
      "name": "tech",
      "in": "query",
      "required": false,
      "type": "string"
    }
  ],
  "formParams": [],
  "responses": {
    "2xx": {
      "content": {
        "application/json": {
          "schema": {
            "properties": {
              "data": {
                "description": "该 Handler 返回的真实业务结果；以实际 API JSON 为准。"
              },
              "msg": {
                "example": "ok",
                "type": "string"
              },
              "ret": {
                "const": 1,
                "type": "integer"
              }
            },
            "required": [
              "ret",
              "msg",
              "data"
            ],
            "type": "object"
          }
        }
      },
      "description": "成功。JSON 接口使用 OMC 标准响应信封；文件或 SSE 接口按 Content-Type 返回二进制或事件流。"
    },
    "4xx": {
      "description": "请求参数、身份、权限或 Agent policy 不允许。不要绕过限制。"
    },
    "5xx": {
      "description": "OMC 内部处理失败。不要编造结果或自动重试非幂等写操作。"
    }
  },
  "contractCoverage": {
    "request": "go-handler",
    "response": "standard-envelope"
  },
  "sources": [
    "runtime-route",
    "go-handler"
  ]
}
