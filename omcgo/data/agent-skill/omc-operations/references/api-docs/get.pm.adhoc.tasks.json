{
  "schemaVersion": "1.0",
  "operationId": "get.pm.adhoc.tasks",
  "method": "GET",
  "path": "/api/v1/pm/adhoc/tasks",
  "handler": "github.com/omcgo/omcgo/internal/pm/adhoc.(*Handler).List-fm",
  "category": "pm",
  "title": "性能 PM - 列表",
  "summary": "性能 PM - 列表",
  "description": "性能 PM：按筛选条件查询资源列表 Handler: List GET /pm/adhoc/tasks?mode=\u0026status=\u0026limit=\u0026offset=\u0026all=true\n\nT-0164 收尾 G7-Gap-7：默认按 creator=current_user 过滤（\"我的任务\"），\nadmin 角色传 ?all=true 可看全部任务（运维 / 审计场景）。",
  "intents": [
    "性能 PM - 列表",
    "性能 PM：按筛选条件查询资源列表 Handler: List GET /pm/adhoc/tasks?mode=\u0026status=\u0026limit=\u0026offset=\u0026all=true\n\nT-0164 收尾 G7-Gap-7：默认按 creator=current_user 过滤（\"我的任务\"），\nadmin 角色传 ?all=true 可看全部任务（运维 / 审计场景）。",
    "pm",
    "性能",
    "adhoc",
    "tasks"
  ],
  "tags": [
    "pm",
    "性能",
    "adhoc",
    "tasks"
  ],
  "risk": "read",
  "confirmationRequired": false,
  "idempotent": true,
  "sideEffects": "无；该操作只读取当前用户可见数据。",
  "pathParams": [],
  "queryParams": [
    {
      "name": "all",
      "in": "query",
      "required": false,
      "type": "string"
    },
    {
      "name": "creator",
      "in": "query",
      "required": false,
      "type": "string"
    },
    {
      "name": "is_builtin",
      "in": "query",
      "required": false,
      "type": "string"
    },
    {
      "name": "limit",
      "in": "query",
      "required": false,
      "type": "string"
    },
    {
      "name": "mode",
      "in": "query",
      "required": false,
      "type": "string"
    },
    {
      "name": "offset",
      "in": "query",
      "required": false,
      "type": "string"
    },
    {
      "name": "status",
      "in": "query",
      "required": false,
      "type": "string"
    }
  ],
  "formParams": [],
  "responses": {
    "2xx": {
      "content": {
        "application/json": {
          "schema": {
            "properties": {
              "data": {
                "description": "该 Handler 返回的真实业务结果；以实际 API JSON 为准。"
              },
              "msg": {
                "example": "ok",
                "type": "string"
              },
              "ret": {
                "const": 1,
                "type": "integer"
              }
            },
            "required": [
              "ret",
              "msg",
              "data"
            ],
            "type": "object"
          }
        }
      },
      "description": "成功。JSON 接口使用 OMC 标准响应信封；文件或 SSE 接口按 Content-Type 返回二进制或事件流。"
    },
    "4xx": {
      "description": "请求参数、身份、权限或 Agent policy 不允许。不要绕过限制。"
    },
    "5xx": {
      "description": "OMC 内部处理失败。不要编造结果或自动重试非幂等写操作。"
    }
  },
  "contractCoverage": {
    "request": "go-handler",
    "response": "standard-envelope"
  },
  "emptyResult": "空数组或 total=0 表示当前用户权限和过滤条件下没有可见记录；除非接口另有明确语义，不要改用其他接口猜测数据。",
  "sources": [
    "runtime-route",
    "go-handler"
  ]
}
