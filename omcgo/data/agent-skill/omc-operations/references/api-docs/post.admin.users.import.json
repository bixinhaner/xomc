{
  "schemaVersion": "1.0",
  "operationId": "post.admin.users.import",
  "method": "POST",
  "path": "/api/v1/admin/users/import",
  "handler": "github.com/omcgo/omcgo/internal/admin.(*Handler).ImportUsers-fm",
  "category": "users",
  "title": "用户 - 导入",
  "summary": "用户 - 导入",
  "description": "用户：导入外部文件或业务数据 Handler: ImportUsers 解析 multipart/form-data 中的 xlsx 文件，逐行调用 service.CreateUser。\n部分行失败不会阻塞其他行；最终返回汇总。",
  "intents": [
    "用户 - 导入",
    "用户：导入外部文件或业务数据 Handler: ImportUsers 解析 multipart/form-data 中的 xlsx 文件，逐行调用 service.CreateUser。\n部分行失败不会阻塞其他行；最终返回汇总。",
    "users",
    "用户",
    "admin",
    "import"
  ],
  "tags": [
    "users",
    "用户",
    "admin",
    "import"
  ],
  "risk": "low",
  "confirmationRequired": true,
  "idempotent": false,
  "sideEffects": "会修改业务状态或触发任务；执行前确认目标、范围和影响。操作语义：用户 - 导入",
  "pathParams": [],
  "queryParams": [],
  "formParams": [
    {
      "name": "file",
      "in": "form",
      "required": false,
      "type": "file"
    }
  ],
  "responses": {
    "2xx": {
      "content": {
        "application/json": {
          "schema": {
            "properties": {
              "data": {
                "description": "该 Handler 返回的真实业务结果；以实际 API JSON 为准。"
              },
              "msg": {
                "example": "ok",
                "type": "string"
              },
              "ret": {
                "const": 1,
                "type": "integer"
              }
            },
            "required": [
              "ret",
              "msg",
              "data"
            ],
            "type": "object"
          }
        }
      },
      "description": "成功。JSON 接口使用 OMC 标准响应信封；文件或 SSE 接口按 Content-Type 返回二进制或事件流。"
    },
    "4xx": {
      "description": "请求参数、身份、权限或 Agent policy 不允许。不要绕过限制。"
    },
    "5xx": {
      "description": "OMC 内部处理失败。不要编造结果或自动重试非幂等写操作。"
    }
  },
  "contractCoverage": {
    "request": "go-handler",
    "response": "standard-envelope"
  },
  "sources": [
    "runtime-route",
    "go-handler"
  ]
}
