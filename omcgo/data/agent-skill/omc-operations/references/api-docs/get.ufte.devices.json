{
  "schemaVersion": "1.0",
  "operationId": "get.ufte.devices",
  "method": "GET",
  "path": "/api/v1/ufte/devices",
  "handler": "github.com/omcgo/omcgo/internal/ufte.(*Handler).ListDevices-fm",
  "category": "ufte",
  "title": "查询传输任务设备执行明细与失败原因",
  "summary": "查询传输任务设备执行明细与失败原因",
  "description": "可按 taskId、category、typeCode 和 status 筛选；失败记录返回 failureReason 和设备上报的原始 failureDetail，适用于日志收集、升级、备份和恢复任务排障。",
  "intents": [
    "ufte - 列表",
    "查询传输任务设备执行明细与失败原因",
    "可按 taskId、category、typeCode 和 status 筛选；失败记录返回 failureReason 和设备上报的原始 failureDetail，适用于日志收集、升级、备份和恢复任务排障。",
    "ufte",
    "统一文件传输",
    "devices"
  ],
  "tags": [
    "ufte",
    "统一文件传输",
    "devices"
  ],
  "risk": "read",
  "confirmationRequired": false,
  "idempotent": true,
  "sideEffects": "无；该操作只读取当前用户可见数据。",
  "pathParams": [],
  "queryParams": [
    {
      "name": "category",
      "in": "query",
      "required": false,
      "type": "string",
      "description": "业务分类，如日志收集 station_log。",
      "schema": {
        "description": "业务分类，如日志收集 station_log。",
        "type": "string"
      }
    },
    {
      "name": "keyword",
      "in": "query",
      "required": false,
      "type": "string",
      "description": "匹配任务名、设备名、设备 SN 或产品类型。",
      "schema": {
        "description": "匹配任务名、设备名、设备 SN 或产品类型。",
        "type": "string"
      }
    },
    {
      "name": "page",
      "in": "query",
      "required": false,
      "type": "integer",
      "schema": {
        "goType": "int",
        "type": "integer"
      }
    },
    {
      "name": "page_size",
      "in": "query",
      "required": false,
      "type": "integer",
      "schema": {
        "goType": "int",
        "type": "integer"
      }
    },
    {
      "name": "productName",
      "in": "query",
      "required": false,
      "type": "string",
      "description": "ProductName #524：设备列表筛选改按产品名（DeviceItem.ProductName 由 mapDeviceItem\n经 productNameLookup 回填）。前端「产品名称」下拉传此参数；与 ProductType(productClass)\n二选一，优先 ProductName。列表与 CSV 导出共用本过滤器，故两条路径同时生效。",
      "schema": {
        "description": "ProductName #524：设备列表筛选改按产品名（DeviceItem.ProductName 由 mapDeviceItem\n经 productNameLookup 回填）。前端「产品名称」下拉传此参数；与 ProductType(productClass)\n二选一，优先 ProductName。列表与 CSV 导出共用本过滤器，故两条路径同时生效。",
        "type": "string"
      }
    },
    {
      "name": "productType",
      "in": "query",
      "required": false,
      "type": "string",
      "schema": {
        "type": "string"
      }
    },
    {
      "name": "status",
      "in": "query",
      "required": false,
      "type": "string",
      "description": "设备执行状态；failed 用于定位失败记录。",
      "schema": {
        "description": "设备执行状态；failed 用于定位失败记录。",
        "type": "string"
      }
    },
    {
      "name": "taskId",
      "in": "query",
      "required": false,
      "type": "string",
      "description": "TaskID #615：按主任务 ID 精确收窄（任务详情抽屉「已选设备列表」用）。\n复用 /ufte/devices 端点 + matchesDeviceFilter，不新增路由；空值 ≡ 原有\"全量\"行为，\n完全向后兼容。底层来自 upgrade_sub_tasks.task_id（mapDeviceItem 已回填到 DeviceItem.TaskID）。",
      "schema": {
        "description": "TaskID #615：按主任务 ID 精确收窄（任务详情抽屉「已选设备列表」用）。\n复用 /ufte/devices 端点 + matchesDeviceFilter，不新增路由；空值 ≡ 原有\"全量\"行为，\n完全向后兼容。底层来自 upgrade_sub_tasks.task_id（mapDeviceItem 已回填到 DeviceItem.TaskID）。",
        "type": "string"
      }
    },
    {
      "name": "typeCode",
      "in": "query",
      "required": false,
      "type": "string",
      "description": "任务类型，如 RUNTIME_LOG_COLLECT。",
      "schema": {
        "description": "任务类型，如 RUNTIME_LOG_COLLECT。",
        "type": "string"
      }
    }
  ],
  "formParams": [],
  "responses": {
    "2xx": {
      "content": {
        "application/json": {
          "schema": {
            "properties": {
              "data": {
                "description": "该 Handler 返回的真实业务结果；以实际 API JSON 为准。"
              },
              "msg": {
                "example": "ok",
                "type": "string"
              },
              "ret": {
                "const": 1,
                "type": "integer"
              }
            },
            "required": [
              "ret",
              "msg",
              "data"
            ],
            "type": "object"
          }
        }
      },
      "description": "成功。JSON 接口使用 OMC 标准响应信封；文件或 SSE 接口按 Content-Type 返回二进制或事件流。"
    },
    "4xx": {
      "description": "请求参数、身份、权限或 Agent policy 不允许。不要绕过限制。"
    },
    "5xx": {
      "description": "OMC 内部处理失败。不要编造结果或自动重试非幂等写操作。"
    }
  },
  "contractCoverage": {
    "request": "go-handler",
    "response": "standard-envelope"
  },
  "emptyResult": "空数组或 total=0 只说明该操作在当前用户权限和本次过滤条件下没有返回记录，不能单独证明业务对象、源数据或系统能力不存在。先核对参数、标识符、时间范围和权限；若用户目标仍未回答，再沿 relatedOperations 选择汇总、明细、原始或派生数据操作补充证据。",
  "relatedOperations": [
    {
      "operationId": "get.ufte.devices.export",
      "method": "GET",
      "path": "/api/v1/ufte/devices/export",
      "relation": "same-resource"
    },
    {
      "operationId": "get.ufte.device_candidates",
      "method": "GET",
      "path": "/api/v1/ufte/device-candidates",
      "relation": "same-domain"
    },
    {
      "operationId": "get.ufte.overview",
      "method": "GET",
      "path": "/api/v1/ufte/overview",
      "relation": "same-domain"
    },
    {
      "operationId": "get.ufte.task_types",
      "method": "GET",
      "path": "/api/v1/ufte/task-types",
      "relation": "same-domain"
    },
    {
      "operationId": "get.ufte.tasks",
      "method": "GET",
      "path": "/api/v1/ufte/tasks",
      "relation": "same-domain"
    }
  ],
  "sources": [
    "runtime-route",
    "go-handler"
  ]
}
