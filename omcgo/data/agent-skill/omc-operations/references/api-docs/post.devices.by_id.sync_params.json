{
  "schemaVersion": "1.0",
  "operationId": "post.devices.by_id.sync_params",
  "method": "POST",
  "path": "/api/v1/devices/:id/sync-params",
  "handler": "github.com/omcgo/omcgo/internal/device.(*Handler).SyncDeviceParams-fm",
  "category": "devices",
  "title": "设备 - 同步参数",
  "summary": "设备 - 同步参数",
  "description": "设备：处理同步参数相关数据 Handler: SyncDeviceParams T-0126: 手动触发设备参数 Path B 全量同步。\n\nPOST /api/v1/devices/:id/sync-params\nBody (可选)：{\"force\": true}（预留供未来节流绕过；当前 manual 端点天然不走 Redis 节流）\n响应 202：{\"status\": \"queued\", \"source_id\": \"manual:UUID\", \"device_id\": \"UUID\"}\n响应 404：device 不存在\n响应 503：Path B 不可用（设备 productClass 未路由 / MappingSet 缺失）\n\n替代旧 /param-sync 端点（Path A 已下线），完整接入触发链：\nreason=\"manual\" → 差异日志 (T-0127) + last_param_sync_at 回写 (T-0124) + Translator (T-0098)。",
  "intents": [
    "设备 - 同步参数",
    "设备：处理同步参数相关数据 Handler: SyncDeviceParams T-0126: 手动触发设备参数 Path B 全量同步。\n\nPOST /api/v1/devices/:id/sync-params\nBody (可选)：{\"force\": true}（预留供未来节流绕过；当前 manual 端点天然不走 Redis 节流）\n响应 202：{\"status\": \"queued\", \"source_id\": \"manual:UUID\", \"device_id\": \"UUID\"}\n响应 404：device 不存在\n响应 503：Path B 不可用（设备 productClass 未路由 / MappingSet 缺失）\n\n替代旧 /param-sync 端点（Path A 已下线），完整接入触发链：\nreason=\"manual\" → 差异日志 (T-0127) + last_param_sync_at 回写 (T-0124) + Translator (T-0098)。",
    "devices",
    "设备",
    "sync-params",
    "sync params"
  ],
  "tags": [
    "devices",
    "设备",
    "sync-params"
  ],
  "risk": "low",
  "confirmationRequired": true,
  "idempotent": false,
  "sideEffects": "会修改业务状态或触发任务；执行前确认目标、范围和影响。操作语义：设备 - 同步参数",
  "pathParams": [
    {
      "name": "id",
      "in": "path",
      "required": true,
      "type": "string",
      "description": "替换 URL 路径中的 :id"
    }
  ],
  "queryParams": [],
  "formParams": [],
  "requestBody": {
    "contentType": "application/json",
    "required": true,
    "schema": {
      "properties": {
        "force": {
          "type": "boolean"
        },
        "parameter_paths": {
          "items": {
            "type": "string"
          },
          "type": "array"
        }
      },
      "type": "object"
    }
  },
  "responses": {
    "2xx": {
      "content": {
        "application/json": {
          "schema": {
            "properties": {
              "data": {
                "description": "该 Handler 返回的真实业务结果；以实际 API JSON 为准。"
              },
              "msg": {
                "example": "ok",
                "type": "string"
              },
              "ret": {
                "const": 1,
                "type": "integer"
              }
            },
            "required": [
              "ret",
              "msg",
              "data"
            ],
            "type": "object"
          }
        }
      },
      "description": "成功。JSON 接口使用 OMC 标准响应信封；文件或 SSE 接口按 Content-Type 返回二进制或事件流。"
    },
    "4xx": {
      "description": "请求参数、身份、权限或 Agent policy 不允许。不要绕过限制。"
    },
    "5xx": {
      "description": "OMC 内部处理失败。不要编造结果或自动重试非幂等写操作。"
    }
  },
  "contractCoverage": {
    "request": "go-handler",
    "response": "standard-envelope"
  },
  "sources": [
    "runtime-route",
    "go-handler"
  ]
}
