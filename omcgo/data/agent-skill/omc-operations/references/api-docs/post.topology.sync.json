{
  "schemaVersion": "1.0",
  "operationId": "post.topology.sync",
  "method": "POST",
  "path": "/api/v1/topology/sync",
  "handler": "github.com/omcgo/omcgo/internal/topology.(*Handler).SyncDevicesFromTopology-fm",
  "category": "topology",
  "title": "拓扑 - 同步",
  "summary": "拓扑 - 同步",
  "description": "拓扑：从运行态或外部系统同步最新数据 Handler: SyncDevicesFromTopology handles POST /api/v1/topology/sync.\nSynchronizes devices from the devices table to topology nodes.",
  "intents": [
    "拓扑 - 同步",
    "拓扑：从运行态或外部系统同步最新数据 Handler: SyncDevicesFromTopology handles POST /api/v1/topology/sync.\nSynchronizes devices from the devices table to topology nodes.",
    "topology",
    "拓扑",
    "sync"
  ],
  "tags": [
    "topology",
    "拓扑",
    "sync"
  ],
  "risk": "low",
  "confirmationRequired": true,
  "idempotent": false,
  "sideEffects": "会修改业务状态或触发任务；执行前确认目标、范围和影响。操作语义：拓扑 - 同步",
  "pathParams": [],
  "queryParams": [
    {
      "name": "domain_id",
      "in": "query",
      "required": false,
      "type": "string"
    },
    {
      "name": "limit",
      "in": "query",
      "required": false,
      "type": "string"
    }
  ],
  "formParams": [],
  "responses": {
    "2xx": {
      "content": {
        "application/json": {
          "schema": {
            "properties": {
              "data": {
                "description": "该 Handler 返回的真实业务结果；以实际 API JSON 为准。"
              },
              "msg": {
                "example": "ok",
                "type": "string"
              },
              "ret": {
                "const": 1,
                "type": "integer"
              }
            },
            "required": [
              "ret",
              "msg",
              "data"
            ],
            "type": "object"
          }
        }
      },
      "description": "成功。JSON 接口使用 OMC 标准响应信封；文件或 SSE 接口按 Content-Type 返回二进制或事件流。"
    },
    "4xx": {
      "description": "请求参数、身份、权限或 Agent policy 不允许。不要绕过限制。"
    },
    "5xx": {
      "description": "OMC 内部处理失败。不要编造结果或自动重试非幂等写操作。"
    }
  },
  "contractCoverage": {
    "request": "go-handler",
    "response": "standard-envelope"
  },
  "sources": [
    "runtime-route",
    "go-handler"
  ]
}
