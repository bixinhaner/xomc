{
  "schemaVersion": "1.0",
  "operationId": "delete.pm.adhoc.tasks.by_id.definition",
  "method": "DELETE",
  "path": "/api/v1/pm/adhoc/tasks/:id/definition",
  "handler": "github.com/omcgo/omcgo/internal/pm/adhoc.(*Handler).Delete-fm",
  "category": "pm",
  "title": "性能 PM - DELETE",
  "summary": "性能 PM - DELETE",
  "description": "性能 PM：执行 DELETE 请求对应的业务操作 Handler: Delete DELETE /pm/adhoc/tasks/:id/definition\n\n硬删终态（succeeded/failed/canceled）自建（is_builtin=false）adhoc 任务的定义行（#392）。\n只删 pm_tasks 定义行，结果数据交 TimescaleDB retention 自然过期（不级联删）。\n  - 非终态（pending/running/scheduled）任务返 409（仍活跃，应走「取消」）。\n  - 内置任务返 403（永不可删）。\n  - 行不存在返 404。",
  "intents": [
    "性能 PM - DELETE",
    "性能 PM：执行 DELETE 请求对应的业务操作 Handler: Delete DELETE /pm/adhoc/tasks/:id/definition\n\n硬删终态（succeeded/failed/canceled）自建（is_builtin=false）adhoc 任务的定义行（#392）。\n只删 pm_tasks 定义行，结果数据交 TimescaleDB retention 自然过期（不级联删）。\n  - 非终态（pending/running/scheduled）任务返 409（仍活跃，应走「取消」）。\n  - 内置任务返 403（永不可删）。\n  - 行不存在返 404。",
    "pm",
    "性能",
    "adhoc",
    "tasks",
    "definition"
  ],
  "tags": [
    "pm",
    "性能",
    "adhoc",
    "tasks",
    "definition"
  ],
  "risk": "high",
  "confirmationRequired": true,
  "idempotent": true,
  "sideEffects": "会修改业务状态或触发任务；执行前确认目标、范围和影响。操作语义：性能 PM - DELETE",
  "pathParams": [
    {
      "name": "id",
      "in": "path",
      "required": true,
      "type": "string",
      "description": "替换 URL 路径中的 :id"
    }
  ],
  "queryParams": [],
  "formParams": [],
  "responses": {
    "2xx": {
      "content": {
        "application/json": {
          "schema": {
            "properties": {
              "data": {
                "description": "该 Handler 返回的真实业务结果；以实际 API JSON 为准。"
              },
              "msg": {
                "example": "ok",
                "type": "string"
              },
              "ret": {
                "const": 1,
                "type": "integer"
              }
            },
            "required": [
              "ret",
              "msg",
              "data"
            ],
            "type": "object"
          }
        }
      },
      "description": "成功。JSON 接口使用 OMC 标准响应信封；文件或 SSE 接口按 Content-Type 返回二进制或事件流。"
    },
    "4xx": {
      "description": "请求参数、身份、权限或 Agent policy 不允许。不要绕过限制。"
    },
    "5xx": {
      "description": "OMC 内部处理失败。不要编造结果或自动重试非幂等写操作。"
    }
  },
  "contractCoverage": {
    "request": "path-only",
    "response": "standard-envelope"
  },
  "sources": [
    "runtime-route",
    "go-handler"
  ]
}
