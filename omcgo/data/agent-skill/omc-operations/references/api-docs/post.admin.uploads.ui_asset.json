{
  "schemaVersion": "1.0",
  "operationId": "post.admin.uploads.ui_asset",
  "method": "POST",
  "path": "/api/v1/admin/uploads/ui-asset",
  "handler": "github.com/omcgo/omcgo/internal/admin.(*UIAssetHandler).Upload-fm",
  "category": "uploads",
  "title": "uploads - uiasset",
  "summary": "uploads - uiasset",
  "description": "uploads：处理uiasset相关数据 Handler: Upload 接收 multipart 文件 + kind 字段，写入 MinIO 后返回相对 URL。",
  "intents": [
    "uploads - uiasset",
    "uploads：处理uiasset相关数据 Handler: Upload 接收 multipart 文件 + kind 字段，写入 MinIO 后返回相对 URL。",
    "uploads",
    "admin",
    "ui-asset",
    "ui asset"
  ],
  "tags": [
    "uploads",
    "admin",
    "ui-asset"
  ],
  "risk": "low",
  "confirmationRequired": true,
  "idempotent": false,
  "sideEffects": "会修改业务状态或触发任务；执行前确认目标、范围和影响。操作语义：uploads - uiasset",
  "pathParams": [],
  "queryParams": [],
  "formParams": [
    {
      "name": "file",
      "in": "form",
      "required": false,
      "type": "file"
    },
    {
      "name": "kind",
      "in": "form",
      "required": false,
      "type": "string"
    }
  ],
  "responses": {
    "2xx": {
      "content": {
        "application/json": {
          "schema": {
            "properties": {
              "data": {
                "description": "该 Handler 返回的真实业务结果；以实际 API JSON 为准。"
              },
              "msg": {
                "example": "ok",
                "type": "string"
              },
              "ret": {
                "const": 1,
                "type": "integer"
              }
            },
            "required": [
              "ret",
              "msg",
              "data"
            ],
            "type": "object"
          }
        }
      },
      "description": "成功。JSON 接口使用 OMC 标准响应信封；文件或 SSE 接口按 Content-Type 返回二进制或事件流。"
    },
    "4xx": {
      "description": "请求参数、身份、权限或 Agent policy 不允许。不要绕过限制。"
    },
    "5xx": {
      "description": "OMC 内部处理失败。不要编造结果或自动重试非幂等写操作。"
    }
  },
  "contractCoverage": {
    "request": "go-handler",
    "response": "standard-envelope"
  },
  "sources": [
    "runtime-route",
    "go-handler"
  ]
}
