{
  "schemaVersion": "1.0",
  "operationId": "put.mr.mappings.by_id",
  "method": "PUT",
  "path": "/api/v1/mr/mappings/:id",
  "handler": "github.com/omcgo/omcgo/internal/mr.(*Handler).UpdateMapping-fm",
  "category": "mr",
  "title": "测量报告 MR - 映射更新",
  "summary": "测量报告 MR - 映射更新",
  "description": "测量报告 MR：查询或维护指定对象的映射更新数据 Handler: UpdateMapping handles PUT /api/v1/mr/mappings/:id.",
  "intents": [
    "测量报告 MR - 映射更新",
    "测量报告 MR：查询或维护指定对象的映射更新数据 Handler: UpdateMapping handles PUT /api/v1/mr/mappings/:id.",
    "mr",
    "测量报告",
    "mappings"
  ],
  "tags": [
    "mr",
    "测量报告",
    "mappings"
  ],
  "risk": "low",
  "confirmationRequired": true,
  "idempotent": true,
  "sideEffects": "会修改业务状态或触发任务；执行前确认目标、范围和影响。操作语义：测量报告 MR - 映射更新",
  "pathParams": [
    {
      "name": "id",
      "in": "path",
      "required": true,
      "type": "string",
      "description": "替换 URL 路径中的 :id"
    }
  ],
  "queryParams": [],
  "formParams": [],
  "requestBody": {
    "contentType": "application/json",
    "required": true,
    "schema": {
      "goType": "updateMappingRequest",
      "properties": {
        "cell_id": {
          "type": "string"
        },
        "cell_name": {
          "nullable": true,
          "type": "string"
        },
        "device_name": {
          "nullable": true,
          "type": "string"
        },
        "device_sn": {
          "type": "string"
        },
        "enabled": {
          "type": "boolean"
        },
        "sampling_interval": {
          "goType": "int",
          "type": "integer"
        }
      },
      "type": "object"
    }
  },
  "responses": {
    "2xx": {
      "content": {
        "application/json": {
          "schema": {
            "properties": {
              "data": {
                "description": "该 Handler 返回的真实业务结果；以实际 API JSON 为准。"
              },
              "msg": {
                "example": "ok",
                "type": "string"
              },
              "ret": {
                "const": 1,
                "type": "integer"
              }
            },
            "required": [
              "ret",
              "msg",
              "data"
            ],
            "type": "object"
          }
        }
      },
      "description": "成功。JSON 接口使用 OMC 标准响应信封；文件或 SSE 接口按 Content-Type 返回二进制或事件流。"
    },
    "4xx": {
      "description": "请求参数、身份、权限或 Agent policy 不允许。不要绕过限制。"
    },
    "5xx": {
      "description": "OMC 内部处理失败。不要编造结果或自动重试非幂等写操作。"
    }
  },
  "contractCoverage": {
    "request": "go-handler",
    "response": "standard-envelope"
  },
  "sources": [
    "runtime-route",
    "go-handler"
  ]
}
