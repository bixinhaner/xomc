{
  "schemaVersion": "1.0",
  "operationId": "get.upgrade_tasks",
  "method": "GET",
  "path": "/api/v1/upgrade-tasks",
  "handler": "github.com/omcgo/omcgo/internal/software.(*Handler).ListUpgradeTasks-fm",
  "category": "upgrade-tasks",
  "title": "升级任务 - 列表",
  "summary": "升级任务 - 列表",
  "description": "升级任务：按筛选条件查询资源列表",
  "intents": [
    "升级任务 - 列表",
    "升级任务：按筛选条件查询资源列表",
    "upgrade-tasks",
    "升级任务",
    "upgrade tasks"
  ],
  "tags": [
    "upgrade-tasks",
    "升级任务"
  ],
  "risk": "read",
  "confirmationRequired": false,
  "idempotent": true,
  "sideEffects": "无；该操作只读取当前用户可见数据。",
  "pathParams": [],
  "queryParams": [
    {
      "name": "create_user",
      "in": "query",
      "required": false,
      "type": "string",
      "schema": {
        "nullable": true,
        "type": "string"
      }
    },
    {
      "name": "page",
      "in": "query",
      "required": false,
      "type": "integer",
      "schema": {
        "goType": "int",
        "minimum": 1,
        "type": "integer"
      }
    },
    {
      "name": "page_size",
      "in": "query",
      "required": false,
      "type": "integer",
      "schema": {
        "goType": "int",
        "maximum": 1000,
        "minimum": 1,
        "type": "integer"
      }
    },
    {
      "name": "product_class",
      "in": "query",
      "required": false,
      "type": "string",
      "schema": {
        "nullable": true,
        "type": "string"
      }
    },
    {
      "name": "product_id",
      "in": "query",
      "required": false,
      "type": "string",
      "description": "ProductID #638：按产品名过滤升级任务。任务本身不存 product_id（来自 firmware），\nrepository 用 JOIN firmware_versions 命中 :pid = ANY(fv.product_ids)；用 string 而非\n*uuid.UUID —— gin query 绑定不支持 uuid，repository 层再 uuid.Parse，解析失败跳过。",
      "schema": {
        "description": "ProductID #638：按产品名过滤升级任务。任务本身不存 product_id（来自 firmware），\nrepository 用 JOIN firmware_versions 命中 :pid = ANY(fv.product_ids)；用 string 而非\n*uuid.UUID —— gin query 绑定不支持 uuid，repository 层再 uuid.Parse，解析失败跳过。",
        "type": "string"
      }
    },
    {
      "name": "sort_by",
      "in": "query",
      "required": false,
      "type": "string",
      "schema": {
        "type": "string"
      }
    },
    {
      "name": "sort_dir",
      "in": "query",
      "required": false,
      "type": "string",
      "enum": [
        "asc",
        "desc"
      ],
      "schema": {
        "enum": [
          "asc",
          "desc"
        ],
        "type": "string"
      }
    },
    {
      "name": "status",
      "in": "query",
      "required": false,
      "type": "string",
      "schema": {
        "goType": "TaskStatus",
        "nullable": true,
        "type": "string"
      }
    },
    {
      "name": "task_type",
      "in": "query",
      "required": false,
      "type": "integer",
      "schema": {
        "goType": "TaskType",
        "nullable": true,
        "type": "integer"
      }
    }
  ],
  "formParams": [],
  "responses": {
    "2xx": {
      "content": {
        "application/json": {
          "schema": {
            "properties": {
              "data": {
                "description": "该 Handler 返回的真实业务结果；以实际 API JSON 为准。"
              },
              "msg": {
                "example": "ok",
                "type": "string"
              },
              "ret": {
                "const": 1,
                "type": "integer"
              }
            },
            "required": [
              "ret",
              "msg",
              "data"
            ],
            "type": "object"
          }
        }
      },
      "description": "成功。JSON 接口使用 OMC 标准响应信封；文件或 SSE 接口按 Content-Type 返回二进制或事件流。"
    },
    "4xx": {
      "description": "请求参数、身份、权限或 Agent policy 不允许。不要绕过限制。"
    },
    "5xx": {
      "description": "OMC 内部处理失败。不要编造结果或自动重试非幂等写操作。"
    }
  },
  "contractCoverage": {
    "request": "go-handler",
    "response": "standard-envelope"
  },
  "emptyResult": "空数组或 total=0 表示当前用户权限和过滤条件下没有可见记录；除非接口另有明确语义，不要改用其他接口猜测数据。",
  "sources": [
    "runtime-route",
    "go-handler"
  ]
}
