{
  "schemaVersion": "1.0",
  "operationId": "post.device_groups.move_devices",
  "method": "POST",
  "path": "/api/v1/device-groups/move-devices",
  "handler": "github.com/omcgo/omcgo/internal/topology.(*Handler).MoveDevicesHandler-fm",
  "category": "device-groups",
  "title": "设备分组 - 移动设备",
  "summary": "设备分组 - 移动设备",
  "description": "设备分组：将设备移动到目标分组 Handler: MoveDevicesHandler handles POST /device-groups/move-devices.",
  "intents": [
    "设备分组 - 移动设备",
    "设备分组：将设备移动到目标分组 Handler: MoveDevicesHandler handles POST /device-groups/move-devices.",
    "device-groups",
    "设备分组",
    "move-devices",
    "device groups",
    "move devices"
  ],
  "tags": [
    "device-groups",
    "设备分组",
    "move-devices"
  ],
  "risk": "low",
  "confirmationRequired": true,
  "idempotent": false,
  "sideEffects": "会修改业务状态或触发任务；执行前确认目标、范围和影响。操作语义：设备分组 - 移动设备",
  "pathParams": [],
  "queryParams": [],
  "formParams": [],
  "requestBody": {
    "contentType": "application/json",
    "required": true,
    "schema": {
      "goType": "MoveDevicesRequest",
      "properties": {
        "device_ids": {
          "items": {
            "type": "string"
          },
          "type": "array"
        },
        "target_group_id": {
          "type": "string"
        }
      },
      "required": [
        "device_ids",
        "target_group_id"
      ],
      "type": "object"
    }
  },
  "responses": {
    "2xx": {
      "content": {
        "application/json": {
          "schema": {
            "properties": {
              "data": {
                "description": "该 Handler 返回的真实业务结果；以实际 API JSON 为准。"
              },
              "msg": {
                "example": "ok",
                "type": "string"
              },
              "ret": {
                "const": 1,
                "type": "integer"
              }
            },
            "required": [
              "ret",
              "msg",
              "data"
            ],
            "type": "object"
          }
        }
      },
      "description": "成功。JSON 接口使用 OMC 标准响应信封；文件或 SSE 接口按 Content-Type 返回二进制或事件流。"
    },
    "4xx": {
      "description": "请求参数、身份、权限或 Agent policy 不允许。不要绕过限制。"
    },
    "5xx": {
      "description": "OMC 内部处理失败。不要编造结果或自动重试非幂等写操作。"
    }
  },
  "contractCoverage": {
    "request": "go-handler",
    "response": "standard-envelope"
  },
  "sources": [
    "runtime-route",
    "go-handler"
  ]
}
