{
  "schemaVersion": "1.0",
  "operationId": "post.mml.templates.by_id.paths.batch",
  "method": "POST",
  "path": "/api/v1/mml/templates/:id/paths/batch",
  "handler": "github.com/omcgo/omcgo/internal/mml.(*Handler).BatchAddTemplatePaths-fm",
  "category": "mml",
  "title": "MML - 批量",
  "summary": "MML - 批量",
  "description": "MML：对多个对象执行批量操作 Handler: BatchAddTemplatePaths handles POST /api/v1/mml/templates/:id/paths/batch.\n批量追加 path（owner/super 才能写）。已关联的 path 静默跳过。",
  "intents": [
    "MML - 批量",
    "MML：对多个对象执行批量操作 Handler: BatchAddTemplatePaths handles POST /api/v1/mml/templates/:id/paths/batch.\n批量追加 path（owner/super 才能写）。已关联的 path 静默跳过。",
    "mml",
    "templates",
    "paths",
    "batch"
  ],
  "tags": [
    "mml",
    "templates",
    "paths",
    "batch"
  ],
  "risk": "low",
  "confirmationRequired": true,
  "idempotent": false,
  "sideEffects": "会修改业务状态或触发任务；执行前确认目标、范围和影响。操作语义：MML - 批量",
  "pathParams": [
    {
      "name": "id",
      "in": "path",
      "required": true,
      "type": "string",
      "description": "替换 URL 路径中的 :id"
    }
  ],
  "queryParams": [],
  "formParams": [],
  "requestBody": {
    "contentType": "application/json",
    "required": true,
    "schema": {
      "goType": "BatchAddTemplatePathsRequest",
      "properties": {
        "standard_path_ids": {
          "items": {
            "format": "uuid",
            "goType": "uuid.UUID",
            "type": "string"
          },
          "maxItems": 200,
          "minItems": 1,
          "type": "array"
        }
      },
      "required": [
        "standard_path_ids"
      ],
      "type": "object"
    }
  },
  "responses": {
    "2xx": {
      "content": {
        "application/json": {
          "schema": {
            "properties": {
              "data": {
                "description": "该 Handler 返回的真实业务结果；以实际 API JSON 为准。"
              },
              "msg": {
                "example": "ok",
                "type": "string"
              },
              "ret": {
                "const": 1,
                "type": "integer"
              }
            },
            "required": [
              "ret",
              "msg",
              "data"
            ],
            "type": "object"
          }
        }
      },
      "description": "成功。JSON 接口使用 OMC 标准响应信封；文件或 SSE 接口按 Content-Type 返回二进制或事件流。"
    },
    "4xx": {
      "description": "请求参数、身份、权限或 Agent policy 不允许。不要绕过限制。"
    },
    "5xx": {
      "description": "OMC 内部处理失败。不要编造结果或自动重试非幂等写操作。"
    }
  },
  "contractCoverage": {
    "request": "go-handler",
    "response": "standard-envelope"
  },
  "sources": [
    "runtime-route",
    "go-handler"
  ]
}
