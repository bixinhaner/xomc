{
  "schemaVersion": "1.0",
  "operationId": "post.indicator_groups",
  "method": "POST",
  "path": "/api/v1/indicator-groups",
  "handler": "github.com/omcgo/omcgo/internal/pm/indicator.(*RESTHandler).CreateGroup-fm",
  "category": "indicator-groups",
  "title": "indicator-groups - 创建",
  "summary": "indicator-groups - 创建",
  "description": "indicator-groups：创建新的资源记录",
  "intents": [
    "indicator-groups - 创建",
    "indicator-groups：创建新的资源记录",
    "indicator-groups",
    "indicator groups"
  ],
  "tags": [
    "indicator-groups"
  ],
  "risk": "low",
  "confirmationRequired": true,
  "idempotent": false,
  "sideEffects": "会修改业务状态或触发任务；执行前确认目标、范围和影响。操作语义：indicator-groups - 创建",
  "pathParams": [],
  "queryParams": [],
  "formParams": [],
  "requestBody": {
    "contentType": "application/json",
    "required": true,
    "schema": {
      "goType": "CreateGroupRequest",
      "properties": {
        "cn_name": {
          "type": "string"
        },
        "device_type": {
          "description": "DeviceType 在 REST/GNB 入口由 query/路由单一注入，bind 后覆盖；\n故此处不可 binding:required（校验早于覆盖会误拒），改 omitempty 仅做枚举校验。\n真正的空值/非法值由 service.CreateGroup → ParseDeviceType 兜底（映射 400 ErrInvalidInput）。",
          "enum": [
            "ENB",
            "GSM",
            "GNB"
          ],
          "type": "string"
        },
        "en_name": {
          "type": "string"
        },
        "operator_code": {
          "type": "string"
        },
        "parent_id": {
          "type": "string"
        }
      },
      "required": [
        "parent_id"
      ],
      "type": "object"
    }
  },
  "responses": {
    "2xx": {
      "content": {
        "application/json": {
          "schema": {
            "properties": {
              "data": {
                "description": "该 Handler 返回的真实业务结果；以实际 API JSON 为准。"
              },
              "msg": {
                "example": "ok",
                "type": "string"
              },
              "ret": {
                "const": 1,
                "type": "integer"
              }
            },
            "required": [
              "ret",
              "msg",
              "data"
            ],
            "type": "object"
          }
        }
      },
      "description": "成功。JSON 接口使用 OMC 标准响应信封；文件或 SSE 接口按 Content-Type 返回二进制或事件流。"
    },
    "4xx": {
      "description": "请求参数、身份、权限或 Agent policy 不允许。不要绕过限制。"
    },
    "5xx": {
      "description": "OMC 内部处理失败。不要编造结果或自动重试非幂等写操作。"
    }
  },
  "contractCoverage": {
    "request": "go-handler",
    "response": "standard-envelope"
  },
  "sources": [
    "runtime-route",
    "go-handler"
  ]
}
