{
  "schemaVersion": "1.0",
  "operationId": "post.pm.thresholds",
  "method": "POST",
  "path": "/api/v1/pm/thresholds",
  "handler": "github.com/omcgo/omcgo/internal/pm.(*ThresholdHandler).CreateThreshold-fm",
  "category": "pm",
  "title": "Create a KPI threshold",
  "summary": "Create a KPI threshold",
  "description": "性能 PM：查询或维护阈值配置 Handler: CreateThreshold handles POST /thresholds.",
  "intents": [
    "性能 PM - 阈值",
    "Create a KPI threshold",
    "性能 PM：查询或维护阈值配置 Handler: CreateThreshold handles POST /thresholds.",
    "pm",
    "性能",
    "thresholds"
  ],
  "tags": [
    "pm",
    "性能",
    "thresholds",
    "KPIThresholds"
  ],
  "risk": "low",
  "confirmationRequired": true,
  "idempotent": false,
  "sideEffects": "会修改业务状态或触发任务；执行前确认目标、范围和影响。操作语义：Create a KPI threshold",
  "pathParams": [],
  "queryParams": [],
  "formParams": [],
  "requestBody": {
    "contentType": "application/json",
    "required": true,
    "schema": {
      "$ref": "#/components/schemas/CreateKPIThresholdRequest"
    }
  },
  "responses": {
    "201": {
      "content": {
        "application/json": {
          "schema": {
            "$ref": "#/components/schemas/KPIThreshold"
          }
        }
      },
      "description": "KPI threshold created"
    },
    "400": {
      "$ref": "#/components/responses/BadRequest"
    },
    "401": {
      "$ref": "#/components/responses/Unauthorized"
    }
  },
  "referencedSchemas": {
    "CreateKPIThresholdRequest": {
      "properties": {
        "carrier": {
          "type": "string"
        },
        "comparison": {
          "enum": [
            "gt",
            "gte",
            "lt",
            "lte",
            "eq"
          ],
          "type": "string"
        },
        "critical_threshold": {
          "format": "double",
          "type": "number"
        },
        "enabled": {
          "default": true,
          "type": "boolean"
        },
        "kpi_name": {
          "type": "string"
        },
        "technology": {
          "type": "string"
        },
        "warning_threshold": {
          "format": "double",
          "type": "number"
        }
      },
      "required": [
        "kpi_name",
        "warning_threshold",
        "critical_threshold",
        "comparison"
      ],
      "type": "object"
    },
    "KPIThreshold": {
      "properties": {
        "carrier": {
          "type": "string"
        },
        "comparison": {
          "enum": [
            "gt",
            "gte",
            "lt",
            "lte",
            "eq"
          ],
          "type": "string"
        },
        "created_at": {
          "format": "date-time",
          "type": "string"
        },
        "critical_threshold": {
          "format": "double",
          "type": "number"
        },
        "enabled": {
          "type": "boolean"
        },
        "id": {
          "format": "uuid",
          "type": "string"
        },
        "kpi_name": {
          "type": "string"
        },
        "technology": {
          "type": "string"
        },
        "updated_at": {
          "format": "date-time",
          "type": "string"
        },
        "warning_threshold": {
          "format": "double",
          "type": "number"
        }
      },
      "type": "object"
    }
  },
  "contractCoverage": {
    "request": "openapi",
    "response": "openapi"
  },
  "sources": [
    "runtime-route",
    "openapi",
    "go-handler"
  ]
}
