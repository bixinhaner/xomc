{
  "schemaVersion": "1.0",
  "operationId": "patch.mml.templates.by_id.paths.by_pathId",
  "method": "PATCH",
  "path": "/api/v1/mml/templates/:id/paths/:pathId",
  "handler": "github.com/omcgo/omcgo/internal/mml.(*Handler).UpdateTemplatePath-fm",
  "category": "mml",
  "title": "MML - 部分更新",
  "summary": "MML - 部分更新",
  "description": "MML：更新指定资源的部分字段 Handler: UpdateTemplatePath handles PATCH /api/v1/mml/templates/:id/paths/:pathId.\n改单条关联的 default_selected / sort_order（owner/super 才能写）。",
  "intents": [
    "MML - 部分更新",
    "MML：更新指定资源的部分字段 Handler: UpdateTemplatePath handles PATCH /api/v1/mml/templates/:id/paths/:pathId.\n改单条关联的 default_selected / sort_order（owner/super 才能写）。",
    "mml",
    "templates",
    "paths"
  ],
  "tags": [
    "mml",
    "templates",
    "paths"
  ],
  "risk": "low",
  "confirmationRequired": true,
  "idempotent": false,
  "sideEffects": "会修改业务状态或触发任务；执行前确认目标、范围和影响。操作语义：MML - 部分更新",
  "pathParams": [
    {
      "name": "id",
      "in": "path",
      "required": true,
      "type": "string",
      "description": "替换 URL 路径中的 :id"
    },
    {
      "name": "pathId",
      "in": "path",
      "required": true,
      "type": "string",
      "description": "替换 URL 路径中的 :pathId"
    }
  ],
  "queryParams": [],
  "formParams": [],
  "requestBody": {
    "contentType": "application/json",
    "required": true,
    "schema": {
      "goType": "UpdateTemplatePathRequest",
      "properties": {
        "default_selected": {
          "nullable": true,
          "type": "boolean"
        },
        "sort_order": {
          "goType": "int",
          "nullable": true,
          "type": "integer"
        }
      },
      "type": "object"
    }
  },
  "responses": {
    "2xx": {
      "content": {
        "application/json": {
          "schema": {
            "properties": {
              "data": {
                "description": "该 Handler 返回的真实业务结果；以实际 API JSON 为准。"
              },
              "msg": {
                "example": "ok",
                "type": "string"
              },
              "ret": {
                "const": 1,
                "type": "integer"
              }
            },
            "required": [
              "ret",
              "msg",
              "data"
            ],
            "type": "object"
          }
        }
      },
      "description": "成功。JSON 接口使用 OMC 标准响应信封；文件或 SSE 接口按 Content-Type 返回二进制或事件流。"
    },
    "4xx": {
      "description": "请求参数、身份、权限或 Agent policy 不允许。不要绕过限制。"
    },
    "5xx": {
      "description": "OMC 内部处理失败。不要编造结果或自动重试非幂等写操作。"
    }
  },
  "contractCoverage": {
    "request": "go-handler",
    "response": "standard-envelope"
  },
  "sources": [
    "runtime-route",
    "go-handler"
  ]
}
