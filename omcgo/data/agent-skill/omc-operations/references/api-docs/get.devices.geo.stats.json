{
  "schemaVersion": "1.0",
  "operationId": "get.devices.geo.stats",
  "method": "GET",
  "path": "/api/v1/devices/geo/stats",
  "handler": "github.com/omcgo/omcgo/internal/device.(*Handler).GetGeoStats-fm",
  "category": "devices",
  "title": "设备 - 统计",
  "summary": "设备 - 统计",
  "description": "设备：查询统计汇总数据 Handler: GetGeoStats handles GET /api/v1/devices/geo/stats.\nReturns device statistics for map display.\n\n入参与 ListGeo 对齐：group_ids / status 两个筛选都受 stats 影响，避免顶部统计带与地图点位不一致。\ngroup_ids 包含 DefaultLevel2GroupID 时由 service 层 splitGeoGroupIDs 归一化为未分组分支。",
  "intents": [
    "设备 - 统计",
    "设备：查询统计汇总数据 Handler: GetGeoStats handles GET /api/v1/devices/geo/stats.\nReturns device statistics for map display.\n\n入参与 ListGeo 对齐：group_ids / status 两个筛选都受 stats 影响，避免顶部统计带与地图点位不一致。\ngroup_ids 包含 DefaultLevel2GroupID 时由 service 层 splitGeoGroupIDs 归一化为未分组分支。",
    "devices",
    "设备",
    "geo",
    "stats"
  ],
  "tags": [
    "devices",
    "设备",
    "geo",
    "stats"
  ],
  "risk": "read",
  "confirmationRequired": false,
  "idempotent": true,
  "sideEffects": "无；该操作只读取当前用户可见数据。",
  "pathParams": [],
  "queryParams": [
    {
      "name": "group_ids",
      "in": "query",
      "required": false,
      "type": "string"
    },
    {
      "name": "status",
      "in": "query",
      "required": false,
      "type": "string"
    }
  ],
  "formParams": [],
  "responses": {
    "2xx": {
      "content": {
        "application/json": {
          "schema": {
            "properties": {
              "data": {
                "description": "该 Handler 返回的真实业务结果；以实际 API JSON 为准。"
              },
              "msg": {
                "example": "ok",
                "type": "string"
              },
              "ret": {
                "const": 1,
                "type": "integer"
              }
            },
            "required": [
              "ret",
              "msg",
              "data"
            ],
            "type": "object"
          }
        }
      },
      "description": "成功。JSON 接口使用 OMC 标准响应信封；文件或 SSE 接口按 Content-Type 返回二进制或事件流。"
    },
    "4xx": {
      "description": "请求参数、身份、权限或 Agent policy 不允许。不要绕过限制。"
    },
    "5xx": {
      "description": "OMC 内部处理失败。不要编造结果或自动重试非幂等写操作。"
    }
  },
  "contractCoverage": {
    "request": "go-handler",
    "response": "standard-envelope"
  },
  "emptyResult": "空数组或 total=0 表示当前用户权限和过滤条件下没有可见记录；除非接口另有明确语义，不要改用其他接口猜测数据。",
  "sources": [
    "runtime-route",
    "go-handler"
  ]
}
