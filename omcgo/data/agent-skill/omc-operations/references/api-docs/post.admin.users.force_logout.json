{
  "schemaVersion": "1.0",
  "operationId": "post.admin.users.force_logout",
  "method": "POST",
  "path": "/api/v1/admin/users/force-logout",
  "handler": "github.com/omcgo/omcgo/internal/admin.(*Handler).ForceLogout-fm",
  "category": "users",
  "title": "用户 - 强制下线",
  "summary": "用户 - 强制下线",
  "description": "强制指定用户会话失效 Handler: ForceLogout 强制指定用户下线（批量）。所有受影响 token 在下一次中间件校验时失败。",
  "intents": [
    "用户 - 强制下线",
    "强制指定用户会话失效 Handler: ForceLogout 强制指定用户下线（批量）。所有受影响 token 在下一次中间件校验时失败。",
    "users",
    "用户",
    "admin",
    "force-logout",
    "force logout"
  ],
  "tags": [
    "users",
    "用户",
    "admin",
    "force-logout"
  ],
  "risk": "low",
  "confirmationRequired": true,
  "idempotent": false,
  "sideEffects": "会修改业务状态或触发任务；执行前确认目标、范围和影响。操作语义：用户 - 强制下线",
  "pathParams": [],
  "queryParams": [],
  "formParams": [],
  "requestBody": {
    "contentType": "application/json",
    "required": true,
    "schema": {
      "goType": "ForceLogoutRequest",
      "properties": {
        "user_ids": {
          "items": {
            "format": "uuid",
            "goType": "uuid.UUID",
            "type": "string"
          },
          "minItems": 1,
          "type": "array"
        }
      },
      "required": [
        "user_ids"
      ],
      "type": "object"
    }
  },
  "responses": {
    "2xx": {
      "content": {
        "application/json": {
          "schema": {
            "properties": {
              "data": {
                "description": "该 Handler 返回的真实业务结果；以实际 API JSON 为准。"
              },
              "msg": {
                "example": "ok",
                "type": "string"
              },
              "ret": {
                "const": 1,
                "type": "integer"
              }
            },
            "required": [
              "ret",
              "msg",
              "data"
            ],
            "type": "object"
          }
        }
      },
      "description": "成功。JSON 接口使用 OMC 标准响应信封；文件或 SSE 接口按 Content-Type 返回二进制或事件流。"
    },
    "4xx": {
      "description": "请求参数、身份、权限或 Agent policy 不允许。不要绕过限制。"
    },
    "5xx": {
      "description": "OMC 内部处理失败。不要编造结果或自动重试非幂等写操作。"
    }
  },
  "contractCoverage": {
    "request": "go-handler",
    "response": "standard-envelope"
  },
  "sources": [
    "runtime-route",
    "go-handler"
  ]
}
