{
  "schemaVersion": "1.0",
  "operationId": "post.admin.sysDictionary.refreshSource",
  "method": "POST",
  "path": "/api/v1/admin/sysDictionary/refreshSource",
  "handler": "github.com/omcgo/omcgo/internal/admin.(*DictionaryHandler).RefreshSource-fm",
  "category": "sysDictionary",
  "title": "字典 - 创建",
  "summary": "字典 - 创建",
  "description": "字典：创建新的资源记录 Handler: RefreshSource handles POST /sysDictionary/refreshSource?id=123\nManually triggers a sync run for one source-bound dictionary (T-0182).",
  "intents": [
    "字典 - 创建",
    "字典：创建新的资源记录 Handler: RefreshSource handles POST /sysDictionary/refreshSource?id=123\nManually triggers a sync run for one source-bound dictionary (T-0182).",
    "sysDictionary",
    "字典",
    "admin",
    "refreshSource"
  ],
  "tags": [
    "sysDictionary",
    "字典",
    "admin",
    "refreshSource"
  ],
  "risk": "low",
  "confirmationRequired": true,
  "idempotent": false,
  "sideEffects": "会修改业务状态或触发任务；执行前确认目标、范围和影响。操作语义：字典 - 创建",
  "pathParams": [],
  "queryParams": [
    {
      "name": "id",
      "in": "query",
      "required": false,
      "type": "string"
    }
  ],
  "formParams": [],
  "responses": {
    "2xx": {
      "content": {
        "application/json": {
          "schema": {
            "properties": {
              "data": {
                "description": "该 Handler 返回的真实业务结果；以实际 API JSON 为准。"
              },
              "msg": {
                "example": "ok",
                "type": "string"
              },
              "ret": {
                "const": 1,
                "type": "integer"
              }
            },
            "required": [
              "ret",
              "msg",
              "data"
            ],
            "type": "object"
          }
        }
      },
      "description": "成功。JSON 接口使用 OMC 标准响应信封；文件或 SSE 接口按 Content-Type 返回二进制或事件流。"
    },
    "4xx": {
      "description": "请求参数、身份、权限或 Agent policy 不允许。不要绕过限制。"
    },
    "5xx": {
      "description": "OMC 内部处理失败。不要编造结果或自动重试非幂等写操作。"
    }
  },
  "contractCoverage": {
    "request": "go-handler",
    "response": "standard-envelope"
  },
  "sources": [
    "runtime-route",
    "go-handler"
  ]
}
