{
  "schemaVersion": "1.0",
  "operationId": "post.config.sync.pull.by_deviceId",
  "method": "POST",
  "path": "/api/v1/config/sync/pull/:deviceId",
  "handler": "github.com/omcgo/omcgo/internal/config.(*SyncHandler).PullConfig-fm",
  "category": "config",
  "title": "Pull configuration from a device",
  "summary": "Pull configuration from a device",
  "description": "配置：从设备或外部系统拉取数据 Handler: PullConfig queues GetParameterValues commands for a device.\n\n走 GPVBatcher：按统一 batchSize 拆批，commandKey 用 \"sync-gpv-{sn}-{i}\" 前缀，\n即可享受 ACS handler.tryRecoverGPVFault 的 Fault 自愈循环（剔除坏 path 续查）。",
  "intents": [
    "配置 - 拉取",
    "Pull configuration from a device",
    "配置：从设备或外部系统拉取数据 Handler: PullConfig queues GetParameterValues commands for a device.\n\n走 GPVBatcher：按统一 batchSize 拆批，commandKey 用 \"sync-gpv-{sn}-{i}\" 前缀，\n即可享受 ACS handler.tryRecoverGPVFault 的 Fault 自愈循环（剔除坏 path 续查）。",
    "config",
    "配置",
    "sync",
    "pull"
  ],
  "tags": [
    "config",
    "配置",
    "sync",
    "pull",
    "ConfigSync"
  ],
  "risk": "low",
  "confirmationRequired": true,
  "idempotent": false,
  "sideEffects": "会修改业务状态或触发任务；执行前确认目标、范围和影响。操作语义：Pull configuration from a device",
  "pathParams": [
    {
      "name": "deviceId",
      "in": "path",
      "required": true,
      "type": "string",
      "description": "替换 URL 路径中的 :deviceId",
      "schema": {
        "format": "uuid",
        "type": "string"
      }
    }
  ],
  "queryParams": [],
  "formParams": [],
  "requestBody": {
    "contentType": "application/json",
    "required": true,
    "schema": {
      "goType": "PullConfigRequest",
      "properties": {
        "parameter_names": {
          "items": {
            "type": "string"
          },
          "minItems": 1,
          "type": "array"
        }
      },
      "required": [
        "parameter_names"
      ],
      "type": "object"
    }
  },
  "responses": {
    "200": {
      "description": "Configuration pulled"
    },
    "401": {
      "$ref": "#/components/responses/Unauthorized"
    },
    "404": {
      "$ref": "#/components/responses/NotFound"
    }
  },
  "contractCoverage": {
    "request": "openapi",
    "response": "openapi"
  },
  "sources": [
    "runtime-route",
    "openapi",
    "go-handler"
  ]
}
