# Nginx server configuration
# Note: worker_processes is set via NGINX_WORKER_PROCESSES env var in docker-compose.yml
# This file is mounted to /etc/nginx/conf.d/default.conf (already in http context)

# Frontend + API Gateway (port 8081)
server {
    listen 8081;
    root /usr/share/nginx/html;
    index index.html;

    # Docker 内置 DNS 解析
    resolver 127.0.0.11 valid=30s ipv6=off;

    # REST API 代理到 App
    location /api/ {
        set $backend http://app:8081/api/;
        proxy_pass $backend;
        proxy_set_header Host $host;
        proxy_set_header X-Real-IP $remote_addr;
        proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
        proxy_set_header X-Forwarded-Proto $scheme;
    }

    # 前端 SPA fallback
    location / {
        try_files $uri $uri/ /index.html;
    }
}

# ACS Gateway (port 8080) - TR-069 CWMP
server {
    listen 8080;

    # 透传所有请求到 ACS 服务，路径由 ACS 内部路由处理
    location / {
        proxy_pass http://acs:7547;
        proxy_set_header Host $host;
        proxy_set_header X-Real-IP $remote_addr;
        proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
        proxy_set_header X-Forwarded-Proto $scheme;

        # TR-069 SOAP/XML 长连接支持
        proxy_http_version 1.1;
        proxy_set_header Connection "";
        proxy_read_timeout 300s;
        proxy_send_timeout 300s;
        client_max_body_size 100m;
    }
}
