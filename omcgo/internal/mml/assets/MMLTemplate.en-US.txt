# MML TXT Script Template
# Valid line format:
# Operation command_code[:param=value,param2=value2];device_sn
#
# Supported operations:
# LST query. Parameters are usually omitted.
# MOD modify. Fill one or more param=value pairs as required by the command catalog.
# ADD add an object/configuration. Use this when an ADD command exists in the command catalog, and fill required parameters.
# RMV remove an object/configuration. Use this when an RMV command exists in the command catalog.
#
# Rules:
# 1. Each line may contain only one command and one device SN.
# 2. Multiple commands for the same device run in top-to-bottom file order.
# 3. Separate parameters with English commas; when a value contains a comma or semicolon, wrap it in double quotes, single quotes, or {}.
# 4. Blank lines and lines starting with # are ignored.
# 5. One file supports at most 200 devices, 2000 valid commands, and 2 MiB.
# 6. Command codes must match the command_code shown in the MML command catalog.
#
# Examples: replace DEVICE_SN with a real base station serial number.
LST DEVICE_INFO;DEVICE_SN
MOD MANAGEMENT_SERVER:URL=http://localhost:8080/smallcell/AcsService,USERNAME=acs,PASSWORD=acs;DEVICE_SN
ADD ETHERNET_INTERFACE;DEVICE_SN
RMV ETHERNET_INTERFACE;DEVICE_SN
MOD DEVICE_INFO:DESCRIPTION="site,a;sector-b",ALIAS={main,backup};DEVICE_SN
