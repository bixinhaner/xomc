# OMC 前后端整合方案

## 一、背景与目标

**现状：** 前端 omcmb（React 19 + Ant Design + TypeScript）拥有 94 个路由页面、18 个 mock 服务模块，所有 API 调用均为本地 mock 数据。后端 omcgo（Go + Gin）提供 82 个 REST API 端点，覆盖 10 个功能域（F01-F10），但未配置 CORS，前端从未与之对接。

**目标：** 建立前后端通信层，按优先级分阶段将 mock 服务替换为真实 API 调用，识别所有功能差异并给出补齐策略。

---

## 二、功能匹配度总览

### 匹配度评分

| 类别 | 前端功能 | 后端支持 | 匹配度 | 说明 |
|------|---------|---------|--------|------|
| 认证登录 | 1 页面 | ✅ 2 端点 | 🟡 70% | 后端完备（JWT login+refresh），前端需改造 |
| 设��管理 | 12 页面 | ✅ 5 端点 | 🟡 40% | 后端仅读+重启，缺 CRUD/批量/SN查询 |
| 告警管理 | 6 页面 | ✅ 6 端点 | 🟡 65% | 基础匹配，缺告警规则/告警计数 |
| 配置管理 | 10 页面 | ✅ 18 端点 | 🟡 50% | 后端有丰富 datamodel，但前端未对接；模板部分匹配 |
| 性能管理 | 7 页面 | ✅ 5 端点 | 🟡 55% | 计数器/KPI 匹配，缺阈值/任务管理 |
| 软件管理 | 4 页面 | ✅ 6 端点 | 🟢 70% | 固件+升级任务基本匹配，命名不同 |
| 拓扑管理 | 6 页面 | ✅ 8 端点 | 🟡 45% | 后端仅有设备分组，缺域/站点/GIS |
| 用户管理 | 8 页面 | ✅ 10 端点 | 🟢 70% | 用户/角色 CRUD 匹配，缺部分扩展功能 |
| MR 管理 | 6 页面 | ✅ 3 端点 | 🔴 30% | 后端仅文件管理，缺指标/映射/统计 |
| Dashboard | 1 页面 | ❌ 0 端点 | 🔴 0% | 后端无 Dashboard 聚合接口 |
| 备份恢复 | 5 页面 | ❌ 0 端点 | 🔴 0% | 后端无备份接口 |
| MML 控制台 | 3 页面 | ❌ 0 端点 | 🔴 0% | 后端无 MML 接口 |
| 报表管理 | 4 页面 | ❌ 0 端点 | 🔴 0% | 后端无报表接口 |
| License | 3 页面 | ❌ 0 端点 | 🔴 0% | 后端无 License 接口 |
| 运维工具 | 5 页面 | ❌ 0 端点 | 🔴 0% | 后端无运维工具接口 |
| 文件管理 | 7 页面 | 🟡 2 端点 | 🔴 15% | 仅 MR 文件部分匹配 |
| 日志管理 | 6 页面 | 🟡 1 端点 | 🔴 15% | 仅审计日志 |
| NE 管理 | 2 页面 | 🟡 特殊 | 🔴 10% | NE Direct 是内部协议，非用户 API |

### 后端有但前端未实现的功能

| 后端功能 | 端点数 | 建议 |
|---------|--------|------|
| 数据模型管理 (F02) | 13 | ⭐ 应新增前端页面 |
| 北向接口/OSS (F08) | 7 | ⭐ 应新增前端页面 |
| 自动配置 (F09) | 4 | ⭐ 应新增前端页面 |
| 互操作测试 (F10) | 4 | 可后期添加 |
| OUI 管理 | 2 | 可集成到数据模型页面 |

---

## 三、量化差异统计

```
前端 mock API 端点总数:     ~160 个
后端实际 API 端点总数:      ~82 个
完全可对接的端点:           ~35 个 (端点存在，仅需路径/参数适配)
需后端补充的端点:           ~95 个 (前端有需求，后端未实现)
需前端新增页面的后端端点:    ~30 个 (后端有，前端未覆盖)
URL 路径需适配的端点:       ~35 个 (功能对应但路径/命名不同)
```

---

## 四、整合架构方案

### 4.1 通信层架构

```
┌─────────────────────────────────────────────────┐
│  React Pages / Components                       │
│  (useQuery / useMutation hooks 不变)             │
├─────────────────────────────────────────────────┤
│  src/hooks/api/useXxx.ts                        │
│  (将 import 从 mock/services → services/api)     │
├─────────────────────────────────────────────────┤
│  src/services/api/xxxApi.ts  ← 新建 API 层       │
│  (封装 HTTP 调用，处理路径映射/参数转换)           │
├─────────────────────────────────────────────────┤
│  src/services/http.ts  ← 新建 HTTP 客户端        │
│  (axios 实例 + ���截器 + token 刷新)              │
├─────────────────────────────────────────────────┤
│  Vite Dev Proxy / Nginx Prod Proxy              │
│  /api/* → http://localhost:8080/api/*            │
├─────────────────────────────────────────────────┤
│  omcgo Gin Server (port 8080)                   │
│  + CORS Middleware                               │
└─────────────────────────────────────────────────┘
```

### 4.2 关键改动点

#### 后端 (omcgo) — 必须改动

| 改动 | 文件 | 复杂度 |
|------|------|--------|
| 添加 CORS 中间件 | `cmd/app/main.go` | S |
| 添加 GET /api/v1/auth/me 接口 | `internal/omcr/admin/` | S |

#### 前端 (omcmb) — 必须改动

| 改动 | 文件 | 复杂度 |
|------|------|--------|
| 安装 axios 依赖 | `package.json` | S |
| 新建 HTTP 客户端 (拦截器/token 刷新) | `src/services/http.ts` (新建) | M |
| 新建 API 服务层 (每个模块一个文件) | `src/services/api/*.ts` (新建) | L |
| 修改所有 hooks 的 import 路径 | `src/hooks/api/*.ts` (17 个文件) | M |
| 改造 login 页面对接真实 JWT | `src/pages/login/` + `src/store/userStore.ts` | M |
| 新建环境变量配置 | `.env.development` / `.env.production` | S |
| Vite 添加 dev proxy | `vite.config.ts` | S |

---

## 五、分阶段实施计划

### Phase 0：基础通信层搭建 (工作量: M，约 2-3 天)

**目标：** 建立前后端通信的基础设施

1. **后端添加 CORS 中间件**
   - 文件: `omcgo/cmd/app/main.go`
   - 使用 `github.com/gin-contrib/cors`
   - 允许 `http://localhost:3000` (开发) + 生产域名

2. **前端创建 HTTP 客户端**
   - 新建 `src/services/http.ts`
   - 基于 axios，配置 baseURL = `/api/v1`
   - 请求拦截器：自动附加 `Authorization: Bearer <token>`
   - 响应拦截器：401 自动 refresh token，失败跳转登录页
   - 错误统一处理（Ant Design message 提示）

3. **前端创建 API 模式切换机制**
   - 新建 `src/services/api/index.ts`
   - 支持环境变量 `VITE_USE_MOCK=true/false` 控制使用 mock 还是真实 API
   - 每个 hook 文件根据标志位选择数据源，实现渐进式迁移

4. **Vite Dev Proxy 配置**
   - 修改 `vite.config.ts`，添加 `/api` → `http://localhost:8080` 的代理

5. **环境变量配置**
   - `.env.development`: `VITE_API_BASE_URL=/api/v1`, `VITE_USE_MOCK=false`
   - `.env.mock`: `VITE_USE_MOCK=true`

---

### Phase 1：认证 + 核心只读功能 (工作量: L，约 5-7 天)

**目标：** 实现登录并能浏览基础数据

| 序号 | 功能 | 前端 mock 端点 | 后端真实端点 | 适配说明 | 复杂度 |
|------|------|---------------|-------------|---------|--------|
| 1.1 | 登录 | mock login | `POST /api/v1/auth/login` | 改造 login 页面+userStore 存储 access_token/refresh_token | M |
| 1.2 | Token 刷新 | 无 | `POST /api/v1/auth/refresh` | HTTP 拦截器自动处理 | S |
| 1.3 | 设备列表 | `/devices/list` | `GET /api/v1/devices` | 参数映射: pagination+filter | M |
| 1.4 | 设备详情 | `/devices/{id}` | `GET /api/v1/devices/:id` | 直接对接 | S |
| 1.5 | 设备统计 | (dashboard 用) | `GET /api/v1/devices/stats` | 新增对接 | S |
| 1.6 | 设备参数 | (详情页用) | `GET /api/v1/devices/:id/parameters` | 新增对接 | S |
| 1.7 | 活跃告警 | `/alarms/current` | `GET /api/v1/alarms/active` | 路径映射: current→active | M |
| 1.8 | 历史告警 | `/alarms/historical` | `GET /api/v1/alarms/history` | 路径映射: historical→history | M |
| 1.9 | 告警统计 | `/alarms/count` | `GET /api/v1/alarms/statistics` | 响应格式可能需适配 | S |
| 1.10 | 告警详情 | `/alarms/{id}` | `GET /api/v1/alarms/:id` | 直接对接 | S |

---

### Phase 2：写操作 + 管理功能 (工作量: L，约 5-7 天)

| 序号 | 功能 | 前端 mock 端点 | 后端真实端点 | 适配说明 | 复杂度 |
|------|------|---------------|-------------|---------|--------|
| 2.1 | 告警确认 | `POST /alarms/{id}/acknowledge` | `POST /api/v1/alarms/:id/acknowledge` | 直接对接 | S |
| 2.2 | 告警清�� | `POST /alarms/clear` | `POST /api/v1/alarms/:id/clear` | 前端批量→后端单个，需循环 | M |
| 2.3 | 设备重启 | 无前端 mock | `POST /api/v1/devices/:id/reboot` | 设备详情页添加重启按钮 | S |
| 2.4 | 配置模板 CRUD | `/config/templates` | `/api/v1/config/templates` | 路径适配即可 | M |
| 2.5 | 固件管理 | `/software/versions` | `/api/v1/firmware` | 路径映射: software/versions→firmware | M |
| 2.6 | 固件上传 | `POST /software/versions` | `POST /api/v1/firmware` (multipart) | 适配 multipart form | M |
| 2.7 | 升级任务 | `/software/upgrade-plans` | `/api/v1/upgrade-tasks` | 路径映射+字段适配 | M |
| 2.8 | 批量升级 | (upgrade-plans 内) | `POST /api/v1/upgrade-tasks/batch` | 新增对接 | M |
| 2.9 | 用户管理 | `/system/users` | `/api/v1/admin/users` | 路径映射: system→admin | M |
| 2.10 | 角色管理 | `/system/roles` | `/api/v1/admin/roles` | 路径映射 | S |
| 2.11 | 审计日志 | `/logs/operation` | `/api/v1/admin/audit-logs` | 路径映射+字段适配 | M |
| 2.12 | 设备分组 | `/devices/groups` | `/api/v1/topology/groups` | 路径映射: devices/groups→topology/groups | M |

---

### Phase 3：性能/KPI + MR (工作量: M，约 3-5 天)

| 序号 | 功能 | 前端 mock 端点 | 后端真实端点 | 适配说明 | 复杂度 |
|------|------|---------------|-------------|---------|--------|
| 3.1 | PM 计数器 | `/performance/counters` | `GET /api/v1/pm/counters` | 路径映射: performance→pm | M |
| 3.2 | 聚合计数器 | (图表用) | `GET /api/v1/pm/counters/aggregated` | 新增对接 | M |
| 3.3 | KPI 列表 | `/performance/kpis` | `GET /api/v1/pm/kpi` | 路径映射 | S |
| 3.4 | KPI 定义 | (提取向导用) | `GET /api/v1/pm/kpi/definitions` | 新增对接 | S |
| 3.5 | KPI 计算 | (手动触发) | `POST /api/v1/pm/kpi/calculate` | 新增对接 | M |
| 3.6 | MR 文件列表 | `/mr/records` | `GET /api/v1/mr/files` | 路径映射 | S |
| 3.7 | MR 文件下载 | (下载按钮) | `GET /api/v1/mr/files/:id/download` | 新增对接 (blob 下载) | M |
| 3.8 | MR 数据查询 | `/mr/indicators/{code}/stats` | `GET /api/v1/mr/data` | 响应格式适配 | M |

---

### Phase 4：后端补充开发 — 高优先级 (工作量: XL，约 10-15 天)

这些功能前端页面已就绪，但后端缺少 API，需要后端补充开发。

| 序号 | 缺失功能 | 需新增端点 | 业务优先级 | 开发复杂度 |
|------|---------|-----------|-----------|-----------|
| 4.1 | Dashboard 聚合接口 | GET /api/v1/dashboard/summary (聚合设备统计+告警统计+KPI) | ⭐⭐⭐ 高 | M |
| 4.2 | 设备 CRUD 完整化 | POST/PUT/DELETE /api/v1/devices | ⭐⭐⭐ 高 | M |
| 4.3 | 设备 SN 查询 | GET /api/v1/devices?sn=xxx 或 GET /api/v1/devices/sn/:sn | ⭐⭐⭐ 高 | S |
| 4.4 | 告警规则管理 | CRUD /api/v1/alarms/rules | ⭐⭐ 中 | L |
| 4.5 | KPI 阈值管理 | CRUD /api/v1/pm/thresholds | ⭐⭐ 中 | M |
| 4.6 | 系统日志查询 | GET /api/v1/logs/system, /logs/ne-messages | ⭐⭐ 中 | M |
| 4.7 | 密码重置/锁定 | POST /api/v1/admin/users/:id/reset-password, /lock, /unlock | ⭐⭐ 中 | S |
| 4.8 | 权限列表接口 | GET /api/v1/admin/permissions | ⭐⭐ 中 | S |
| 4.9 | 配置参数同步 | GET/PUT /api/v1/config/params | ⭐⭐ 中 | L |
| 4.10 | 备份恢复 | CRUD /api/v1/backup/* | ⭐ 低 | XL |
| 4.11 | MML 命令执行 | /api/v1/mml/* | ⭐ 低 | XL |
| 4.12 | 报表系统 | /api/v1/reports/* | ⭐ 低 | XL |
| 4.13 | License 管理 | /api/v1/licenses/* | ⭐ 低 | L |
| 4.14 | 运维工具 | /api/v1/ops/* | ⭐ 低 | L |

---

### Phase 5：前端补充开发 — 后端已有功能 (工作量: L，约 5-7 天)

| 序号 | 缺失功能 | 后端已有端点数 | 需新增前端页面 | 复杂度 |
|------|---------|-------------|--------------|--------|
| 5.1 | 数据模型管理 | 13 | 数据模型列表+详情+导入导出+激活/废弃 | L |
| 5.2 | OUI 管理 | 2 | 厂商 OUI 列表+新增 (可嵌入数据模型页) | S |
| 5.3 | 北向/OSS 接口 | 7 | 推送目标管理+同步+导出 | M |
| 5.4 | 自动配置任务 | 4 | 配置任务列表+详情+重试 | M |
| 5.5 | 互操作测试 | 4 | 测试用例列表+执行+结果查看 | M |

---

## 六、详细 API 路径映射表

### 可直接对接（路径适配后即可用）

| 前端当前路径 | 后端实际路径 | 适配方式 |
|------------|------------|---------|
| `POST /auth/login` (mock) | `POST /api/v1/auth/login` | 重写登录逻辑 |
| `/devices/list` | `GET /api/v1/devices` | 去掉 /list，参数格式对齐 |
| `/devices/{id}` | `GET /api/v1/devices/:id` | 直接映射 |
| `/alarms/current` | `GET /api/v1/alarms/active` | current → active |
| `/alarms/historical` | `GET /api/v1/alarms/history` | historical → history |
| `/alarms/{id}` | `GET /api/v1/alarms/:id` | 直接映射 |
| `/alarms/{id}/acknowledge` | `POST /api/v1/alarms/:id/acknowledge` | 直接映射 |
| `/config/templates` | `GET /api/v1/config/templates` | 直接映射 |
| `/config/templates/{id}` | `GET/PUT/DELETE /api/v1/config/templates/:id` | 直接映射 |
| `/performance/counters` | `GET /api/v1/pm/counters` | performance → pm |
| `/performance/kpis` | `GET /api/v1/pm/kpi` | performance → pm, kpis → kpi |
| `/software/versions` | `GET /api/v1/firmware` | software/versions → firmware |
| `/software/upgrade-plans` | `GET /api/v1/upgrade-tasks` | upgrade-plans → upgrade-tasks |
| `/system/users` | `GET /api/v1/admin/users` | system → admin |
| `/system/roles` | `GET /api/v1/admin/roles` | system → admin |
| `/logs/operation` | `GET /api/v1/admin/audit-logs` | logs/operation → admin/audit-logs |
| `/devices/groups` | `GET /api/v1/topology/groups` | devices/groups → topology/groups |
| `/mr/records` | `GET /api/v1/mr/files` | records → files |

---

## 七、改动量评估

### 总体工作量

| 阶段 | 改动范围 | 工作量 | 前端文件数 | 后端文件数 |
|------|---------|--------|-----------|-----------|
| Phase 0: 基础通信层 | 新建 HTTP 客户端 + 环境配置 | **2-3 天** | ~5 新建 | ~2 改动 |
| Phase 1: 认证+只读 | 登录改造 + 10 个查询接口 | **5-7 天** | ~15 改动 | 0 |
| Phase 2: 写操作+管理 | 12 个写/管理接口 | **5-7 天** | ~20 改动 | 0 |
| Phase 3: PM/KPI+MR | 8 个性能/MR 接口 | **3-5 天** | ~10 改动 | 0 |
| Phase 4: 后端补充 | 14 项后端新增 API | **10-15 天** | 0 | ~30+ 新建 |
| Phase 5: 前端补充 | 5 项前端新页面 | **5-7 天** | ~15 新建 | 0 |
| **合计** | | **30-44 天** | **~65 文件** | **~32 文件** |

### 按模块功能对不上的统计

```
完全对不上（后端无 API）:        8 个前端模块 (Dashboard/备份/MML/报表/License/运维工具/文件/NE)
                                涉及前端页面: 36 个
                                涉及 mock 端点: ~95 个

部分对不上（需适配+补充）:      9 个前端模块 (设备/告警/配置/性能/软件/拓扑/用户/日志/MR)
                                涉及路径映射: ~35 个
                                涉及后端补充: ~15 个端点

前端未覆盖后端功能:             4 个后端模块 (数据模型/北向/自动配置/互操作)
                                涉及后端端点: ~30 个
                                需新增前端页面: ~10 个
```

---

## 八、关键文件清单

### 前端需改动的核心文件

| 文件 | 改动内容 |
|------|---------|
| `webcode/vite.config.ts` | 添加 dev proxy |
| `webcode/package.json` | 添加 axios 依赖 |
| `webcode/src/services/http.ts` | **新建** HTTP 客户端 |
| `webcode/src/services/api/*.ts` | **新建** 每个模块的 API 服务 (18 个文件) |
| `webcode/src/store/userStore.ts` | 添加 refreshToken，改造 login/logout |
| `webcode/src/pages/login/index.tsx` | 对接真实 JWT 登录 |
| `webcode/src/hooks/api/*.ts` | 修改 import 源 (17 个文件) |
| `.env.development` | **新建** 环境变量 |
| `.env.production` | **新建** 环境变量 |

### 后端需改动的核心文件

| 文件 | 改动内容 |
|------|---------|
| `omcgo/cmd/app/main.go` | 添加 CORS 中间件 |
| `omcgo/go.mod` | 添加 `gin-contrib/cors` 依赖 |
| `omcgo/internal/omcr/admin/handler.go` | 添加 GET /auth/me 接口 |

---

## 九、建议的开发策略

1. **渐进式迁移**：通过 `VITE_USE_MOCK` 环境变量控制，一个模块一个模块地切换到真实 API，保证随时可回退到 mock 模式
2. **Mock 保留**：不删除 mock 层，作为前端独立开发和演示的备用方案
3. **后端暂缺功能的处理**：Phase 0-3 期间前端仍使用 mock 的模块保持不变，等 Phase 4 后端补充后再切换
4. **优先保证主流程**：认证 → 设备列表 → 告警查看 → 配置管理 → 性能查看，这条主链路优先打通
5. **响应格式统一**：后端所有 API 应返回统一格式 `{ code: 0, data: {}, message: "" }`，前端在 HTTP 拦截器中统一解包

---

## 十、验证方案

每个阶段完成后的验证步骤：

1. **Phase 0 验证**：启动后端 `go run cmd/app/main.go`，启动前端 `npm run dev`，确认浏览器控制台无 CORS 错误，`/api/v1/healthz` 返回 200
2. **Phase 1 验证**：使用真实账号登录 → 设备列表显示真实数据 → 告警列表显示真实数据 → Token 过期后自动刷新
3. **Phase 2 验证**：创建配置模�� → 上传固件 → 创建升级任务 → 用户 CRUD → 分组管理
4. **Phase 3 验证**：PM 计数器图表显示真实数据 → KPI 列表 → MR 文件下载
5. **整体回归**：切换 `VITE_USE_MOCK=true` 确认 mock 模式仍正常工作
