# OMC 前后端整合状态报告

> 更新日期: 2026-03-07 | 全模块 100% 对齐完成

## 一、现状总结

全模块对齐开发完成后，前后端实现 **100% 对齐**：

```
前端 API 服务文件:     24 个 (src/services/api/*.ts)     [+5 新建]
前端 Hook 文件:        21 个 (src/hooks/api/*.ts)
后端注册路由:         ~198 个 (omcgo/cmd/app/main.go)    [+60 新增]
已对接真实 API 的端点: ~198 个                           [100%]
仍委托 Mock 的函数:      0 个                            [全部消除]
纯 Mock Hook (无后端):   0 个                            [全部消除]
后端有但前端未消费:      0 个端点
新增数据库迁移:          9 对 (000025-000033)
新增数据库表:           17 张
```

**里程碑对比:**

```
                      Sprint 0-5    Sprint 0-9    全模块对齐后    变化
──────────────────────────────────────────────────────────────────────
前端 API 服务文件:        15          19              24          +5
后端注册路由:            ~110        ~138            ~198         +60
已对接真实 API 端点:      ~75        ~136            ~198         全部
仍委托 Mock 函数:         ~35         ~18               0         -100%
纯 Mock Hook (无后端):      7           3               0         -100%
后端无前端消费端点:       ~20          ~0               0           0
整体对齐率:              ~68%        ~92%           100%         +8pp
```

**技术栈:**
- 前端: React 19 + TypeScript + Vite + Ant Design + Zustand + React Query + Axios
- 后端: Go 1.25 + Gin + PostgreSQL + TimescaleDB + Redis + MinIO
- 通信: REST API, JWT 认证, `VITE_USE_MOCK` 环境变量控制 mock/real 切换
- 基础设施: HTTP 拦截器 (camelCase↔snake_case 自动转换), Token 自动刷新, CORS 中间件

---

## 二、对齐矩阵

### A. 完全对齐 — 前端 real API → 后端路由，路径匹配 ✅ (全部 27 个模块, ~198 个端点)

| 前端 API 服务 | 后端路由组 | 端点数 | 说明 |
|---|---|---|---|
| `authApi.ts` | `/auth/*` | 3 | login + refresh + me |
| `deviceApi.ts` | `/devices/*` | 11 | 设备 CRUD + stats + params + reboot + 分组列表 + NE |
| `alarmApi.ts` | `/alarms/*` | 11 | active/history/stats/ack/clear + 规则 CRUD |
| `templateApi.ts` | `/templates/*` | 5 | 配置模板 CRUD |
| `softwareApi.ts` | `/firmware/*` + `/upgrade-tasks/*` | 7 | 固件 CRUD + 升级任务 |
| `logApi.ts` | `/logs/*` | 2 | 系统日志 + NE 消息日志 |
| `datamodelApi.ts` | `/datamodels/*` + `/oui/*` | 14 | 全套 CRUD + 导入导出 + 激活/废弃 + 缓存 + OUI |
| `northboundApi.ts` | `/northbound/*` | 8 | 推送目标 + 同步 + 导出 |
| `provisionApi.ts` | `/provisioning/tasks/*` | 4 | 任务 CRUD + 重试 |
| `interopApi.ts` | `/interop/*` | 4 | 测试用例 + 执行 + 验证 |
| `adminApi.ts` | `/admin/*` | 17 | **Sprint 6-9 升级** — Users CRUD + lock/unlock/reset-pwd + Roles 全套 CRUD + 权限列表 + 审计日志 + 角色分配/移除 |
| `configSyncApi.ts` | `/config/sync/*` | 3 | **Sprint 6-9 新增** — Push + Pull + Status |
| `backupApi.ts` | `/backup/*` | 9 | **Sprint 8 新增** — 任务 CRUD + cancel + 计划 CRUD |
| `fileApi.ts` | `/files/*` | 6 | **Sprint 8 新增** — 文件 CRUD + 下载 + 分发 |
| `mmlApi.ts` | `/mml/*` | 10 | **Sprint 8 新增** — 命令列表/详情 + 执行 + 脚本 CRUD + 任务列表/详情 |

以下模块在全模块对齐开发中完成了 100% 对齐：
- `topologyApi.ts` — 15 个真实端点 (Group CRUD + 成员管理 + 站点 CRUD + 拓扑图 + GIS)
- `pmApi.ts` — 12 个真实端点 (KPI/Counter/Threshold 全套 + Tasks CRUD)
- `dashboardApi.ts` — 9 个真实端点 (summary + 4 图表 + widgets + alarm-type-pie + kpi-time-series)
- `mrApi.ts` — 10 个真实端点 (文件/数据 + 指标管理 + 映射 + 导出 + 统计)
- `configBaselineApi.ts` — 8 个真实端点 (基线 CRUD + 配置任务 + 邻区参数) **新建**
- `backupApi.ts` — 14 个真实端点 (任务 + 计划 + FTP 配置 CRUD + 测试连接)
- `licenseApi.ts` — 6 个真实端点 (列表/详情/摘要/激活/吊销/导入) **新建**
- `reportsApi.ts` — 9 个真实端点 (定义 CRUD + 记录 + 生成/下载/示例) **新建**
- `opsToolsApi.ts` — 13 个真实端点 (模板 CRUD + 任务生命周期 + 命令记录) **新建**
- `systemApi.ts` — 1 个真实端点 (系统信息) **新建**

### B. 部分对齐 — ~~已消除~~ ✅ (0 个模块)

> 全模块对齐开发后，所有原部分对齐模块已升级为 100% 对齐。

### C. 纯 Mock — ~~已消除~~ ✅ (0 个 Hook)

> 全模块对齐开发后，所有原纯 Mock Hook 已获得真实后端支持。

**历史变迁:**
- ~~`useLicense.ts`~~ → 现用 `useMock ? licenseService : licenseApi` (6 端点接入)
- ~~`useReports.ts`~~ → 现用 `useMock ? reportService : reportsApi` (9 端点接入)
- ~~`useOpsTools.ts`~~ → ���用 `useMock ? opsToolsService : opsToolsApi` (13 端点接入)
- ~~`useBackup.ts`~~ → FTP 配置补充完成 (14 端点全部接入)
- ~~`useConfig.ts`~~ → 基线/任务/邻区补充完成 (15 端点全部接入)
- ~~`useMR.ts`~~ → 指标/映射/导出补充完成 (10 端点全部接入)

### D. 后端有端点但前端完全未消费 (~0 个)

Sprint 0-5 报���中的 ~20 个未消费端点已全部在 Sprint 6-9 中完成前端对接：

| 原未消费端点 | 对接状态 | 对接文件 |
|---|---|---|
| `POST/POST/GET /config/sync/*` | ✅ 新增 configSyncApi.ts | Sprint 6-9 |
| `GET /devices/stats` | ✅ deviceApi.getStats() | Sprint 6-9 |
| `GET /devices/:id/parameters` | ✅ deviceApi.getParameters() | Sprint 6-9 |
| `POST /devices/:id/reboot` | ✅ deviceApi.reboot() | Sprint 6-9 |
| `GET /pm/counters/aggregated` | ✅ pmApi.getAggregatedCounters() | Sprint 6-9 |
| `POST /pm/kpi/calculate` | ✅ pmApi.calculateKPI() | Sprint 6-9 |
| `POST/PUT/DELETE /groups/*` | ✅ topologyApi.createGroup/updateGroup/deleteGroup | Sprint 6-9 |
| `POST/DELETE/GET /groups/:id/devices` | ✅ topologyApi.addDevice/removeDevice/getGroupDevices | Sprint 6-9 |
| `POST/DELETE /admin/users/:id/roles` | ✅ adminApi.assignRole/removeRole | Sprint 6-9 |
| `GET /admin/permissions` | ✅ adminApi.getPermissions() | Sprint 6-9 |
| `POST/PUT/DELETE/GET /admin/roles/*` | ✅ adminApi Roles CRUD | Sprint 6-9 |
| `POST /admin/users/:id/reset-password` | ✅ adminApi.resetPassword() | Sprint 6-9 |
| `POST /admin/users/:id/lock/unlock` | ✅ adminApi.lockUser/unlockUser() | Sprint 6-9 |
| `GET /pm/thresholds` CRUD | ✅ pmApi Thresholds CRUD | Sprint 6-9 |
| `GET /dashboard/alarm-trend` | ✅ dashboardApi.getAlarmTrend() | Sprint 7 |
| `GET /dashboard/device-status` | ✅ dashboardApi.getDeviceStatusPie() | Sprint 7 |
| `GET /dashboard/kpi-trend` | ✅ dashboardApi.getKPITrend() | Sprint 7 |
| `GET /dashboard/region-stats` | ✅ dashboardApi.getRegionStats() | Sprint 7 |

---

## 三、已验证的 API 路径映射表

以下路径经过代码验证，前端 `http.get/post/put/delete` 的路径与后端 `RegisterRoutes` 注册的路径完全匹配：

### 认证 (authApi.ts → admin/handler.go)

| 前端调用 | 后端路由 | 状态 |
|---|---|---|
| `POST /auth/login` | `publicV1.POST("/auth/login")` | ✅ |
| `POST /auth/refresh` | `publicV1.POST("/auth/refresh")` | ✅ |
| `GET /auth/me` | `v1.GET("/auth/me")` | ✅ |

### 设备管理 (deviceApi.ts → device/handler.go)

| 前端调用 | 后端路由 | 状态 |
|---|---|---|
| `GET /devices` (list/NE/SN) | `devices.GET("")` | ✅ |
| `GET /devices/:id` | `devices.GET("/:id")` | ✅ |
| `POST /devices` | `devices.POST("")` | ✅ |
| `PUT /devices/:id` | `devices.PUT("/:id")` | ✅ |
| `DELETE /devices/:id` | `devices.DELETE("/:id")` | ✅ |
| `GET /groups` | `groups.GET("")` → ListTree | ✅ |
| `GET /devices/stats` | `devices.GET("/stats")` | ✅ Sprint 6-9 新增 |
| `GET /devices/:id/parameters` | `devices.GET("/:id/parameters")` | ✅ Sprint 6-9 新增 |
| `POST /devices/:id/reboot` | `devices.POST("/:id/reboot")` | ✅ Sprint 6-9 新增 |

### 告警管理 (alarmApi.ts → alarm/handler.go + rule_handler.go)

| 前端调用 | 后端路由 | 状态 |
|---|---|---|
| `GET /alarms/active` | `alarms.GET("/active")` | ✅ |
| `GET /alarms/history` | `alarms.GET("/history")` | ✅ |
| `GET /alarms/statistics` | `alarms.GET("/statistics")` | ✅ |
| `GET /alarms/:id` | `alarms.GET("/:id")` | ✅ |
| `POST /alarms/:id/acknowledge` | `alarms.POST("/:id/acknowledge")` | ✅ |
| `POST /alarms/:id/clear` | `alarms.POST("/:id/clear")` | ✅ |
| `GET /alarms/rules` | `rules.GET("")` | ✅ |
| `GET /alarms/rules/:id` | `rules.GET("/:id")` | ✅ |
| `POST /alarms/rules` | `rules.POST("")` | ✅ |
| `PUT /alarms/rules/:id` | `rules.PUT("/:id")` | ✅ |
| `DELETE /alarms/rules/:id` | `rules.DELETE("/:id")` | ✅ |

### 配置模板 (templateApi.ts → config/template/handler.go)

| 前端调用 | 后端路由 | 状态 |
|---|---|---|
| `GET /templates` | `templates.GET("")` | ✅ |
| `GET /templates/:id` | `templates.GET("/:id")` | ✅ |
| `POST /templates` | `templates.POST("")` | ✅ |
| `PUT /templates/:id` | `templates.PUT("/:id")` | ✅ |
| `DELETE /templates/:id` | `templates.DELETE("/:id")` | ✅ |

### 配置同步 (configSyncApi.ts → config/sync_handler.go) — Sprint 6-9 新增

| 前端调用 | 后端路由 | 状态 |
|---|---|---|
| `POST /config/sync/push/${deviceId}` | `sync.POST("/push/:deviceId")` | ✅ |
| `POST /config/sync/pull/${deviceId}` | `sync.POST("/pull/:deviceId")` | ✅ |
| `GET /config/sync/status/${deviceId}` | `sync.GET("/status/:deviceId")` | ✅ |

### 软件管理 (softwareApi.ts → software/handler.go)

| 前端调用 | 后端路由 | 状态 |
|---|---|---|
| `GET /firmware` | `firmware.GET("")` | ✅ |
| `GET /firmware/:id` | `firmware.GET("/:id")` | ✅ |
| `POST /firmware` | `firmware.POST("")` (multipart) | ✅ |
| `DELETE /firmware/:id` | `firmware.DELETE("/:id")` | ✅ |
| `GET /upgrade-tasks` | `upgradeTasks.GET("")` | ✅ |
| `GET /upgrade-tasks/:id` | `upgradeTasks.GET("/:id")` | ✅ |
| `POST /upgrade-tasks/batch` | `upgradeTasks.POST("/batch")` | ✅ |

### 用户/角色管理 (adminApi.ts → admin/handler.go) — Sprint 6-9 大幅扩展

| 前端调用 | 后端路由 | 状态 |
|---|---|---|
| `GET /admin/users` | `users.GET("")` | ✅ |
| `GET /admin/users/:id` | `users.GET("/:id")` | ✅ |
| `POST /admin/users` | `users.POST("")` | ✅ |
| `PUT /admin/users/:id` | `users.PUT("/:id")` | ✅ |
| `DELETE /admin/users/:id` | `users.DELETE("/:id")` | ✅ |
| `POST /admin/users/:id/reset-password` | `users.POST("/:id/reset-password")` | ✅ Sprint 6-9 新增 |
| `POST /admin/users/:id/lock` | `users.POST("/:id/lock")` | ✅ Sprint 6-9 新增 |
| `POST /admin/users/:id/unlock` | `users.POST("/:id/unlock")` | ✅ Sprint 6-9 新增 |
| `POST /admin/users/:id/roles` | `users.POST("/:id/roles")` | ✅ Sprint 6-9 新增 |
| `DELETE /admin/users/:id/roles/:roleId` | `users.DELETE("/:id/roles/:roleId")` | ✅ Sprint 6-9 新增 |
| `GET /admin/roles` | `roles.GET("")` | ✅ |
| `GET /admin/roles/:id` | `roles.GET("/:id")` | ✅ Sprint 6-9 新增 |
| `POST /admin/roles` | `roles.POST("")` | ✅ Sprint 6-9 新增 |
| `PUT /admin/roles/:id` | `roles.PUT("/:id")` | ✅ Sprint 6-9 新增 |
| `DELETE /admin/roles/:id` | `roles.DELETE("/:id")` | ✅ Sprint 6-9 新增 |
| `GET /admin/permissions` | `adminGroup.GET("/permissions")` | ✅ Sprint 6-9 新增 |
| `GET /admin/audit-logs` | `adminGroup.GET("/audit-logs")` | ✅ |

### 性能管理 (pmApi.ts → pm/handler.go + threshold_handler.go) — Sprint 6-9 大幅扩展

| 前端调用 | 后端路由 | 状态 |
|---|---|---|
| `GET /pm/kpi/definitions` | `pm.GET("/kpi/definitions")` | ✅ |
| `GET /pm/counters` | `pm.GET("/counters")` | ✅ |
| `GET /pm/kpi` | `pm.GET("/kpi")` | ✅ |
| `GET /pm/counters/aggregated` | `pm.GET("/counters/aggregated")` | ✅ Sprint 6-9 新增 |
| `POST /pm/kpi/calculate` | `pm.POST("/kpi/calculate")` | ✅ Sprint 6-9 新增 |
| `GET /pm/thresholds` | `thresholds.GET("")` | ✅ Sprint 6-9 新增 |
| `GET /pm/thresholds/:id` | `thresholds.GET("/:id")` | ✅ Sprint 6-9 新增 |
| `POST /pm/thresholds` | `thresholds.POST("")` | ✅ Sprint 6-9 新增 |
| `PUT /pm/thresholds/:id` | `thresholds.PUT("/:id")` | ✅ Sprint 6-9 新增 |
| `DELETE /pm/thresholds/:id` | `thresholds.DELETE("/:id")` | ✅ Sprint 6-9 新增 |

### 拓扑/分组管理 (topologyApi.ts → topology/handler.go) — Sprint 6-9 大幅扩展

| 前端调用 | 后端路由 | 状态 |
|---|---|---|
| `GET /groups` (getDomains / getDomainTree) | `groups.GET("")` → ListTree | ✅ |
| `POST /groups` | `groups.POST("")` | ✅ Sprint 6-9 新增 |
| `PUT /groups/:id` | `groups.PUT("/:id")` | ✅ Sprint 6-9 新增 |
| `DELETE /groups/:id` | `groups.DELETE("/:id")` | ✅ Sprint 6-9 新增 |
| `POST /groups/:id/devices` | `groups.POST("/:id/devices")` | ✅ Sprint 6-9 新增 |
| `DELETE /groups/:id/devices/:deviceId` | `groups.DELETE("/:id/devices/:deviceId")` | ✅ Sprint 6-9 新增 |
| `GET /groups/:id/devices` | `groups.GET("/:id/devices")` | ✅ Sprint 6-9 新增 |

### Dashboard (dashboardApi.ts → dashboard/handler.go) — Sprint 7 大幅扩展

| 前端调用 | 后端路由 | 状态 |
|---|---|---|
| `GET /dashboard/summary` | `dashboard.GET("/summary")` | ✅ |
| `GET /dashboard/alarm-trend` | `dashboard.GET("/alarm-trend")` | ✅ Sprint 7 新增 |
| `GET /dashboard/device-status` | `dashboard.GET("/device-status")` | ✅ Sprint 7 新增 |
| `GET /dashboard/kpi-trend` | `dashboard.GET("/kpi-trend")` | ✅ Sprint 7 新增 |
| `GET /dashboard/region-stats` | `dashboard.GET("/region-stats")` | ✅ Sprint 7 新增 |

### 日志管理 (logApi.ts → syslog/handler.go)

| 前端调用 | 后端路由 | 状态 |
|---|---|---|
| `GET /logs/system` | `logs.GET("/system")` | ✅ |
| `GET /logs/ne-messages` | `logs.GET("/ne-messages")` | ✅ |

### MR 管理 (mrApi.ts → mr/handler.go)

| 前端调用 | 后端路由 | 状态 |
|---|---|---|
| `GET /mr/files` | `mr.GET("/files")` | ✅ |
| `GET /mr/files/:id/download` | `mr.GET("/files/:id/download")` | ✅ |
| `GET /mr/data` | `mr.GET("/data")` | ✅ |

### 数据模型 (datamodelApi.ts → config/datamodel/handler.go)

| 前端调用 | 后端路由 | 状态 |
|---|---|---|
| `GET /datamodels` | `datamodels.GET("")` | ✅ |
| `GET /datamodels/:id` | `datamodels.GET("/:id")` | ✅ |
| `POST /datamodels` | `datamodels.POST("")` | ✅ |
| `PUT /datamodels/:id` | `datamodels.PUT("/:id")` | ✅ |
| `DELETE /datamodels/:id` | `datamodels.DELETE("/:id")` | ✅ |
| `GET /datamodels/resolve` | `datamodels.GET("/resolve")` | ✅ |
| `GET /datamodels/statistics` | `datamodels.GET("/statistics")` | ✅ |
| `POST /datamodels/import` | `datamodels.POST("/import")` | ✅ |
| `POST /datamodels/cache/refresh` | `datamodels.POST("/cache/refresh")` | ✅ |
| `POST /datamodels/:id/activate` | `datamodels.POST("/:id/activate")` | ✅ |
| `POST /datamodels/:id/deprecate` | `datamodels.POST("/:id/deprecate")` | ✅ |
| `GET /datamodels/:id/export` | `datamodels.GET("/:id/export")` | ✅ |
| `GET /oui` + `POST /oui` | `oui.GET("")` + `oui.POST("")` | ✅ |

### 北向接口 (northboundApi.ts → northbound/router.go)

| 前端调用 | 后端路由 | 状态 |
|---|---|---|
| `GET /northbound/push/targets` | `nb.GET("/push/targets")` | ✅ |
| `POST /northbound/push/targets` | `nb.POST("/push/targets")` | ✅ |
| `DELETE /northbound/push/targets/:id` | `nb.DELETE("/push/targets/:id")` | ✅ |
| `GET /northbound/sync/full` | `nb.GET("/sync/full")` | ✅ |
| `GET /northbound/sync/incremental` | `nb.GET("/sync/incremental")` | ✅ |
| `POST /northbound/export/pm` | `nb.POST("/export/pm")` | ✅ |
| `POST /northbound/export/alarms` | `nb.POST("/export/alarms")` | ✅ |
| `GET /northbound/export/config/:deviceId` | `nb.GET("/export/config/:deviceId")` | ✅ |

### 自动配置 (provisionApi.ts → provision/handler.go)

| 前端调用 | 后端路由 | 状态 |
|---|---|---|
| `GET /provisioning/tasks` | `tasks.GET("")` | ✅ |
| `GET /provisioning/tasks/:id` | `tasks.GET("/:id")` | ✅ |
| `POST /provisioning/tasks` | `tasks.POST("")` | ✅ |
| `POST /provisioning/tasks/:id/retry` | `tasks.POST("/:id/retry")` | ✅ |

### 互操作测试 (interopApi.ts → interop/handler.go)

| 前端调用 | 后端路由 | 状态 |
|---|---|---|
| `GET /interop/test-cases` | `interop.GET("/test-cases")` | ✅ |
| `POST /interop/run` | `interop.POST("/run")` | ✅ |
| `POST /interop/run/:category` | `interop.POST("/run/:category")` | ✅ |
| `POST /interop/validate/:deviceId` | `interop.POST("/validate/:deviceId")` | ✅ |

### 备份恢复 (backupApi.ts → backup/handler.go) — Sprint 8 新增

| 前端调用 | 后端路由 | 状态 |
|---|---|---|
| `GET /backup/tasks` | `tasks.GET("")` | ✅ |
| `POST /backup/tasks` | `tasks.POST("")` | ✅ |
| `GET /backup/tasks/:id` | `tasks.GET("/:id")` | ✅ |
| `DELETE /backup/tasks/:id` | `tasks.DELETE("/:id")` | ✅ |
| `POST /backup/tasks/:id/cancel` | `tasks.POST("/:id/cancel")` | ✅ |
| `GET /backup/schedules` | `schedules.GET("")` | ✅ |
| `POST /backup/schedules` | `schedules.POST("")` | ✅ |
| `PUT /backup/schedules/:id` | `schedules.PUT("/:id")` | ✅ |
| `DELETE /backup/schedules/:id` | `schedules.DELETE("/:id")` | ✅ |

### 文件管理 (fileApi.ts → filemanager/handler.go) — Sprint 8 新增

| 前端调用 | 后端路由 | 状态 |
|---|---|---|
| `GET /files` | `files.GET("")` | ✅ |
| `POST /files` | `files.POST("")` (multipart) | ✅ |
| `GET /files/:id` | `files.GET("/:id")` | ✅ |
| `DELETE /files/:id` | `files.DELETE("/:id")` | ✅ |
| `GET /files/:id/download` | `files.GET("/:id/download")` | ✅ |
| `POST /files/:id/distribute` | `files.POST("/:id/distribute")` | ✅ |

### MML 控制台 (mmlApi.ts → mml/handler.go) — Sprint 8 新增

| 前端调用 | 后端路由 | 状态 |
|---|---|---|
| `GET /mml/commands` | `commands.GET("")` | ✅ |
| `GET /mml/commands/:id` | `commands.GET("/:id")` | ✅ |
| `POST /mml/execute` | `mml.POST("/execute")` | ✅ |
| `GET /mml/scripts` | `scripts.GET("")` | ✅ |
| `POST /mml/scripts` | `scripts.POST("")` | ✅ |
| `PUT /mml/scripts/:id` | `scripts.PUT("/:id")` | ✅ |
| `DELETE /mml/scripts/:id` | `scripts.DELETE("/:id")` | ✅ |
| `GET /mml/tasks` | `tasks.GET("")` | ✅ |
| `GET /mml/tasks/:id` | `tasks.GET("/:id")` | ✅ |

---

## 四、部分对齐模块详情

### 4.1 topologyApi.ts — 拓扑管理 (6 个函数委托 Mock)

**已对接:** 8 个端点 — getDomains, getDomainTree, createGroup, updateGroup, deleteGroup, addDeviceToGroup, removeDeviceFromGroup, getGroupDevices

**委托 Mock 且后端无对应 (`topologyApi.ts:92-97`):**
- `getSites()` / `getSiteById()` — 站点管理 (后端无站点概念)
- `getTopoNodes()` / `getTopoEdges()` / `getTopoGraph()` — 拓扑图可视化 (后端无图 API)
- `getGeoData()` — GIS 数据 (后端无 GIS)

### 4.2 dashboardApi.ts — 仪表盘 (3 个 Mock 数据源)

**已对接:** 5 个真实端点 — summary, alarm-trend, device-status, kpi-trend, region-stats + topAlarmDevices (从 summary 提取)

**仍使用 Mock 的复合数据 (`dashboardApi.ts:165-176`):**
- `widgets` — 仪表盘 widget 布局配置 (通过 `dashboardService.getDashboardData()` 获取)
- `kpiTimeSeries` — KPI 时序图表数据 (通过 `dashboardService.getChartData()` 获取)
- `alarmTypePie` — 告警类型饼图数据 (通过 `dashboardService.getChartData()` 获取)

### 4.3 pmApi.ts — 性能管理 (2 个函数委托 Mock)

**已对接:** 10 个端点 — KPI 定义, 计数器, KPI 值, KPI 时序, 阈值 CRUD (5), 聚合计数器, KPI 计算

**委托 Mock 且后端无对应 (`pmApi.ts:340-341`):**
- `getTasks()` — 性能采集任务列表
- `createTask()` — 创建性能采集任务

### 4.4 mrApi.ts — MR 管理 (7 个函数委托 Mock)

**已对接:** 3 个端点 — 文件列表, 数据查询, 文件下载

**委托 Mock 且后端无对应 (`mrApi.ts:181-187`):**
- `getIndicators()` / `getAllIndicators()` — MR 指标定义
- `getMappings()` / `updateMapping()` / `toggleMapping()` — MR 指标映射管理
- `exportMRData()` — MR 数据导出
- `getIndicatorStats()` — MR 指标统计

### 4.5 useConfig.ts — 配置管理 (8 个函数直接调用 Mock)

**已对接 (通过 templateApi + deviceApi + configSyncApi):**
- `useConfigTemplates` (CRUD, 5 函数) → `templateApi`
- `useConfigParams` → `deviceApi.getParameters()`
- `useUpdateConfigParam` → `configSyncApi.pushConfig()`

**直接调用 Mock 且后端无对应 (`useConfig.ts:95-167`):**
- `useBaselineConfigs` (CRUD, 5 函数) → `configService.getBaselines/createBaseline/updateBaseline/deleteBaselines` (L95-142)
- `useConfigTasks` (2 函数) → `configService.getTasks/createTask` (L144-160)
- `useNeighborParams` (1 函数) → `configService.getNeighbors` (L162-167)

### 4.6 useBackup.ts — 备份恢复 (5 个函数直接调用 Mock)

**已对接 (通过 backupApi):** 9 个端点 — 任务 CRUD + cancel + 计划 CRUD

**直接调用 Mock 且后端无对应 (`useBackup.ts:104-147`):**
- `useFTPConfigs` → `backupService.getFTPConfigs` (L104-108)
- `useCreateFTPConfig` → `backupService.createFTPConfig` (L111-119)
- `useUpdateFTPConfig` → `backupService.updateFTPConfig` (L122-130)
- `useDeleteFTPConfigs` → `backupService.deleteFTPConfigs` (L133-141)
- `useTestFTPConnection` → `backupService.testFTPConnection` (L143-146)

### 4.7 useSystem.ts — 系统管理 (1 个函数直接调用 Mock)

**已对接 (通过 adminApi):** Users CRUD + lock/unlock/reset-pwd + Roles CRUD + Permissions + Audit Logs (17 个端点)

**直接调用 Mock (`useSystem.ts:179-185`):**
- `useSystemInfo()` → `systemService.getSystemInfo()` — 后端无系统信息端点

---

## 五、useConfig.ts 特殊分析

`useConfig.ts` 是混合模式最复杂的 hook，部分函数使用 `useMock` 切换，部分直接调用 mock：

| Hook 函数 | 数据源 | 后端状态 |
|---|---|---|
| `useConfigParams` | `useMock ? configService : deviceApi.getParameters` | ✅ 已对接 |
| `useUpdateConfigParam` | `useMock ? configService : configSyncApi.pushConfig` | ✅ 已对接 |
| `useConfigTemplates` | `useMock ? configService : templateApi` | ✅ 已对接 |
| `useConfigTemplateById` | `useMock ? configService : templateApi` | ✅ 已对接 |
| `useCreateConfigTemplate` | `useMock ? configService : templateApi` | ✅ 已对接 |
| `useUpdateConfigTemplate` | `useMock ? configService : templateApi` | ✅ 已对接 |
| `useDeleteConfigTemplates` | `useMock ? configService : templateApi` | ✅ 已对接 |
| `useBaselineConfigs` (CRUD) | 直接 `configService` | ❌ 后端无基线配置 |
| `useConfigTasks` | 直接 `configService` | ❌ 后端无配置任务 |
| `useNeighborParams` | 直接 `configService` | ❌ 后端无邻区参数 |

---

## 六、量化统计

### 整体对齐度

```
                    Sprint 0-5    Sprint 0-9    全模块对齐后 (当前)
─────────────────────────────────────────────────────────────────
完全对齐端点:            ~64         ~120            ~198
部分对齐模块:              4            6               0
纯 Mock 模块:              7            3               0
后端无前端消费:           ~20           ~0               0
─────────────────────────────────────────────────────────────────
整体对齐率:              ~68%        ~92%            100%
```

### 按模块匹配度

| 模块 | Sprint 0-5 | Sprint 0-9 | 全模块对齐后 | 说明 |
|---|---|---|---|---|
| 认证登录 | 🟢 100% | 🟢 100% | 🟢 100% (3/3) | 无变化 |
| 设备管理 | 🟡 88% | 🟢 100% | 🟢 100% (11/11) | 无变化 |
| 告警管理 | 🟢 100% | 🟢 100% | 🟢 100% (11/11) | 无变化 |
| 配置模板 | 🟢 100% | 🟢 100% | 🟢 100% (5/5) | 无变化 |
| 配置同步 | 🔴 0% | 🟢 100% | 🟢 100% (3/3) | 无变化 |
| 配置管理 (基线/任务/邻区) | 🟡 56% | 🟡 64% | 🟢 **100% (15/15)** | +8: 基线 CRUD + 配置任务 + 邻区参数 |
| 软件管理 | 🟢 100% | 🟢 100% | 🟢 100% (7/7) | 无变化 |
| 用户管理 | 🟡 47% | 🟢 100% | 🟢 100% (17/17) | 无变化 |
| 性能管理 | 🟡 30% | 🟢 83% | 🟢 **100% (12/12)** | +2: PM Tasks CRUD |
| 拓扑管理 (含站点/图/GIS) | 🟡 14% | 🟡 57% | 🟢 **100% (15/15)** | +7: 站点 CRUD + 拓扑图 + GIS |
| Dashboard | 🟡 13% | 🟡 63% | 🟢 **100% (9/9)** | +4: widgets + alarm-type-pie + kpi-time-series |
| 日志管理 | 🟢 100% | 🟢 100% | 🟢 100% (2/2) | 无变化 |
| MR 管理 | 🟢 100% | 🟡 30% | 🟢 **100% (10/10)** | +7: 指标管理 + 映射 + 导出 + 统计 |
| 数据模型 | 🟢 100% | 🟢 100% | 🟢 100% (14/14) | 无变化 |
| 北向接口 | 🟢 100% | 🟢 100% | 🟢 100% (8/8) | 无变化 |
| 自动配置 | 🟢 100% | 🟢 100% | 🟢 100% (4/4) | 无变化 |
| 互操作测试 | 🟢 100% | 🟢 100% | 🟢 100% (4/4) | 无变化 |
| 备份恢复 (含 FTP) | 🔴 0% | 🟡 64% | 🟢 **100% (14/14)** | +5: FTP 配置 CRUD + 测试连接 |
| MML 控制台 | 🔴 0% | 🟢 100% | 🟢 100% (10/10) | 无变化 |
| 文件管理 | 🔴 0% | 🟢 100% | 🟢 100% (6/6) | 无变化 |
| NE 管理 | 🔴 0% | 🟢 100% | 🟢 100% (3/3) | 无变化 |
| **System Info** | 🔴 0% | 🔴 mock | 🟢 **100% (1/1)** | 新增: /system/info |
| **License** | 🔴 0% | 🔴 0% | 🟢 **100% (6/6)** | 全新模块: CRUD + 激活/吊销/导入/摘要 |
| **报表管理** | 🔴 0% | 🔴 0% | 🟢 **100% (9/9)** | 全新模块: 定义 CRUD + 记录 + 生成/下载 |
| **运维工具** | 🔴 0% | 🔴 0% | 🟢 **100% (13/13)** | 全新模块: 模板 CRUD + 任务生命周期 + 命令记录 |

---

## 七、开发清单 — 全部完成

> 以下所有项目已在全模块对齐开发中完成。

| 序号 | 功能 | 后端端点 | 状态 |
|---|---|---|---|
| 1 | System Info | `GET /system/info` | ✅ Phase A |
| 2 | Dashboard widgets | `GET/PUT /dashboard/widgets` | ✅ Phase A |
| 3 | Dashboard alarm-type-pie | `GET /dashboard/alarm-type-pie` | ✅ Phase A |
| 4 | Dashboard kpi-time-series | `GET /dashboard/kpi-time-series` | ✅ Phase A |
| 5 | PM Tasks | `GET/POST /pm/tasks` | ✅ Phase A |
| 6 | Config 基线管理 | `GET/POST/PUT/DELETE /config/baselines` | ✅ Phase B |
| 7 | Config 配置任务 | `GET/POST /config/tasks` | ✅ Phase B |
| 8 | Config 邻区参数 | `GET /config/neighbors` | ✅ Phase B |
| 9 | Backup FTP 配置 | `GET/POST/PUT/DELETE /backup/ftp-configs` + test | ✅ Phase B |
| 10 | MR 指标管理 | `GET /mr/indicators` + `/mr/indicators/all` | ✅ Phase B |
| 11 | MR 设备映射 | `GET/PUT /mr/mappings` + toggle | ✅ Phase B |
| 12 | MR 导出/统计 | `POST /mr/export` + `GET /mr/indicators/:code/stats` | ✅ Phase B |
| 13 | License 管理 | 6 endpoints: list/detail/summary/activate/revoke/import | ✅ Phase C |
| 14 | Topology 站点/图/GIS | 7 endpoints: sites CRUD + nodes/edges/graph/geo | ✅ Phase C |
| 15 | Reports 报表管理 | 9 endpoints: definitions CRUD + records + generate/download/sample | ✅ Phase C |
| 16 | OpsTools 运维工具 | 13 endpoints: templates CRUD + tasks lifecycle + command records | ✅ Phase C |

---

## 八、数据转换层说明

前端 API 服务中已建立的字段映射 (camelCase ↔ snake_case)：

### 通用 (http.ts 拦截器自动转换)

| 前端字段 | 后端字段 | 映射方式 |
|---|---|---|
| `pageSize` | `page_size` | 拦截器自动 |
| `sortField` | `sort_by` | 拦截器自动 |
| `sortOrder: 'ascend'/'descend'` | `sort_dir: 'asc'/'desc'` | 拦截器映射 |

### Device (deviceApi.ts)

| 前端字段 | 后端字段 | 映射位置 |
|---|---|---|
| `sn` | `serial_number` | `mapBackendDevice()` |
| `connStatus` | `status` | `mapStatus()` |
| `vendor` | `manufacturer` | `mapBackendDevice()` |
| `productType` | `product_class` | `mapBackendDevice()` |

### Alarm (alarmApi.ts)

| 前端字段 | 后端字段 | 映射位置 |
|---|---|---|
| `severity` (string: critical/major/minor/warning) | `severity` (number: 1-4) | `severityNumToStr()` |
| `ackStatus` | `acknowledged_at` (推导) | `mapBackendAlarm()` |

### Admin (adminApi.ts)

| 前端字段 | 后端字段 | 映射位置 |
|---|---|---|
| `status: 'inactive'` | `status: 'disabled'` | `mapBackendStatus()` |
| `roleName` | `name` | `mapBackendRole()` |
| `permissions: ['resource:action']` | `permissions: [{resource, action}]` | 字符串拆分/合并 |

### PM Thresholds (pmApi.ts) — Sprint 6-9 新增

| 前端字段 | 后端字段 | 映射位置 |
|---|---|---|
| `kpiCode` | `kpi_name` | `mapBackendThreshold()` |
| `operator: 'gt'/'lt'/...` | `comparison: '>'/'<'/...` | `mapComparisonToOperator()` |
| `warningValue` | `warning_threshold` | `mapBackendThreshold()` |
| `criticalValue` | `critical_threshold` | `mapBackendThreshold()` |
| `thresholdName` | `description` | `mapBackendThreshold()` |

### Topology (topologyApi.ts) — Sprint 6-9 新增

| 前端字段 | 后端字段 | 映射位置 |
|---|---|---|
| `Domain` 树结构 | `BackendDeviceGroup` 嵌套 | `mapGroupToDomain()` + `flattenTree()` |
| `level` (1-5) | 由嵌套深度推导 | `mapGroupToDomain()` |
| `parentId` | `parent_id` | `mapGroupToDomain()` |

### Dashboard (dashboardApi.ts) — Sprint 7 新增

| 前端字段 | 后端字段 | 映射位置 |
|---|---|---|
| `deviceCounts` | `device_stats` | `mapBackendSummary()` |
| `alarmCounts` | `alarm_stats` | `mapBackendSummary()` |
| `kpiSummary.rrcSuccRate` | `kpi_overview.rrc_succ_rate` | `mapBackendSummary()` |
| `deviceStatusPie[].name/value` | `Record<string, number>` | `mapDeviceStatusMap()` |
| `deviceByRegion[].total/online` | `device_count/online_count` | `mapRegionStats()` |

### Backup (backupApi.ts) — Sprint 8 新增

| 前端字段 | 后端字段 | 映射位置 |
|---|---|---|
| `backupType: 'config-only'` | `task_type: 'config_only'` | `mapBackendTaskType()` |
| `status: 'success'` | `status: 'completed'` | `mapBackendTaskStatus()` |
| `scheduleName` | `name` | `mapBackendSchedule()` |
| `cronExpression` | `cron_expr` | `mapBackendSchedule()` |

### File Manager (fileApi.ts) — Sprint 8 新增

| 前端字段 | 后端字段 | 映射位置 |
|---|---|---|
| `fileName` | `file_name` | `mapBackendFile()` |
| `FileStatus: 'available'` | `status: 'ready'` | `mapBackendStatus()` |
| `mimeType` | `content_type` | `mapBackendFile()` |

### MML Console (mmlApi.ts) — Sprint 8 新增

| 前端字段 | 后端字段 | 映射位置 |
|---|---|---|
| `commandName` | `command_name` | `mapBackendCommand()` |
| `commandCode` | `command_code` | `mapBackendCommand()` |
| `params: MMLParam[]` | `param_template: JSON` | `mapParamTemplate()` |
| `scriptName` | `script_name` | `mapBackendScript()` |
| `deviceSns` | `device_sns` | `mapBackendTask()` |

---

## 九、关键文件清单

### 前端已建的 API 服务文件 (19 个)

```
omcmb/webcode/src/services/api/
├── authApi.ts              ← 认证 (3 端点)
├── deviceApi.ts            ← 设备 (11 端点)
├── alarmApi.ts             ← 告警 (11 端点)
├── templateApi.ts          ← 配置模板 (5 端点)
├── configSyncApi.ts        ← 配置同步 (3 端点)
├── configBaselineApi.ts    ← 基线/任务/邻区 (8 端点)     [全模块对齐 新建]
├── softwareApi.ts          ← 软件/固件 (7 端点)
├── adminApi.ts             ← 用户/角色管理 (17 端点)
├── pmApi.ts                ← 性能 (12 端点)              [+2: tasks]
├── dashboardApi.ts         ← 仪表盘 (9 端点)             [+4: widgets/kpi/alarm]
├── logApi.ts               ← 日志 (2 端点)
├── mrApi.ts                ← MR (10 端点)                [+7: indicators/mappings]
├── topologyApi.ts          ← 拓扑 (15 端点)              [+7: sites/graph/geo]
├── datamodelApi.ts         ← 数据模型 (14 端点)
├── northboundApi.ts        ← 北向 (8 端点)
├── provisionApi.ts         ← 自动配置 (4 端点)
├── interopApi.ts           ← 互操作测试 (4 端点)
├── backupApi.ts            ← 备份恢复 (14 端点)          [+5: FTP configs]
├── fileApi.ts              ← 文件管理 (6 端点)
├── mmlApi.ts               ← MML 控制台 (10 端点)
├── systemApi.ts            ← 系统信息 (1 端点)           [全模块对齐 新建]
├── licenseApi.ts           ← License (6 端点)            [全模块对齐 新建]
├── reportsApi.ts           ← 报表 (9 端点)               [全模块对齐 新建]
└── opsToolsApi.ts          ← 运维工具 (13 端点)          [全模块对齐 新建]
```

### 纯 Mock Hook 文件: 0 个 (全部消除)

> 所有 21 个 Hook 文件均使用 `useMock ? mockService : realApi` 模式，无直接 mock 调用。

### 后端路由注册文件

```
omcgo/cmd/app/main.go                          ← 主路由注册 (~198 路由)
omcgo/internal/omcr/device/handler.go           ← /devices (11 路由)
omcgo/internal/omcr/admin/handler.go            ← /admin/users, /admin/roles, /auth (18 路由)
omcgo/internal/omcr/dashboard/handler.go        ← /dashboard/* (9 路由)    [+4: widgets/kpi/alarm]
omcgo/internal/omcr/syslog/handler.go           ← /logs/* (2 路由)
omcgo/internal/omcr/topology/handler.go         ← /groups/* + /sites/* + /topology/* (15 路由) [+7]
omcgo/internal/omcr/software/handler.go         ← /firmware, /upgrade-tasks (8 路由)
omcgo/internal/omcr/backup/handler.go           ← /backup/* (14 路由)      [+5: FTP configs]
omcgo/internal/omcr/filemanager/handler.go      ← /files/* (6 路由)
omcgo/internal/omcr/mml/handler.go              ← /mml/* (10 路由)
omcgo/internal/omcr/license/handler.go          ← /licenses/* (6 路由)     [全模块对齐 新建]
omcgo/internal/omcr/report/handler.go           ← /reports/* (9 路由)      [全模块对齐 新建]
omcgo/internal/omcr/ops/handler.go              ← /ops/* (13 路由)         [全模块对齐 新建]
omcgo/internal/config/template/handler.go       ← /templates (5 路由)
omcgo/internal/config/datamodel/handler.go      ← /datamodels, /oui (14 路由)
omcgo/internal/config/sync_handler.go           ← /config/sync/* (3 路由)
omcgo/internal/config/baseline/handler.go       ← /config/baselines + tasks + neighbors (8 路由) [新建]
omcgo/internal/alarm/handler.go                 ← /alarms (6 路由)
omcgo/internal/alarm/rule_handler.go            ← /alarms/rules (5 路由)
omcgo/internal/pm/handler.go                    ← /pm/* (7 路由)           [+2: tasks]
omcgo/internal/pm/threshold_handler.go          ← /pm/thresholds (5 路由)
omcgo/internal/mr/handler.go                    ← /mr/* (10 路由)          [+7: indicators/mappings]
omcgo/internal/infra/sysinfo.go                 ← /system/info (1 路由)    [新建]
omcgo/internal/provision/handler.go             ← /provisioning/tasks (4 路由)
omcgo/internal/interop/handler.go               ← /interop (4 路由)
omcgo/internal/northbound/router.go             ← /northbound (8 路由)
```

---

## 十、整合架构 (当前已实现)

```
┌─────────────────────────────────────────────────────────┐
│  React Pages / Components                               │
│  (useQuery / useMutation hooks)                         │
├─────────────────────────────────────────────────────────┤
│  src/hooks/api/useXxx.ts                                │
│  useMock ? mockService.fn() : realApi.fn()              │
├───────────────┬─────────────────────────────────────────┤
│ Mock Path     │ Real API Path                           │
│ mock/services │ services/api/xxxApi.ts                  │
│ ↓ 直接返回    │ ↓ axios → http.ts 拦截器                │
│   mock 数据   │   ↓ camelCase→snake_case                │
│               │   ↓ Authorization: Bearer <token>       │
│               │   ↓ 401 → auto refresh token            │
├───────────────┴─────────────────────────────────────────┤
│  Vite Dev Proxy: /api/* → http://localhost:8080/api/*   │
├─────────────────────────────────────────────────────────┤
│  omcgo Gin Server (port 8080)                           │
│  CORS Middleware → RequireAuth → RequireCarrier          │
│  → AuditLogger → Handler                                │
└─────────────────────────────────────────────────────────┘
```

环境变量控制：
- `.env.development`: `VITE_USE_MOCK=false` → 使用真实 API (覆盖 ~92% 功能)
- `.env.mock`: `VITE_USE_MOCK=true` → 使用 Mock 数据 (100% 功能)
- `.env.production`: `VITE_USE_MOCK=false` → 使用真实 API
