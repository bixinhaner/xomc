# OMC 前后端整合状态报告

> 更新日期: 2026-03-07 | 基于 Sprint 0-5 完成后的实际代码分析

## 一、现状总结

Sprint 0-5 全部完成后，前后端整合已取得重大进展：

```
前端 API 服务文件:     15 个 (src/services/api/*.ts)
前端 Hook 文件:        22 个 (src/hooks/api/*.ts)
后端注册路由:         ~110 个 (omcgo/cmd/app/main.go)
已对接真实 API 的端点:  ~75 个
仍委托 Mock 的函数:    ~35 个
纯 Mock Hook (无后端):   7 个
后端有但前端未消费:    ~20 个端点
```

**技术栈:**
- 前端: React 19 + TypeScript + Vite + Ant Design + Zustand + React Query + Axios
- 后端: Go 1.25 + Gin + PostgreSQL + TimescaleDB + Redis + MinIO
- 通信: REST API, JWT 认证, `VITE_USE_MOCK` 环境变量控制 mock/real 切换
- 基础设施: HTTP 拦截器 (camelCase↔snake_case 自动转换), Token 自动刷新, CORS 中间件

---

## 二、对齐矩阵

### A. 完全对齐 — 前端 real API → 后端路由，路径匹配 ✅ (10 个模块, ~64 个端点)

| 前端 API 服务 | 后端路由组 | 端点数 | 说明 |
|---|---|---|---|
| `authApi.ts` | `/auth/*` | 3 | login + refresh + me |
| `deviceApi.ts` | `/devices/*` + `/groups` | 7 | 设备 CRUD + 分组列表 + NE 列表 |
| `alarmApi.ts` | `/alarms/*` | 11 | active/history/stats/ack/clear + 规则 CRUD |
| `templateApi.ts` | `/templates/*` | 5 | 配置模板 CRUD |
| `softwareApi.ts` | `/firmware/*` + `/upgrade-tasks/*` | 7 | 固件 CRUD + 升级任务 |
| `mrApi.ts` | `/mr/*` | 3 | 文件列表 + 数据查询 + 下载 |
| `datamodelApi.ts` | `/datamodels/*` + `/oui/*` | 12 | 全套 CRUD + 导入导出 + 激活/废弃 + OUI |
| `northboundApi.ts` | `/northbound/*` | 8 | 推送目标 + 同步 + 导出 |
| `provisionApi.ts` | `/provisioning/tasks/*` | 4 | 任务 CRUD + 重试 |
| `interopApi.ts` | `/interop/*` | 4 | 测试用例 + 执行 + 验证 |

### B. 部分对齐 — 前端 API 服务存在，但部分函数仍委托 Mock ⚠️ (4 个模块)

| 前端 API 服务 | 已对接真实 API | 仍委托 Mock | 后端已有但未连接 |
|---|---|---|---|
| `adminApi.ts` | Users CRUD, Roles 列表, 审计日志 | resetPassword, lockUser, unlockUser | 后端有 `/admin/users/:id/reset-password`, `/lock`, `/unlock` |
| `pmApi.ts` | KPI 定义, PM 计数器, KPI 值/时序 | Thresholds CRUD, Counters(raw), Tasks | 后端有 `/pm/thresholds` CRUD |
| `topologyApi.ts` | getDomains, getDomainTree | getSites, getTopoNodes/Edges/Graph, getGeoData | 后端有 Group CRUD + 成员管理 |
| `dashboardApi.ts` | getSummary, getTopAlarmDevices | chartData, alarmTrend, deviceStatusPie, kpiTrend, regionStats | 后端仅 `/dashboard/summary` |

`logApi.ts` 已完全对接 (`/logs/system` + `/logs/ne-messages`)，无 mock 委托。

### C. 纯 Mock — 无 real API 服务，无后端端点 ❌ (7 个 Hook, ~29 个页面)

| Hook | Mock 服务 | 页面数 | 后端状态 | 备注 |
|---|---|---|---|---|
| `useBackup.ts` | backupService | 5 | ❌ 无后端 | 备份恢复功能 |
| `useLicense.ts` | licenseService | 3 | ❌ 无后端 | License 管理 |
| `useMML.ts` | mmlService | 3 | ❌ 无后端 | MML 命令控制台 |
| `useOpsTools.ts` | opsToolsService | 5 | ❌ 无后端 | 运维工具集 |
| `useReports.ts` | reportService | 4 | ❌ 无后端 | 报表生成/导出 |
| `useFiles.ts` | fileService | 7 | ❌ 无后端 | 通用文件管理 |
| `useNEs.ts` | neService | 2 | ❌ 无后端 | `deviceApi.getNEList` 可替代 |

### D. 后端有端点但前端完全未消费 (~20 个端点)

| 后端端点 | 模块 | 建议 |
|---|---|---|
| `POST /config/sync/push/:deviceId` | 配置同步 | 前端应新增 configSyncApi |
| `POST /config/sync/pull/:deviceId` | 配置同步 | 同上 |
| `GET /config/sync/status/:deviceId` | 配置同步 | 同上 |
| `GET /devices/stats` | 设备统计 | 可在设备页面补充调用 |
| `GET /devices/:id/parameters` | 设备参数 | 设备详情页应对接 |
| `POST /devices/:id/reboot` | 设备重启 | 设备详情页应添加重启按钮 |
| `GET /pm/counters/aggregated` | PM 聚合 | 性能图表可对接 |
| `POST /pm/kpi/calculate` | KPI 计算 | 性能页面可对接 |
| `POST /groups` (Create) | 分组创建 | 拓扑页面应对接 |
| `PUT /groups/:id` | 分组更新 | 拓扑页面应对接 |
| `DELETE /groups/:id` | 分组删除 | 拓扑页面应对接 |
| `POST /groups/:id/devices` | 添加设备到分组 | 拓扑页面应对接 |
| `DELETE /groups/:id/devices/:deviceId` | 移除设备 | 拓扑页面应对接 |
| `GET /groups/:id/devices` | 分组设备列表 | 拓扑页面应对接 |
| `GET /datamodels/resolve` | 数据模型解析 | 可在设备详情中使用 |
| `POST /admin/users/:id/roles` | 角色分配 | 用户管理页面应对接 |
| `DELETE /admin/users/:id/roles/:roleId` | 角色移除 | 用户管理页面应对接 |
| `GET /admin/permissions` | 权限列表 | 角色管理页面应对接 |
| `POST/PUT/DELETE /admin/roles/*` | 角色 CRUD | 角色管理页面应对接 |

---

## 三、已验证的 API 路径映射表

以下路径经过代码验证，前端 `http.get/post/put/delete` 的路径与后端 `RegisterRoutes` 注册的路径完全匹配：

### 认证 (authApi.ts → admin/handler.go)

| 前端调用 | 后端路由 | 状态 |
|---|---|---|
| `POST /auth/login` | `publicV1.POST("/auth/login")` | ✅ |
| `POST /auth/refresh` | `publicV1.POST("/auth/refresh")` | ✅ |
| `GET /auth/me` | `v1.GET("/auth/me")` | ✅ |

### 设备管理 (deviceApi.ts → device/handler.go)

| 前端调用 | 后端路由 | 状态 |
|---|---|---|
| `GET /devices` (list/NE/SN) | `devices.GET("")` | ✅ |
| `GET /devices/:id` | `devices.GET("/:id")` | ✅ |
| `POST /devices` | `devices.POST("")` | ✅ |
| `PUT /devices/:id` | `devices.PUT("/:id")` | ✅ |
| `DELETE /devices/:id` | `devices.DELETE("/:id")` | ✅ |
| `GET /groups` | `groups.GET("")` → ListTree | ✅ |

### 告警管理 (alarmApi.ts → alarm/handler.go + rule_handler.go)

| 前端调用 | 后端路由 | 状态 |
|---|---|---|
| `GET /alarms/active` | `alarms.GET("/active")` | ✅ |
| `GET /alarms/history` | `alarms.GET("/history")` | ✅ |
| `GET /alarms/statistics` | `alarms.GET("/statistics")` | ✅ |
| `GET /alarms/:id` | `alarms.GET("/:id")` | ✅ |
| `POST /alarms/:id/acknowledge` | `alarms.POST("/:id/acknowledge")` | ✅ |
| `POST /alarms/:id/clear` | `alarms.POST("/:id/clear")` | ✅ |
| `GET /alarms/rules` | `alarmsGroup → rules.GET("")` | ✅ |
| `POST /alarms/rules` | `rules.POST("")` | ✅ |
| `PUT /alarms/rules/:id` | `rules.PUT("/:id")` | ✅ |
| `DELETE /alarms/rules/:id` | `rules.DELETE("/:id")` | ✅ |

### 配置模板 (templateApi.ts → config/template/handler.go)

| 前端调用 | 后端路由 | 状态 |
|---|---|---|
| `GET /templates` | `templates.GET("")` | ✅ |
| `GET /templates/:id` | `templates.GET("/:id")` | ✅ |
| `POST /templates` | `templates.POST("")` | ✅ |
| `PUT /templates/:id` | `templates.PUT("/:id")` | ✅ |
| `DELETE /templates/:id` | `templates.DELETE("/:id")` | ✅ |

### 软件管理 (softwareApi.ts → software/handler.go)

| 前端调用 | 后端路由 | 状态 |
|---|---|---|
| `GET /firmware` | `firmware.GET("")` | ✅ |
| `GET /firmware/:id` | `firmware.GET("/:id")` | ✅ |
| `POST /firmware` | `firmware.POST("")` | ✅ |
| `DELETE /firmware/:id` | `firmware.DELETE("/:id")` | ✅ |
| `GET /upgrade-tasks` | `upgradeTasks.GET("")` | ✅ |
| `GET /upgrade-tasks/:id` | `upgradeTasks.GET("/:id")` | ✅ |
| `POST /upgrade-tasks/batch` | `upgradeTasks.POST("/batch")` | ✅ |

### 用户管理 (adminApi.ts → admin/handler.go)

| 前端调用 | 后端路由 | 状态 |
|---|---|---|
| `GET /admin/users` | `adminGroup → users.GET("")` | ✅ |
| `GET /admin/users/:id` | `users.GET("/:id")` | ✅ |
| `POST /admin/users` | `users.POST("")` | ✅ |
| `PUT /admin/users/:id` | `users.PUT("/:id")` | ✅ |
| `DELETE /admin/users/:id` | `users.DELETE("/:id")` | ✅ |
| `GET /admin/roles` | `roles.GET("")` | ✅ |
| `GET /admin/audit-logs` | `adminGroup.GET("/audit-logs")` | ✅ |

### 性能管理 (pmApi.ts → pm/handler.go)

| 前端调用 | 后端路由 | 状态 |
|---|---|---|
| `GET /pm/kpi/definitions` | `pm.GET("/kpi/definitions")` | ✅ |
| `GET /pm/counters` | `pm.GET("/counters")` | ✅ |
| `GET /pm/kpi` | `pm.GET("/kpi")` | ✅ |

### Dashboard (dashboardApi.ts → dashboard/handler.go)

| 前端调用 | 后端路由 | 状态 |
|---|---|---|
| `GET /dashboard/summary` | `dashboard.GET("/summary")` | ✅ |

### 日志管理 (logApi.ts → syslog/handler.go)

| 前端调用 | 后端路由 | 状态 |
|---|---|---|
| `GET /logs/system` | `logs.GET("/system")` | ✅ |
| `GET /logs/ne-messages` | `logs.GET("/ne-messages")` | ✅ |

### MR 管理 (mrApi.ts → mr/handler.go)

| 前端调用 | 后端路由 | 状态 |
|---|---|---|
| `GET /mr/files` | `mr.GET("/files")` | ✅ |
| `GET /mr/files/:id/download` | `mr.GET("/files/:id/download")` | ✅ |
| `GET /mr/data` | `mr.GET("/data")` | ✅ |

### 数据模型 (datamodelApi.ts → config/datamodel/handler.go)

| 前端调用 | 后端路由 | 状态 |
|---|---|---|
| `GET /datamodels` | `datamodels.GET("")` | ✅ |
| `GET /datamodels/:id` | `datamodels.GET("/:id")` | ✅ |
| `POST /datamodels` | `datamodels.POST("")` | ✅ |
| `PUT /datamodels/:id` | `datamodels.PUT("/:id")` | ✅ |
| `DELETE /datamodels/:id` | `datamodels.DELETE("/:id")` | ✅ |
| `POST /datamodels/:id/activate` | `datamodels.POST("/:id/activate")` | ✅ |
| `POST /datamodels/:id/deprecate` | `datamodels.POST("/:id/deprecate")` | ✅ |
| `GET /datamodels/statistics` | `datamodels.GET("/statistics")` | ✅ |
| `POST /datamodels/cache/refresh` | `datamodels.POST("/cache/refresh")` | ✅ |
| `GET /datamodels/:id/export` | `datamodels.GET("/:id/export")` | ✅ |
| `POST /datamodels/import` | `datamodels.POST("/import")` | ✅ |
| `GET /oui` + `POST /oui` | `oui.GET("")` + `oui.POST("")` | ✅ |

### 北向接口 (northboundApi.ts → northbound/router.go)

| 前端调用 | 后端路由 | 状态 |
|---|---|---|
| `GET /northbound/push/targets` | `nb.GET("/push/targets")` | ✅ |
| `POST /northbound/push/targets` | `nb.POST("/push/targets")` | ✅ |
| `DELETE /northbound/push/targets/:id` | `nb.DELETE("/push/targets/:id")` | ✅ |
| `GET /northbound/sync/full` | `nb.GET("/sync/full")` | ✅ |
| `GET /northbound/sync/incremental` | `nb.GET("/sync/incremental")` | ✅ |
| `POST /northbound/export/pm` | `nb.POST("/export/pm")` | ✅ |
| `POST /northbound/export/alarms` | `nb.POST("/export/alarms")` | ✅ |
| `GET /northbound/export/config/:deviceId` | `nb.GET("/export/config/:deviceId")` | ✅ |

### 自动配置 (provisionApi.ts → provision/handler.go)

| 前端调用 | 后端路由 | 状态 |
|---|---|---|
| `GET /provisioning/tasks` | `tasks.GET("")` | ✅ |
| `GET /provisioning/tasks/:id` | `tasks.GET("/:id")` | ✅ |
| `POST /provisioning/tasks` | `tasks.POST("")` | ✅ |
| `POST /provisioning/tasks/:id/retry` | `tasks.POST("/:id/retry")` | ✅ |

### 互操作测试 (interopApi.ts → interop/handler.go)

| 前端调用 | 后端路由 | 状态 |
|---|---|---|
| `GET /interop/test-cases` | `interop.GET("/test-cases")` | ✅ |
| `POST /interop/run` | `interop.POST("/run")` | ✅ |
| `POST /interop/run/:category` | `interop.POST("/run/:category")` | ✅ |
| `POST /interop/validate/:deviceId` | `interop.POST("/validate/:deviceId")` | ✅ |

---

## 四、部分对齐模块详情

### 4.1 adminApi.ts — 用户管理 (3 个函数委托 Mock)

**已对接:** Users CRUD (5), Roles 列表 (2), 审计日志 (1) = 8 个端点

**委托 Mock 但后端已有:**

| 前端 Mock 函数 | 后端端点 | 差距原因 | 改动量 |
|---|---|---|---|
| `resetPassword()` → `systemService` (L265) | `POST /admin/users/:id/reset-password` | 前端未连接 | S |
| `lockUser()` → `systemService` (L266) | `POST /admin/users/:id/lock` | 前端未连接 | S |
| `unlockUser()` → `systemService` (L267) | `POST /admin/users/:id/unlock` | 前端未连接 | S |

**后端有但 adminApi 未实现:**
- `POST /admin/users/:id/roles` — 角色分配
- `DELETE /admin/users/:id/roles/:roleId` — 角色移除
- `GET /admin/permissions` — 权限列表
- `POST /admin/roles` — 创建角色
- `PUT /admin/roles/:id` — 更新角色
- `DELETE /admin/roles/:id` — 删除角色
- `GET /admin/roles/:id` — 角色详情

### 4.2 pmApi.ts — 性能管理 (6 个函数委托 Mock)

**已对接:** KPI 定义 (2), PM 计数器 (1), KPI 值/时序 (3) = 6 个端点

**委托 Mock 但后端已有:**

| 前端 Mock 函数 | 后端端点 | 差距原因 | 改动量 |
|---|---|---|---|
| `getThresholds()` → `performanceService` (L223) | `GET /pm/thresholds` | 前端未连接 | M |
| `createThreshold()` → `performanceService` (L224) | `POST /pm/thresholds` | 前端未连接 | S |
| `updateThreshold()` → `performanceService` (L225) | `PUT /pm/thresholds/:id` | 前端未连接 | S |
| `deleteThresholds()` → `performanceService` (L226) | `DELETE /pm/thresholds/:id` | 前端未连接 | S |

**委托 Mock 且后端无对应:**
- `getCounters()` (原始计数器定义列表) — 后端 `/pm/counters` 是时序数据，非定义列表
- `getTasks()` / `createTask()` (性能采集任务) — 后端无性能任务管理

**后端有但 pmApi 未调用:**
- `GET /pm/counters/aggregated` — 聚合计数器
- `POST /pm/kpi/calculate` — KPI 手动计算

### 4.3 topologyApi.ts — 拓扑管理 (6 个函数委托 Mock)

**已对接:** getDomains, getDomainTree → `GET /groups` = 1 个端点

**委托 Mock 且后端无对应:**
- `getSites()`, `getSiteById()` — 站点管理 (后端无站点概念)
- `getTopoNodes()`, `getTopoEdges()`, `getTopoGraph()` — 拓扑图 (后端无拓扑图)
- `getGeoData()` — GIS 数据 (后端无 GIS)

**后端有但前端未连接:**
- `POST /groups` — 创建分组
- `PUT /groups/:id` — 更新分组
- `DELETE /groups/:id` — 删除分组
- `POST /groups/:id/devices` — 添加设备到分组
- `DELETE /groups/:id/devices/:deviceId` — 从分组移除设备
- `GET /groups/:id/devices` — 查询分组下的设备

### 4.4 dashboardApi.ts — 仪表盘 (5 个函数委托 Mock)

**已对接:** getSummary, getTopAlarmDevices → `GET /dashboard/summary` = 1 个端点

**委托 Mock 且后端无对应:**
- `getChartData()` — 图表数据 (需后端补充聚合接口)
- `getAlarmTrend()` — 告警趋势 (可从 `/alarms/history` 聚合)
- `getDeviceStatusPie()` — 设备状态饼图 (可从 `/devices/stats` 获取)
- `getKPITrend()` — KPI 趋势 (可从 `/pm/kpi` 聚合)
- `getRegionStats()` — 区域统计 (需后端补充)

---

## 五、useConfig.ts 特殊分析

`useConfig.ts` 是混合模式最复杂的 hook，部分函数使用 `useMock` 切换，部分直接调用 mock：

| Hook 函数 | 数据源 | 后端状态 |
|---|---|---|
| `useConfigTemplates` | `useMock ? configService : templateApi` | ✅ 已有 |
| `useConfigTemplateById` | `useMock ? configService : templateApi` | ✅ 已有 |
| `useCreateConfigTemplate` | `useMock ? configService : templateApi` | ✅ 已有 |
| `useUpdateConfigTemplate` | `useMock ? configService : templateApi` | ✅ 已有 |
| `useDeleteConfigTemplates` | `useMock ? configService : templateApi` | ✅ 已有 |
| `useConfigParams` | 直接 `configService` | ⚠️ 后端有 `/config/sync/*` |
| `useUpdateConfigParam` | 直接 `configService` | ⚠️ 后端有 `/config/sync/push` |
| `useBaselineConfigs` (CRUD) | 直接 `configService` | ❌ 后端无基线配置 |
| `useConfigTasks` | 直接 `configService` | ❌ 后端无配置任务 |
| `useNeighborParams` | 直接 `configService` | ❌ 后端无邻区参数 |

---

## 六、量化统计

### 整体对齐度

```
                    原文档 (Sprint前)    当前实际 (Sprint后)
─────────────────────────────────────────────────────────
完全对齐端点:            ~35             ~64  (+83%)
部分对齐模块:             0               4  (新增 API 层)
纯 Mock 模块:            18               7  (-61%)
后端无API的前端模块:       8               6  (Dashboard/日志已补)
前端未覆盖的后端功能:     ~30             ~20  (部分已建前端页面)
─────────────────────────────────────────────────────────
整体对齐率:            ~20%            ~68%
```

### 按模块匹配度 (更新后)

| 模块 | 旧匹配度 | 新匹配度 | 变化 | 状态 |
|---|---|---|---|---|
| 认证登录 | 🟡 70% | 🟢 100% | ↑ | 完全对齐 |
| 设备管理 | 🟡 40% | 🟢 90% | ↑↑ | CRUD 全通, 缺 stats/params/reboot 前端调用 |
| 告警管理 | 🟡 65% | 🟢 95% | ↑ | 含规则 CRUD, 完整对齐 |
| 配置管理 | 🟡 50% | 🟡 60% | ↑ | 模板对齐, 参数/基线/邻区仍 mock |
| 性能管理 | 🟡 55% | 🟡 65% | ↑ | 核心对齐, 阈值/任务仍 mock |
| 软件管理 | 🟢 70% | 🟢 100% | ↑ | 完全对齐 |
| 拓扑管理 | 🟡 45% | 🟡 50% | → | 仅域树对齐, 站点/GIS 仍 mock |
| 用户管理 | 🟢 70% | 🟢 85% | ↑ | CRUD+审计对齐, 缺密码重置/锁定连接 |
| MR 管理 | 🔴 30% | 🟢 100% | ↑↑ | 完全对齐 |
| Dashboard | 🔴 0% | 🟡 40% | ↑↑ | summary 对齐, 图表仍 mock |
| 日志管理 | 🔴 15% | 🟢 100% | ↑↑ | 完全对齐 |
| 数据模型 | — (前端无) | 🟢 100% | NEW | Sprint 4-5 新增, 完全对齐 |
| 北向接口 | — (前端无) | 🟢 100% | NEW | Sprint 4-5 新增, 完全对齐 |
| 自动配置 | — (前端无) | 🟢 100% | NEW | Sprint 3-4 新增, 完全对齐 |
| 互操作测试 | — (前端无) | 🟢 100% | NEW | Sprint 5 新增, 完全对齐 |
| 备份恢复 | 🔴 0% | 🔴 0% | → | 仍无后端 |
| MML 控制台 | 🔴 0% | 🔴 0% | → | 仍无后端 |
| 报表管理 | 🔴 0% | 🔴 0% | → | 仍无后端 |
| License | 🔴 0% | 🔴 0% | → | 仍无后端 |
| 运维工具 | 🔴 0% | 🔴 0% | → | 仍无后端 |
| 文件管理 | 🔴 15% | 🔴 15% | → | 仍无通用后端 (MR/固件已覆盖) |
| NE 管理 | 🔴 10% | 🔴 10% | → | hook 仍 mock, 但 deviceApi 有替代 |

---

## 七、仍需开发清单

### 7.1 快速收益 — 前端连接已有后端端点 (改动量 S-M)

这些后端端点已存在，只需前端 API 服务文件中添加调用：

| 序号 | 改动 | 前端文件 | 后端端点 | 工作量 |
|---|---|---|---|---|
| 1 | adminApi 补充密码重置 | `adminApi.ts` L265 | `POST /admin/users/:id/reset-password` | S |
| 2 | adminApi 补充锁定/解锁 | `adminApi.ts` L266-267 | `POST /admin/users/:id/lock`, `/unlock` | S |
| 3 | adminApi 补充权限列表 | `adminApi.ts` (新增) | `GET /admin/permissions` | S |
| 4 | adminApi 补充角色 CRUD | `adminApi.ts` (新增) | `POST/PUT/DELETE /admin/roles` | M |
| 5 | adminApi 补充角色分配 | `adminApi.ts` (新增) | `POST/DELETE /admin/users/:id/roles` | S |
| 6 | pmApi 连接阈值 CRUD | `pmApi.ts` L222-227 | `GET/POST/PUT/DELETE /pm/thresholds` | M |
| 7 | pmApi 连接聚合计数器 | `pmApi.ts` (新增) | `GET /pm/counters/aggregated` | S |
| 8 | pmApi 连接 KPI 计算 | `pmApi.ts` (新增) | `POST /pm/kpi/calculate` | S |
| 9 | topologyApi 补充分组 CRUD | `topologyApi.ts` (新增) | `POST/PUT/DELETE /groups` | M |
| 10 | topologyApi 补充成员管理 | `topologyApi.ts` (新增) | `POST/DELETE/GET /groups/:id/devices` | M |
| 11 | deviceApi 补充设备参数 | `deviceApi.ts` (新增) | `GET /devices/:id/parameters` | S |
| 12 | deviceApi 补充设备重启 | `deviceApi.ts` (新增) | `POST /devices/:id/reboot` | S |
| 13 | 新增 configSyncApi | 新建文件 | `/config/sync/push`, `/pull`, `/status` | M |
| 14 | useNEs 切换到 deviceApi | `useNEs.ts` 改 import | `deviceApi.getNEList` 已存在 | S |

**合计:** ~14 项, 预计 3-5 天

### 7.2 后端补充开发 — 前端页面已有但无后端 API

| 序号 | 缺失功能 | 前端 Hook | 需新增后端 API | 优先级 | 工作量 |
|---|---|---|---|---|---|
| 1 | Dashboard 图表聚合 | dashboardApi mock 函数 | 告警趋势/设备饼图/KPI趋势/区域统计 | ⭐⭐⭐ | M |
| 2 | 配置参数管理 | useConfig → configService | 参数列表/更新 (或对接 config/sync) | ⭐⭐ | M |
| 3 | 基线配置 | useConfig → configService | CRUD /api/v1/config/baselines | ⭐⭐ | L |
| 4 | 配置任务 | useConfig → configService | 与 provisioning 整合或新增 | ⭐ | M |
| 5 | 性能采集任务 | usePerformance → performanceService | CRUD /api/v1/pm/tasks | ⭐ | M |
| 6 | 站点管理 | useTopology → topologyService | CRUD /api/v1/sites | ⭐ | L |
| 7 | 拓扑图/GIS | useTopology → topologyService | /api/v1/topology/graph, /geo | ⭐ | XL |
| 8 | 备份恢复 | useBackup → backupService | CRUD /api/v1/backup | ⭐ | XL |
| 9 | MML 控制台 | useMML → mmlService | /api/v1/mml/execute | ⭐ | XL |
| 10 | 报表生成 | useReports → reportService | /api/v1/reports | ⭐ | XL |
| 11 | License 管理 | useLicense → licenseService | CRUD /api/v1/licenses | ⭐ | L |
| 12 | 运维工具 | useOpsTools → opsToolsService | /api/v1/ops/* | ⭐ | L |
| 13 | 文件管理 | useFiles → fileService | /api/v1/files | ⭐ | M |

---

## 八、数据转换层说明

前端 API 服务中已建立的字段映射 (camelCase ↔ snake_case)：

| 模块 | 前端字段 | 后端字段 | 映射位置 |
|---|---|---|---|
| Device | `sn` | `serial_number` | deviceApi.ts `mapBackendDevice` |
| Device | `connStatus` | `status` | deviceApi.ts `mapStatus` |
| Device | `vendor` | `manufacturer` | deviceApi.ts L56-78 |
| Alarm | `severity` (string) | `severity` (number 1-4) | alarmApi.ts `severityNumToStr` |
| Alarm | `ackStatus` | `acknowledged_at` (推导) | alarmApi.ts L72 |
| Admin | `status: 'inactive'` | `status: 'disabled'` | adminApi.ts `mapBackendStatus` |
| Template | `templateName` | `name` | templateApi.ts `mapBackendTemplate` |
| Template | `params` | `parameters` | templateApi.ts L51-53 |
| PM | `kpiCode` | `kpi_name` | pmApi.ts L87-98 |
| PM | `granularity` (string) | `granularity` (minutes) | pmApi.ts `granularityLabel` |
| Pagination | `pageSize` | `page_size` | http.ts 拦截器自动转换 |
| Pagination | `sortOrder: 'ascend'` | `sort_dir: 'asc'` | http.ts 拦截器 |

---

## 九、关键文件清单

### 前端已建的 API 服务文件 (15 个)

```
omcmb/webcode/src/services/api/
├── authApi.ts          ← 认证 (3 端点)
├── deviceApi.ts        ← 设备 (7 端点)
├── alarmApi.ts         ← 告警 (11 端点)
├── templateApi.ts      ← 配置模板 (5 端点)
├── softwareApi.ts      ← 软件/固件 (7 端点)
├── adminApi.ts         ← 用户管理 (8 端点 + 3 mock)
├── pmApi.ts            ← 性能 (6 端点 + 6 mock)
├── dashboardApi.ts     ← 仪表盘 (1 端点 + 5 mock)
├── logApi.ts           ← 日志 (2 端点)
├── mrApi.ts            ← MR (3 端点)
├── topologyApi.ts      ← 拓扑 (1 端点 + 6 mock)
├── datamodelApi.ts     ← 数据模型 (12 端点)
├── northboundApi.ts    ← 北向 (8 端点)
├── provisionApi.ts     ← 自动配置 (4 端点)
└── interopApi.ts       ← 互操作测试 (4 端点)
```

### 仍为纯 Mock 的 Hook 文件 (7 个)

```
omcmb/webcode/src/hooks/api/
├── useBackup.ts        ← 直接引用 backupService
├── useLicense.ts       ← 直接引用 licenseService
├── useMML.ts           ← 直接引用 mmlService
├── useOpsTools.ts      ← 直接引用 opsToolsService
├── useReports.ts       ← 直接引用 reportService
├── useFiles.ts         ← 直接引用 fileService
└── useNEs.ts           ← 直接引用 neService
```

### 后端路由注册文件

```
omcgo/cmd/app/main.go                          ← 主路由注册 (L256-371)
omcgo/internal/omcr/device/handler.go           ← /devices
omcgo/internal/omcr/admin/handler.go            ← /admin/users, /admin/roles, /auth
omcgo/internal/omcr/dashboard/handler.go        ← /dashboard/summary
omcgo/internal/omcr/syslog/handler.go           ← /logs/system, /logs/ne-messages
omcgo/internal/omcr/topology/handler.go         ← /groups
omcgo/internal/omcr/software/handler.go         ← /firmware, /upgrade-tasks
omcgo/internal/config/template/handler.go       ← /templates
omcgo/internal/config/datamodel/handler.go      ← /datamodels, /oui
omcgo/internal/config/sync_handler.go           ← /config/sync/*
omcgo/internal/alarm/handler.go                 ← /alarms
omcgo/internal/alarm/rule_handler.go            ← /alarms/rules
omcgo/internal/pm/handler.go                    ← /pm/counters, /pm/kpi
omcgo/internal/pm/threshold_handler.go          ← /pm/thresholds
omcgo/internal/mr/handler.go                    ← /mr
omcgo/internal/provision/handler.go             ← /provisioning/tasks
omcgo/internal/interop/handler.go               ← /interop
omcgo/internal/northbound/router.go             ← /northbound
```

---

## 十、整合架构 (当前已实现)

```
┌─────────────────────────────────────────────────────────┐
│  React Pages / Components                               │
│  (useQuery / useMutation hooks)                         │
├─────────────────────────────────────────────────────────┤
│  src/hooks/api/useXxx.ts                                │
│  useMock ? mockService.fn() : realApi.fn()              │
├───────────────┬─────────────────────────────────────────┤
│ Mock Path     │ Real API Path                           │
│ mock/services │ services/api/xxxApi.ts                  │
│ ↓ 直接返回    │ ↓ axios → http.ts 拦截器                │
│   mock 数据   │   ↓ camelCase→snake_case                │
│               │   ↓ Authorization: Bearer <token>       │
│               │   ↓ 401 → auto refresh token            │
├───────────────┴─────────────────────────────────────────┤
│  Vite Dev Proxy: /api/* → http://localhost:8080/api/*   │
├─────────────────────────────────────────────────────────┤
│  omcgo Gin Server (port 8080)                           │
│  CORS Middleware → RequireAuth → RequireCarrier          │
│  → AuditLogger → Handler                                │
└─────────────────────────────────────────────────────────┘
```

环境变量控制：
- `.env.development`: `VITE_USE_MOCK=false` → 使用真实 API
- `.env.mock`: `VITE_USE_MOCK=true` → 使用 Mock 数据
- `.env.production`: `VITE_USE_MOCK=false` → 使用真实 API
