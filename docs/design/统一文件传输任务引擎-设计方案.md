# 统一文件传输任务引擎（UFTE）设计方案

> **文档版本**：v2.2  
> **日期**：2026-05-14  
> **目标**：消除 eNB 升级、日志收集等功能中大量重复的 TR-069 Download/Upload 交互代码，
> 将文件传输流程抽象为**配置驱动的通用引擎**，并提供**可视化配置管理页面**，
> 内置功能可查看，用户可在页面上自行新增和维护自定义文件传输类型，无需修改任何代码。

---

## 回答核心问题

**Q1：这个设计能做到文件传输类操作动态可配置吗？**

**可以，但有明确边界**。以下维度完全动态可配置（只改数据/配置，零代码）：
- TR-069 RPC 类型（Download / Upload）
- FileType 字符串、CommandKey 前缀、URL 模板
- 步骤执行顺序（step_chain 数组）
- 每步超时时长
- TransferComplete 之后等待哪个 Inform EventCode 以及成功/失败判定值
- 平台级差异覆盖（不同平台 FileType 不同）
- 前置校验规则（磁盘空间、文件数量等）

以下情况**仍需代码**（极少发生）：
- 全新的交互模式（如需要设备侧回调一个完全不存在的机制）
- 复杂的特殊业务前置校验（现有规则类型覆盖不了时，需实现新的 `PreValidateRule` 并注册）
- TR069Server 需要监听新的 EventCode，需注册一次（后续再来同类型 EventCode 则无需改代码）

---

**Q2：新增文件传输类型时，可以通过页面配置而非直接写数据库吗？**

**是的，这正是本方案的核心设计**。提供专用的「任务类型管理」页面：
- 系统内置类型（4G升级、5G升级、日志收集、回退、备份、恢复等）：**可查看、可局部调参、不可删除**
- 用户自定义类型：**可视化表单创建/编辑，支持复用已有业务 Tab 或输入新的 Tab 名称；删除建议放二期并增加“已有任务引用”校验**

---

---

## 一、现状问题分析

### 1.1 各功能共性流程

```
┌─────────────────────────────────────────────────────────────────┐
│  权限检查 → 在线检查 → 发送 TR-069 RPC → 等待响应 →            │
│  等待文件传输（进度监控）→ 等待 TransferComplete →              │
│  [可选] 等待附加事件（102 UPGRADE FINISH 等）→ 结束             │
└─────────────────────────────────────────────────────────────────┘
```

### 1.2 各功能差异点汇总

| 差异维度 | 4G 基站升级 | 5G 基站升级 | 日志收集（立即）| 日志收集（周期）|
|---------|------------|------------|--------------|--------------|
| **TR-069 RPC 类型** | Download | Download | Upload | SetParameterValues |
| **TR-069 FileType 字段** | `1`（Firmware Upgrade Image） | `1`（Firmware Upgrade Image） | `6/7/8/9`（运行/安全/故障/PCAP） | — |
| **CommandKey 前缀** | `Download Upgrade,` | `Download Upgrade,` | `Collect LOG,` | — |
| **URL 模板（逻辑值）** | `firmware/{minio_path}` | 同 4G | `/smallcell/FileUploadService?fileType={fileType}&filename={targetFileName}` | `/smallcell/FileUploadService?fileType={fileType}&filename={targetFileName}` |
| **等待文件下载进度** | ✅ 轮询 `DownloadingFlag` | ✅ 同 4G | ❌ 无（设备主动上传） | — |
| **等待 TransferComplete** | ✅ 是终态 | ✅ 非终态，继续等 | ✅ 是终态（文件到达即完成）| — |
| **附加事件等待** | ❌ 无 | ✅ 等待 `102 UPGRADE FINISH`| ❌ 无 | — |
| **TC 超时容错** | ✅ 版本比对 | ✅ 版本比对 | ❌ 直接失败 | — |
| **平台限制** | 全平台 | BBU-XSS/BBU-QSS | 部分平台不支持周期日志 | Intel_CR 等不支持 |
| **保留配置字段** | ✅ `rawMode` | ✅ `rawMode` | ❌ | — |
| **文件数量上限校验** | ❌ | ❌ | ✅（默认 5 个）| ❌ |

### 1.3 冗余代码量化

当前每新增一个类似功能，需要重复实现：
- **后端**：独立 Controller、Service、状态机、Redis Key 约定、超时处理（≈ 800~1500 行）
- **前端**：独立页面、任务列表、进度轮询、操作按钮（≈ 500~1000 行）
- **DB**：独立任务表（主任务 + 子任务两张表）
- **文档**：独立接口文档

---

## 二、设计目标

1. **配置驱动**：新增"新型升级"或"其他文件传输"功能时，只需在数据库新增一行任务类型配置，无需修改后端代码
2. **步骤链可组合**：将传输流程拆解为原子步骤，通过 JSON 配置自由组合
3. **事件等待可扩展**：通过配置指定 TransferComplete 之后需要等待哪个 EventCode，未来新的 EventCode 无需改代码
4. **平台差异可注入**：FileType、URL 模板、CommandKey 前缀等通过配置注入
5. **UI 泛化**：前端基于任务类型元数据动态渲染，无需为每个功能单独写页面
6. **业务分类动态化**：业务 Tab 由任务类型的 `category/category_label` 聚合生成；新增模板时可复用现有 Tab，也可直接新建 Tab 名称

---

## 三、核心设计

### 3.1 步骤原子化（Step 枚举）

将所有流程节点拆解为以下 9 个原子步骤：

| 步骤 ID | 步骤名称 | 说明 |
|--------|---------|------|
| `CHECK_PERMISSION` | 权限检查 | 按 permissionCode 校验用户功能权限 |
| `CHECK_ONLINE` | 设备在线检查 | 查 Redis 设备缓存；离线可配置等待/立即失败 |
| `CHECK_CONFLICT` | 并发冲突检查 | 防止同一设备同时执行多个同类任务 |
| `PRE_VALIDATE` | 前置校验 | 文件数量上限、磁盘空间等业务规则（可插拔） |
| `SEND_RPC` | 发送 TR-069 RPC | Download 或 Upload 或 SetParameterValues |
| `WAIT_RPC_RESPONSE` | 等待 RPC 响应 | 等待设备返回 DownloadResponse / UploadResponse |
| `WAIT_FILE_TRANSFER` | 等待文件传输 | 轮询 `DownloadingFlag`（仅 Download 场景）|
| `WAIT_TRANSFER_COMPLETE` | 等待 TransferComplete | 订阅 Redis 中 TR069 进程写入的 TC 结果 |
| `WAIT_INFORM_EVENT` | 等待 Inform 事件 | 等待指定 EventCode 的 Inform（102/其他）|

### 3.2 任务类型配置表（`file_transfer_task_type`）

> 以下 DDL 以 **PostgreSQL 16** 为准，面向当前 Go + pgx + JSONB 的项目实现，不再使用 MySQL / Java 风格定义。

```sql
CREATE TABLE file_transfer_task_type (
    type_code                  TEXT PRIMARY KEY,
    category                   TEXT        NOT NULL,
    category_label             TEXT        NOT NULL,
    display_name               TEXT        NOT NULL,
    description                TEXT,

    rpc_type                   TEXT        NOT NULL CHECK (rpc_type IN ('DOWNLOAD', 'UPLOAD', 'SET_PARAM_VALUES')),
    tr069_file_type            TEXT,
    file_type_label            TEXT,
    file_type_editable         BOOLEAN     NOT NULL DEFAULT TRUE,
    command_key_prefix         TEXT,
    url_template               TEXT,
    transport_path             TEXT,
    target_file_name_template  TEXT,
    file_name_template         TEXT,
    file_size_field            TEXT,
    checksum_field             TEXT,
    raw_mode_default           TEXT,
    delay_seconds              INTEGER     NOT NULL DEFAULT 0,

    step_chain                 JSONB       NOT NULL,
    timeout_config             JSONB       NOT NULL DEFAULT '{}'::jsonb,
    post_tc_event              JSONB       NOT NULL DEFAULT '{}'::jsonb,
    tc_timeout_fallback        TEXT,
    permission_code            TEXT,
    platform_scope             JSONB       NOT NULL DEFAULT '[]'::jsonb,
    platform_overrides         JSONB       NOT NULL DEFAULT '{}'::jsonb,
    pre_validate_rules         JSONB       NOT NULL DEFAULT '[]'::jsonb,
    extra_rpc_fields           JSONB       NOT NULL DEFAULT '{}'::jsonb,

    built_in                   BOOLEAN     NOT NULL DEFAULT FALSE,
    enabled                    BOOLEAN     NOT NULL DEFAULT TRUE,
    last_editor                TEXT,
    created_at                 TIMESTAMPTZ NOT NULL DEFAULT NOW(),
    updated_at                 TIMESTAMPTZ NOT NULL DEFAULT NOW()
);

CREATE INDEX idx_file_transfer_task_type_category ON file_transfer_task_type(category);
CREATE INDEX idx_file_transfer_task_type_enabled ON file_transfer_task_type(enabled);
```

### 3.3 附加事件等待配置（`post_tc_event` JSON 结构）

```json
{
    "enabled": true,
    "eventCode": "102 UPGRADE FINISH",
    "redisKeyTemplate": "{cellCode}_upgrade_finish",
    "resultField": "upgradeStatus",
    "successValues": ["1"],
    "failValues": ["2", "3"],
    "timeoutMs": 600000,
    "preClean": {
        "deleteRedisKey": "{cellCode}_upgrade_finish"
    }
}
```

- `enabled = false` 时（4G 升级、日志收集），WAIT_TRANSFER_COMPLETE 就是终态，不再等附加事件
- `preClean.deleteRedisKey`：5G 升级在 SEND_RPC 完成后需要预删除残留标识，通过此配置实现

### 3.4 前置校验规则（`pre_validate_rules` JSON 结构）

```json
[
    {
        "ruleId": "DISK_USAGE_CHECK",
        "params": { "redisField": "disk_usage:run_log", "threshold": 90 }
    },
    {
        "ruleId": "FILE_COUNT_LIMIT",
        "params": {
            "configKey": "CollectMaxLogFileNum",
            "defaultLimit": 5,
            "countQueries": [
                "SELECT COUNT(*) FROM log_period_report_record WHERE small_cell_code = ?",
                "SELECT COUNT(*) FROM log_device_report_file WHERE file_name LIKE ?"
            ]
        }
    }
]
```

### 3.5 平台差异覆盖（`platform_overrides` JSON 结构）

```json
{
    "DXDF": {
        "tr069_file_type": "6",
        "transport_path": "/smallcell/FileUploadService?fileType={fileType}&filename={targetFileName}"
    },
    "BBU_QSS": {
        "tr069_file_type": "1",
        "post_tc_event": {
            "enabled": true,
            "eventCode": "102 UPGRADE FINISH"
        }
    }
}
```

### 3.6 统一任务表（`file_transfer_task`）

```sql
CREATE TABLE file_transfer_task (
    task_id          UUID PRIMARY KEY DEFAULT gen_random_uuid(),
    type_code        TEXT        NOT NULL REFERENCES file_transfer_task_type(type_code),
    category         TEXT        NOT NULL,
    category_label   TEXT        NOT NULL,
    task_name        TEXT        NOT NULL,
    operator_scope   TEXT,
    create_user      TEXT,
    execution_mode   TEXT        NOT NULL CHECK (execution_mode IN ('immediate', 'scheduled', 'suspended')),
    scheduled_at     TIMESTAMPTZ,
    task_status      TEXT        NOT NULL CHECK (task_status IN ('pending', 'in_progress', 'suspended', 'ended')),
    task_result      TEXT        CHECK (task_result IN ('success', 'partial', 'failure', 'terminated')),
    current_step     TEXT,
    task_params      JSONB       NOT NULL DEFAULT '{}'::jsonb,
    total_count      INTEGER     NOT NULL DEFAULT 0,
    success_count    INTEGER     NOT NULL DEFAULT 0,
    fail_count       INTEGER     NOT NULL DEFAULT 0,
    created_at       TIMESTAMPTZ NOT NULL DEFAULT NOW(),
    started_at       TIMESTAMPTZ,
    ended_at         TIMESTAMPTZ
);

CREATE TABLE file_transfer_sub_task (
    id               UUID PRIMARY KEY DEFAULT gen_random_uuid(),
    task_id          UUID        NOT NULL REFERENCES file_transfer_task(task_id) ON DELETE CASCADE,
    type_code        TEXT        NOT NULL,
    category         TEXT        NOT NULL,
    category_label   TEXT        NOT NULL,
    device_id        UUID,
    device_name      TEXT,
    device_sn        TEXT        NOT NULL,
    product_type     TEXT,
    platform_type    TEXT,
    current_step     TEXT,
    step_status      TEXT        NOT NULL CHECK (step_status IN ('pending', 'in_progress', 'waiting', 'suspended', 'ended', 'failed')),
    step_result      TEXT        CHECK (step_result IN ('success', 'failure', 'terminated')),
    progress         INTEGER     NOT NULL DEFAULT 0,
    failure_reason   TEXT,
    step_context     JSONB       NOT NULL DEFAULT '{}'::jsonb,
    current_version  TEXT,
    target_version   TEXT,
    last_report_at   TIMESTAMPTZ,
    executed_at      TIMESTAMPTZ,
    created_at       TIMESTAMPTZ NOT NULL DEFAULT NOW(),
    updated_at       TIMESTAMPTZ NOT NULL DEFAULT NOW()
);

CREATE INDEX idx_file_transfer_task_category ON file_transfer_task(category);
CREATE INDEX idx_file_transfer_task_status ON file_transfer_task(task_status);
CREATE INDEX idx_file_transfer_sub_task_task_id ON file_transfer_sub_task(task_id);
CREATE INDEX idx_file_transfer_sub_task_category ON file_transfer_sub_task(category);
CREATE INDEX idx_file_transfer_sub_task_device_sn ON file_transfer_sub_task(device_sn);
```

- `category/category_label`：前端业务 Tab 的直接数据来源，不单独维护一张 `tab` 表；页面按任务类型聚合生成 Tab。
- `file_transfer_sub_task` 同时承载设备级执行明细，因此前端“设备列表”可直接基于该表聚合或分页查询，无需额外设备执行表。
- 对外 API 建议使用 camelCase 字段（如 `fileType`、`categoryLabel`），数据库层保留 snake_case。

---

## 四、内置任务类型配置（现有功能迁移）

### 4.1 4G 基站 IMG 升级（ENB_IMG_UPGRADE）

```json
{
    "type_code":          "ENB_IMG_UPGRADE",
    "category":           "enb_upgrade",
    "category_label":     "4G升级",
    "display_name":       "4G 基站软件升级",
    "rpc_type":           "DOWNLOAD",
    "tr069_file_type":    "1",
    "file_type_label":    "Firmware Upgrade Image",
    "file_type_editable": true,
    "command_key_prefix": "Download Upgrade,",
    "url_template":       "firmware/{minio_path}",
    "transport_path":     "/smallcell/FileDownloadService/firmware/img/{path}",
    "file_name_template": "{firmware_name}",
    "target_file_name_template": "{firmware_name}",
    "file_size_field":    "firmware.fileSize",
    "checksum_field":     "firmware.md5",
    "step_chain":         ["CHECK_PERMISSION","CHECK_ONLINE","CHECK_CONFLICT","SEND_RPC","WAIT_RPC_RESPONSE","WAIT_FILE_TRANSFER","WAIT_TRANSFER_COMPLETE"],
    "timeout_config": {
        "CHECK_ONLINE":           3600000,
        "WAIT_RPC_RESPONSE":      60000,
        "WAIT_FILE_TRANSFER":     600000,
        "WAIT_TRANSFER_COMPLETE": 1200000
    },
    "post_tc_event":      { "enabled": false },
    "tc_timeout_fallback": "VERSION_COMPARE",
    "permission_code":    "CODE_ENB_UPGRADE_IMAGE",
    "platform_scope":     ["4G eNB", "QAFA", "QAFB"],
    "extra_rpc_fields": {
        "rawMode": "{isKeepConfig == '1' ? 'false' : 'true'}"
    },
    "built_in": true
}
```

### 4.2 5G 基站 IMG 升级（GNB_IMG_UPGRADE）

```json
{
    "type_code":          "GNB_IMG_UPGRADE",
    "category":           "gnb_upgrade",
    "category_label":     "5G升级",
    "display_name":       "5G 基站软件升级",
    "rpc_type":           "DOWNLOAD",
    "tr069_file_type":    "1",
    "file_type_label":    "Firmware Upgrade Image",
    "file_type_editable": true,
    "command_key_prefix": "Download Upgrade,",
    "url_template":       "firmware/{minio_path}",
    "transport_path":     "/smallcell/FileDownloadService/firmware/img/{path}",
    "file_name_template": "{firmware_name}",
    "target_file_name_template": "{firmware_name}",
    "file_size_field":    "firmware.fileSize",
    "checksum_field":     "firmware.md5",
    "step_chain":         ["CHECK_PERMISSION","CHECK_ONLINE","CHECK_CONFLICT","SEND_RPC","WAIT_RPC_RESPONSE","WAIT_FILE_TRANSFER","WAIT_TRANSFER_COMPLETE","WAIT_INFORM_EVENT"],
    "timeout_config": {
        "CHECK_ONLINE":           3600000,
        "WAIT_RPC_RESPONSE":      60000,
        "WAIT_FILE_TRANSFER":     600000,
        "WAIT_TRANSFER_COMPLETE": 1200000,
        "WAIT_INFORM_EVENT":      600000
    },
    "post_tc_event": {
        "enabled":            true,
        "eventCode":          "102 UPGRADE FINISH",
        "redisKeyTemplate":   "{cellCode}_upgrade_finish",
        "resultField":        "upgradeStatus",
        "successValues":      ["1"],
        "failValues":         ["2","3"],
        "timeoutMs":          600000,
        "preClean": {
            "deleteRedisKey": "{cellCode}_upgrade_finish"
        }
    },
    "tc_timeout_fallback": "VERSION_COMPARE",
    "permission_code":    "CODE_GNB_UPGRADE_IMAGE",
    "platform_scope":     ["5G gNB", "BBU-XSS", "BBU-QSS"],
    "extra_rpc_fields": {
        "rawMode": "{isKeepConfig == '1' ? 'false' : 'true'}"
    },
    "built_in": true
}
```

### 4.3 设备运行日志立即收集（RUNTIME_LOG_COLLECT）

```json
{
    "type_code":          "RUNTIME_LOG_COLLECT",
    "category":           "station_log",
    "category_label":     "基站日志",
    "display_name":       "运行日志采集",
    "rpc_type":           "UPLOAD",
    "tr069_file_type":    "6",
    "file_type_label":    "运行日志",
    "file_type_editable": true,
    "command_key_prefix": "Collect LOG,",
    "transport_path":     "/smallcell/FileUploadService?fileType={fileType}&filename={targetFileName}",
    "target_file_name_template": "runtime-{task_id8}-{sn}.tar.gz",
    "step_chain":         ["CHECK_PERMISSION","CHECK_ONLINE","PRE_VALIDATE","SEND_RPC","WAIT_RPC_RESPONSE","WAIT_TRANSFER_COMPLETE"],
    "timeout_config": {
        "WAIT_RPC_RESPONSE":      60000,
        "WAIT_TRANSFER_COMPLETE": 300000
    },
    "post_tc_event":      { "enabled": false },
    "tc_timeout_fallback": "FAIL",
    "permission_code":    "CODE_RUNTIME_LOG_COLLECT",
    "platform_scope":     ["4G eNB", "5G gNB", "DXDF"],
    "platform_overrides": {
        "DXDF": {
            "tr069_file_type": "6"
        },
        "BBU_QSS": {
            "tr069_file_type": "6"
        }
    },
    "pre_validate_rules": [
        {
            "ruleId": "DISK_USAGE_CHECK",
            "params": { "redisField": "disk_usage:run_log", "threshold": 90 }
        },
        {
            "ruleId": "FILE_COUNT_LIMIT",
            "params": { "configKey": "CollectMaxLogFileNum", "defaultLimit": 5 }
        }
    ],
    "built_in": true
}
```

> 首批内置类型建议至少覆盖：`ENB_IMG_UPGRADE`、`ENB_PATCH_UPGRADE`、`GNB_IMG_UPGRADE`、`GNB_FPGA_UPGRADE`、`VERSION_ROLLBACK`、`RUNTIME_LOG_COLLECT`、`FAULT_LOG_COLLECT`、`CONFIG_BACKUP`、`CONFIG_RESTORE`。

---

## 五、后端统一引擎架构

### 5.1 模块分层

```
┌─────────────────────────────────────────────────────────────────┐
│                   REST API 层（统一）                             │
│  GET  /ufte/overview                 → 概览统计                  │
│  GET  /ufte/task-types               → 查询任务类型元数据         │
│  POST /ufte/task-types               → 新增任务类型（管理员）     │
│  PUT  /ufte/task-types/{typeCode}    → 编辑任务类型（管理员）     │
│  POST /ufte/tasks                    → 创建任务                  │
│  GET  /ufte/tasks                    → 查询任务列表              │
│  GET  /ufte/devices                  → 查询设备执行列表          │
│  POST /ufte/tasks/{taskId}/terminate → 终止任务（预留）          │
└──────────────────────────┬──────────────────────────────────────┘
                           │ Gin Handler / Service / Repository
┌──────────────────────────▼──────────────────────────────────────┐
│              TaskOrchestrator（任务编排层）                       │
│  - 加载 file_transfer_task_type 配置                            │
│  - 前置校验规则执行（PreValidateRuleExecutor）                   │
│  - 写入 file_transfer_task / file_transfer_sub_task             │
│  - 调用 internal/task 入队 / 唤醒设备                            │
└──────────────────────────┬──────────────────────────────────────┘
                           │ Redis / DB / 事件总线
┌──────────────────────────▼──────────────────────────────────────┐
│         UnifiedFileTransferEngine（统一执行引擎）                 │
│  核心状态机，按步骤链配置执行：                                   │
│  - StepExecutor 接口，每个 Step 有独立实现类                     │
│  - 超时管理（按 timeout_config 各步骤独立超时）                  │
│  - 平台差异注入（运行时从 platform_overrides 取覆盖值）           │
│  - 步骤上下文（StepContext）在步骤间传递状态                     │
└──────────────────────────┬──────────────────────────────────────┘
                           │
        ┌──────────────────┼────────────────────┐
        ▼                  ▼                    ▼
 internal/task      internal/transfer      事件监听器
 (任务派发/唤醒)      (接收设备上传文件)      (102 UPGRADE FINISH 等)
```

### 5.2 核心接口定义

```go
type StepExecutor interface {
    StepID() string
    Execute(ctx context.Context, stepCtx *StepContext) (StepResult, error)
}

type StepContext struct {
    TypeDef            FileTransferTaskTypeDef
    Task               FileTransferTask
    SubTask            FileTransferSubTask
    EffectivePlatform  string
    ResolvedParams     map[string]string
    StepData           map[string]any
}

type StepResult string

const (
    StepContinue StepResult = "CONTINUE"
    StepWait     StepResult = "WAIT"
    StepSuccess  StepResult = "SUCCESS"
    StepFailure  StepResult = "FAILURE"
)
```

### 5.3 WAIT_INFORM_EVENT 步骤实现逻辑

```go
func (e *WaitInformEventStepExecutor) Execute(ctx context.Context, stepCtx *StepContext) (StepResult, error) {
    eventCfg := stepCtx.TypeDef.PostTcEvent
    if !eventCfg.Enabled {
        return StepSuccess, nil
    }

    if stepCtx.StepData["eventSubscribed"] == nil {
        redisKey := resolveTemplate(eventCfg.RedisKeyTemplate, stepCtx)
        if err := e.subscribeEvent(ctx, redisKey, stepCtx); err != nil {
            return StepFailure, err
        }
        stepCtx.StepData["eventSubscribed"] = true
        stepCtx.StepData["eventWaitStart"] = time.Now().UnixMilli()
        return StepWait, nil
    }

    waitStart, _ := stepCtx.StepData["eventWaitStart"].(int64)
    if time.Now().UnixMilli()-waitStart > eventCfg.TimeoutMs {
        return StepFailure, nil
    }

    redisKey := resolveTemplate(eventCfg.RedisKeyTemplate, stepCtx)
    result, err := e.redisClient.HGetAll(ctx, redisKey).Result()
    if err != nil {
        return StepFailure, err
    }
    if len(result) == 0 {
        return StepWait, nil
    }

    resultValue := result[eventCfg.ResultField]
    if contains(eventCfg.SuccessValues, resultValue) {
        return StepSuccess, nil
    }
    if contains(eventCfg.FailValues, resultValue) {
        return StepFailure, nil
    }
    return StepWait, nil
}
```

### 5.4 引擎主循环（简化伪代码）

```go
func (e *UnifiedFileTransferEngine) ProcessSubTask(ctx context.Context, subTask *FileTransferSubTask) error {
    typeDef, err := e.typeRepo.Get(ctx, subTask.TypeCode)
    if err != nil {
        return err
    }

    stepChain := typeDef.StepChain
    currentIdx := getStepIndex(stepChain, subTask.CurrentStep)
    stepCtx, err := e.buildContext(ctx, typeDef, subTask)
    if err != nil {
        return err
    }

    for index := currentIdx; index < len(stepChain); index++ {
        stepID := stepChain[index]
        executor := e.stepExecutors[stepID]
        subTask.CurrentStep = stepID

        result, execErr := executor.Execute(ctx, stepCtx)
        if execErr != nil {
            return e.markSubTaskFailed(ctx, subTask, execErr)
        }

        switch result {
        case StepWait:
            return e.subTaskRepo.Save(ctx, subTask)
        case StepFailure:
            return e.markSubTaskFailed(ctx, subTask, nil)
        case StepSuccess:
            return e.markSubTaskSuccess(ctx, subTask)
        case StepContinue:
            continue
        }
    }

    return e.markSubTaskSuccess(ctx, subTask)
}
```

---

## 六、前端统一 UI 设计

### 6.1 任务类型元数据 API 返回结构

```json
{
    "typeCode": "ENB_IMG_UPGRADE",
    "category": "enb_upgrade",
    "categoryLabel": "4G升级",
    "displayName": "4G 基站软件升级",
    "description": "面向 4G 基站 IMG 主包升级",
    "rpcType": "DOWNLOAD",
    "builtIn": true,
    "enabled": true,
    "stepChain": ["CHECK_PERMISSION", "CHECK_ONLINE", "CHECK_CONFLICT", "SEND_RPC", "WAIT_RPC_RESPONSE", "WAIT_FILE_TRANSFER", "WAIT_TRANSFER_COMPLETE"],
    "postTcEventCode": null,
    "permissionCode": "CODE_ENB_UPGRADE_IMAGE",
    "platformScope": ["4G eNB", "QAFA", "QAFB"],
    "fileType": "1",
    "fileTypeLabel": "Firmware Upgrade Image",
    "fileTypeEditable": true,
    "urlTemplate": "firmware/{minio_path}",
    "transportPath": "/smallcell/FileDownloadService/firmware/img/{path}",
    "targetFileNameTemplate": "{firmware_name}",
    "fileNameTemplate": "{firmware_name}",
    "fileSizeField": "firmware.fileSize",
    "checksumField": "firmware.md5",
    "rawMode": "false",
    "delaySeconds": 0,
    "lastEditor": "system-seed",
    "taskCount30d": 128,
    "successRate30d": 96,
    "updatedAt": "2026-05-13T10:00:00Z"
}
```

### 6.2 文件传输中心页（业务发起页）

```
前端路由：/transfer/center

┌──────────────────────────────────────────────────────────────────────┐
│  业务 Tab（4G升级 / 5G升级 / 回退 / 日志 / 备份 / 恢复 / 自定义）   │
├──────────────────────────────────────────────────────────────────────┤
│  执行视图 Tab：任务列表 / 设备列表                                  │
│   - 任务列表：按任务名/模板/状态过滤，支持分页                       │
│   - 设备列表：按设备名/SN/模板/状态过滤，支持分页                    │
├──────────────────────────────────────────────────────────────────────┤
│  新建任务弹窗                                                        │
│   - 选择任务类型                                                     │
│   - 升级/回退类任务额外选择“升级文件”                                │
│   - 升级文件来源于“软件管理 / 升级文件”统一列表                      │
└──────────────────────────────────────────────────────────────────────┘
```

- 业务 Tab **不是固定菜单配置**，而是由 `GET /ufte/task-types` 返回的 `category/categoryLabel` 聚合生成。
- 业务 Tab 只显示名称，不再在标签后附带模板数量。
- 页面不承载任何模板展示、模板详情和模板参数查看；这些能力全部收敛到 `/transfer/template-management`。
- 业务页只负责三件事：切换业务 Tab、查看任务/设备执行、打开新建任务弹窗。
- 任务列表与设备列表共用同一路由，不再为每个功能单独创建页面。
- 升级/回退类业务的执行列表列顺序需尽量与现有升级模块保持一致，降低运维认知切换成本。
- 升级/回退类任务在新建弹窗里必须选择升级文件，文件来源统一复用“软件管理 / 升级文件”已有资产，而不是在模板配置页重复维护文件信息。

### 6.3 传输模板管理页（管理员治理页）

```
前端路由：/transfer/template-management

┌──────────────────────────────────────────────────────────────────────┐
│  业务 Tab（仅名称）                                                 │
├──────────────────────────────────────────────────────────────────────┤
│  内置模板区                                                         │
│   - 查看详情（抽屉）  - 调整模板                                     │
├──────────────────────────────────────────────────────────────────────┤
│  自定义模板区                                                       │
│   - 查看详情（抽屉）  - 编辑模板  - 新增自定义模板                   │
└──────────────────────────────────────────────────────────────────────┘
```

- “传输模板管理”作为独立菜单项挂在“文件传输”一级目录下。
- 该菜单默认**不绑定任何角色**，由管理员在角色菜单配置中勾选后显示。
- 页面本身建议再加一层 admin 角色访问控制，避免非管理员通过直输 URL 进入。

### 6.4 模板创建/编辑对话框

```
前端组件：<TaskTypeDrawer />

┌──────────────────────────────────────────────────────────────┐
│  所属业务 Tab：[选择已有 Tab] [或输入新 Tab 名称]            │
│  显示名称 / 描述 / RPC 类型 / 步骤链                         │
│  FileType / FileType 说明 / URL 模板 / 目标文件名模板         │
│  文件名模板 / fileSizeField / checksumField / rawMode         │
│  transportPath / DelaySeconds / postTcEventCode              │
│  启用状态 / FileType 是否允许页面直接调整                     │
└──────────────────────────────────────────────────────────────┘
```

- 新增模板时可以复用已有业务 Tab，也可以直接输入新的 Tab 名称。
- 内置模板允许“查看 + 受控编辑”，自定义模板允许完整编辑。

### 6.4 新建任务对话框

```json
{
    "taskName": "华东试点-统一入口演示",
    "typeCode": "ENB_IMG_UPGRADE",
    "firmwareId": "firmware-uuid",
    "deviceCount": 20,
    "executionMode": "immediate",
    "note": "灰度范围与领导评审备注"
}
```

- 首期任务创建表单保持精简，只绑定任务类型、文件选择、数量和执行方式；复杂字段仍从模板侧继承。
- `firmwareId` 仅在升级/回退类任务显示并必填，直接引用软件管理中已上传的升级文件，避免 UFTE 重复建设文件资产管理。
- 未来如需完全元数据化输入，可在 `extra_rpc_fields` 基础上逐步补充表单 schema，但不应替代当前已经确认的中心页交互模型。

---

## 七、扩展示例：新增"FPGA 升级带自定义 EventCode"

**场景**：假设新增一种 FPGA 升级，TR-069 FileType 为 `1`，但文件标签显示为 `FPGA Package`，且升级完成需要等待设备上报 `103 FPGA FINISH` 事件（`upgradeStatus` 字段判断成功失败）。

**只需向 `file_transfer_task_type` 表插入一行，无需修改任何后端代码**：

```sql
INSERT INTO file_transfer_task_type (
    type_code, category, category_label, display_name, rpc_type,
    tr069_file_type, file_type_label, file_type_editable,
    command_key_prefix, url_template, transport_path,
    target_file_name_template, file_name_template,
    file_size_field, checksum_field,
    step_chain, timeout_config, post_tc_event,
    tc_timeout_fallback, permission_code, platform_scope,
    built_in, enabled, created_at, updated_at
) VALUES (
    'GNB_FPGA_UPGRADE',
    'gnb_upgrade',
    '5G升级',
    '5G 基站 FPGA 升级',
    'DOWNLOAD',
    '1',
    'FPGA Package',
    TRUE,
    'Download Upgrade,',
    'firmware/{fpga_path}',
    '/smallcell/FileDownloadService/firmware/fpga/{path}',
    '{fpga_name}',
    '{fpga_name}',
    'firmware.fileSize',
    'firmware.md5',
    '["CHECK_PERMISSION","CHECK_ONLINE","CHECK_CONFLICT","SEND_RPC","WAIT_RPC_RESPONSE","WAIT_FILE_TRANSFER","WAIT_TRANSFER_COMPLETE","WAIT_INFORM_EVENT"]'::jsonb,
    '{"CHECK_ONLINE":3600000,"WAIT_RPC_RESPONSE":60000,"WAIT_FILE_TRANSFER":600000,"WAIT_TRANSFER_COMPLETE":1200000,"WAIT_INFORM_EVENT":600000}'::jsonb,
    '{
        "enabled": true,
        "eventCode": "103 FPGA FINISH",
        "redisKeyTemplate": "{cellCode}_fpga_finish",
        "resultField": "upgradeStatus",
        "successValues": ["1"],
        "failValues": ["2","3"],
        "timeoutMs": 600000,
        "preClean": { "deleteRedisKey": "{cellCode}_fpga_finish" }
    }'::jsonb,
    'VERSION_COMPARE',
    'CODE_GNB_UPGRADE_FPGA',
    '["5G gNB", "FPGA"]'::jsonb,
    TRUE,
    TRUE,
    NOW(),
    NOW()
);
```

**新功能上线所需工作量**：
- ~~后端状态机代码（800~1500 行）~~ → ❌ 不需要
- ~~独立任务表~~ → ❌ 不需要
- ~~前端独立页面~~ → ❌ 不需要（统一使用 `/transfer/center`，由业务 Tab 与模板元数据聚合展示）
- **只需**：一条 SQL INSERT + TR069 进程监听新 EventCode（如已有机制则连这个也不需要）

---

## 八、落地实施路线图

### Phase 1：引擎骨架（2~3 周）

1. 设计并创建 `file_transfer_task_type`、`file_transfer_task`、`file_transfer_sub_task` 表
2. 实现 `StepExecutor` 接口 + 9 个原子步骤实现类
3. 实现 `UnifiedFileTransferEngine` 主循环
4. 实现 `WAIT_INFORM_EVENT` 步骤（支持配置化 EventCode）
5. 插入首批内置配置（至少 9 个：4G/5G 升级、Patch、FPGA、回退、运行日志、故障日志、配置备份、配置恢复）

### Phase 2：旧代码迁移（3~4 周）

6. 将 4G/5G 升级状态机迁移到新引擎（旧 Controller 保留转发兼容）
7. 将日志立即收集迁移到新引擎
8. 新旧并行运行，验证功能等价
9. 逐步下线旧状态机代码

### Phase 3：前端泛化（2~3 周）

10. 实现 `/transfer/center` 统一入口页，按 `category/category_label` 动态生成业务 Tab
11. 实现任务列表 / 设备列表双视图，以及模板详情 Drawer
12. 后端实现 `/ufte/overview`、`/ufte/task-types`、`/ufte/tasks`、`/ufte/devices` API
13. 旧页面通过“转发到统一中心”或“内部复用任务模型”逐步收敛

### Phase 4：开放配置管理（1~2 周）

14. 实现任务类型管理页面（admin 权限）
15. 支持在界面上新增/编辑任务类型配置
16. 配置校验（步骤链合法性、JSON Schema 校验）

---

## 九、风险与权衡

| 风险点 | 说明 | 应对方案 |
|--------|------|---------|
| 平台差异分支过多 | 不同平台 URL/FileType/行为差异可能超出配置能力 | `platform_overrides` 覆盖大多数差异；极端情况可注册平台专属 `StepExecutor` |
| 步骤间上下文依赖复杂 | 某些步骤需要前序步骤的特定数据（如 commandKey）| `StepContext.stepData` Map 规范化 key 约定；文档说明各步骤写入/读取哪些 key |
| 旧功能迁移风险 | 迁移过程中新旧并存，可能引入 Bug | 新旧引擎双写，按运营商灰度切换；充分回归测试 |
| DB 表结构变化 | 现有任务历史数据存在旧表 | 旧任务保留旧表只读展示；新任务走新表；提供历史数据归档脚本 |
| 配置错误导致任务异常 | JSON 配置出错可能导致引擎行为异常 | 配置入库前 JSON Schema 校验；步骤链合法性验证；引擎保护性兜底（未知步骤直接 FAILURE）|

---

## 十、与现有架构的兼容策略

```
当前：
  升级功能 → cellHandler 服务 → 独立状态机
  日志功能 → cellHandler 服务 → 独立状态机

目标态：
  所有功能 → Web 层 → TaskOrchestrator → Redis 队列 → UnifiedFileTransferEngine
                                                           └─ 读 file_transfer_task_type 配置
                                                           └─ 执行步骤链

迁移期（新旧并存）：
  新任务 → 新引擎（file_transfer_task 表）
  旧任务展示 → 旧表只读查询
  API 层：/ufte/* 为新接口，旧接口保留向下兼容
```

> **核心原则**：新引擎不是重写，而是把现有的状态机逻辑"配置化"。每个 `StepExecutor` 实现类本质上就是从原有代码中抽取出来的一段逻辑，只是现在由配置决定何时调用它、调用哪个 FileType、等待哪个 EventCode。
