1、新增公有命令 & 新增私有命令 ✅
操作类型：LST、ADD、MOD、RMV
参数路径：字符串
指令名称：字符串
可以选择保存和立即执行两个操作

2、api/v1/mml/commands?page=1&page_size=999 调整后端最大限制为 999 ✅

3、MML 控制台> 命令树 > "分类"的下拉列表，初次进入默认第一个分类，并加载第一个分类下的命令列表 ✅

4、自定义模块作为一个"分类"，并且有两个子类：私有命令和公有命令是固定的，私有命令下的分类遵守之前的需求，公有命令不在有下级分类，所有管理员加的命令都在同一层级 ✅

5、命令树 > 搜索框，支持命令名称和命令内容的搜索 ✅
