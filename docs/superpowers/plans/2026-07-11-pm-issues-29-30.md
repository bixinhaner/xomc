# PM Issues #29/#30 Implementation Plan

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** Make custom PM query ranges obey `[start,end)` and prove weekly aggregation produces queryable data from seven complete business days.

**Architecture:** Keep query range semantics in the shared backend aggregator so every API consumer receives identical boundaries. Preserve the existing daily→weekly deterministic chain and add integration/runtime evidence covering its real data path rather than adding another scheduler.

**Tech Stack:** Go, Squirrel, pgx, TimescaleDB, async_jobs, Docker Compose, Vitest/TypeScript verification.

## Global Constraints

- Every fix must be reproduced by a failing test before production code changes.
- Runtime acceptance must use the Compose Worker, real TimescaleDB, authenticated API, and cleaned isolated fixtures.
- Do not commit `.codex/` or unrelated local changes.

---

### Task 1: Custom range end boundary (#29)

**Files:**
- Modify: `omcgo/internal/pm/aggregator/query.go`
- Test: `omcgo/internal/pm/aggregator/query_test.go`

**Interfaces:**
- Consumes: `applyScalarFilters(sq.SelectBuilder, QueryRequest)`
- Produces: SQL predicates `time >= start` and `time < end`

- [ ] Add a query-builder regression asserting the end predicate is strict `<` and bound to `EndTime`.
- [ ] Run the focused test and confirm it fails because current SQL uses `<=`.
- [ ] Replace the end predicate with `sq.Lt` while retaining the inclusive start predicate.
- [ ] Re-run the focused aggregator tests and confirm they pass.

### Task 2: Weekly rollup data production (#30)

**Files:**
- Modify: `omcgo/internal/pm/aggregator/integration_test.go`

**Interfaces:**
- Consumes: `AggregateCounters(ctx, "pm_metrics_daily", "pm_metrics_weekly", WindowSpec)` and existing daily→weekly chain
- Produces: one weekly Sum row and one weekly Avg row with Monday-to-Monday Asia/Shanghai boundaries

- [ ] Add an integration fixture containing seven complete daily buckets with known Sum/Avg values.
- [ ] Run the integration test against real TimescaleDB and confirm weekly values and boundaries.
- [ ] Rebuild Worker, enqueue a real boundary daily job that chains `pm_aggregate_weekly`, and poll both jobs to terminal success.
- [ ] Query the weekly rows through authenticated `/api/v1/pm/metrics/aggregated` and verify expected values and timestamps.

### Task 3: Verification and integration

**Files:**
- Verify all modified files above.

**Interfaces:**
- Consumes: Tasks 1–2
- Produces: merge-ready commit and MR closing #29/#30

- [ ] Clean all runtime fixtures, jobs, and test watermarks.
- [ ] Run `cd omcgo && go build ./... && go test ./...`.
- [ ] Run `cd omcmb && npm run typecheck` because the affected workflow is frontend-visible.
- [ ] Commit with Conventional Commits, push, create an MR containing real evidence and `Closes #29` / `Closes #30`.
- [ ] Merge only when GitLab reports mergeable, then verify both issues are closed and remote `main` contains the merge commit.
