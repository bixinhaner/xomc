# 105820 电子围栏 104 验收执行日志

> 测试关联 ID：`105820-UAT-20260806-0817`
> 环境：`http://172.17.3.104:8081/`
> 角色：`System Admin`
> 当前测试围栏：`105820-UAT-20260806-0817-EDIT`
> 安全边界：围栏策略固定为“仅通知”；真机 IPSec/RF 写入未授权、未执行。

## P0 前置门禁

| 步骤 | 预期 | 实际 | 判定 | 证据 |
| --- | --- | --- | --- | --- |
| 设备列表加载 | `GET /api/v1/devices` 成功，页面不再 500 | 页面成功显示 2 台设备 | 通过 | 浏览器实时 DOM |
| 电子围栏设置加载 | 设置接口成功，页面不再显示加载失败 | 页签成功显示系统总开关 | 通过 | 浏览器实时页面与接口响应 |
| 初始系统模式 | 默认 `off` | 显示“已关闭，不执行围栏位置判定” | 通过 | 浏览器实时页面与接口响应 |
| GIS 入口 | 系统 `off` 时隐藏 | GIS 未显示电子围栏按钮 | 通过 | 浏览器实时页面与接口响应 |
| 数据库结构/迁移退出码 | 六张表、位置来源列、默认数据存在；迁移容器 exit 0 | 当前执行环境无法 SSH，数据库端口不可达 | 待补证据，不阻断本轮功能验收 | 待 104 终端输出 |

## GF-01 默认关闭与入口隐藏

1. 使用现有超级管理员会话打开系统配置。
2. 进入“电子围栏”页签，开关为未选中，服务端文案为“已关闭，不执行围栏位置判定”。
3. 打开 GIS，电子围栏入口不存在。

判定：**通过（数据库查询证据待补）**。

## GF-02 系统总开关与持久化

1. 点击系统总开关后先出现影响预览。
2. 预览值：已启用围栏 `0`、活动绑定 `0`、新增观察设备 `0`。
3. 确认后显示“电子围栏总开关已更新”和“已开启，按运营商开关生效”。
4. 刷新页面并重新进入电子围栏页签，开关仍为开启。
5. 打开 GIS，电子围栏入口出现。

判定：**通过（服务重启后的持久化仍待后续步骤验证）**。
证据：浏览器实时页面与接口响应。

## GF-03 CMCC 运营商开关

1. 当前运营商为中国移动，初始开关关闭，新增围栏按钮禁用。
2. 点击开关后出现影响预览，三个影响数均为 `0`。
3. 确认后开关保持开启，新增围栏按钮可用。
4. 未创建围栏前没有设备判定或控制影响。

判定：**当前运营商开启流程通过；跨运营商切换与失败回滚待补**。
证据：浏览器实时页面与接口响应。

## GF-04 创建并发布多边形围栏

1. 点击新增后列表隐藏，地图进入绘制模式。
2. 在远离当前设备的地图区域绘制四点多边形。
3. 填写名称 `105820-UAT-20260806-0817`，运营商为中国移动，策略为“仅通知”，连续越界/回区次数均为 `2`。
4. 保存后页面提示“围栏版本已发布”，列表数量由 `0` 变为 `1`，状态为“已启用”。

判定：**通过**。
证据：浏览器实时页面与接口响应。

## GF-05 名称边界

| 边界 | 实际结果 | 判定 |
| --- | --- | --- |
| 空名称 | 表单未关闭、未创建围栏 | 通过 |
| 129 字符 | 服务端返回页面级 `internal error`；保存按钮持续 loading，必须关闭面板恢复 | **失败** |
| 128 字符 | 待执行 | 待测 |
| 同运营商重名 | 待执行 | 待测 |

问题：不符合“超长名称以明确业务提示拒绝”的要求；虽然没有泄漏 SQL，但 `internal error` 不可接受，且 loading 状态未复位。
证据：浏览器实时页面与接口响应。

## GF-06 搜索与定位

1. 输入不存在关键字并按 Enter，返回 `0 个围栏` 和空态。
2. 输入部分名称 `105820-UAT` 并按 Enter，准确返回临时围栏。
3. 点击定位后列表切换为“返回围栏列表”，地图定位到围栏。
4. 点击返回后恢复原搜索条件和列表，不修改服务端状态。

判定：**通过**。
证据：浏览器实时页面与接口响应。

### GF-06 补充整改：GIS 工具栏选中态

104 验收发现“电子围栏”按钮被鼠标选中后，业务激活边框与全局 `button:focus` 的浏览器默认 4px 轮廓叠加，呈现明显的深色双层描边，与同一工具栏按钮风格不一致。

整改要求：选中态改为浅蓝背景、单层品牌蓝边框和轻阴影；鼠标点击后不残留深色焦点框；键盘 `Tab` 聚焦仍保留轻量蓝色可访问性轮廓。

整改状态：代码已完成，围栏集成测试 `3/3` 通过，当前分支 Web Docker 生产构建通过。本机测试容器已换用新镜像。最初误用 `3104`（该端口映射 ACS/TR-069 的容器 `8080`）导致 `502 Bad Gateway`；正确的管理页面入口为 `http://localhost:3105/`（映射 Web/REST 的容器 `8081`），已确认返回 `200 OK`，浏览器可进入登录页。由于本地站点与 104 不共享登录会话，需随 MR 重新部署 104 后，在已登录的真实 GIS 页面完成最终视觉签署。

## GF-07 编辑名称

1. 打开编辑面板，将名称改为 `105820-UAT-20260806-0817-EDIT`。
2. 保存后列表回查到新名称。
3. 页面 toast 为“围栏版本已发布”。

4. 操作日志回查确认同一时刻生成 `config_geofence_modify` 和 `config_geofence_publish`，详细记录显示 `version: 2`，因此改名确实创建了新几何版本。
5. 点击“重新绘制区域”后，编辑面板未进入可识别的绘制态，旧区域仍显示“保护区域已准备”；地图交互未形成草稿版本。

判定：**失败**。名称回查成功，但“改名不创建几何版本”不满足；重绘入口也未完成草稿/发布闭环。
证据：浏览器实时页面、接口响应和操作日志。

## GF-08 启用、禁用与归档

1. 禁用影响预览返回：受影响绑定 `0`、受影响设备 `0`、活动批任务 `1`；原因为空时确认按钮禁用。
2. 填写原因后禁用成功，列表状态变为“已禁用”，地图定位按钮禁用；页面明确提示不会自动恢复或重新激活设备。
3. 重新启用时同样先显示影响预览并要求原因，确认后状态回到“已启用”。启用状态下页面不提供归档入口。
4. 再次禁用后执行归档预览，仍检测到活动批任务 `1`；填写原因确认后，服务端拒绝归档，围栏保持“已禁用”。
5. 拒绝原因原样显示为 `transition geofence definition: geofence has active batch jobs: resource already exists`。

判定：**状态机和活动任务归档门禁通过；用户提示 i18n 失败；最终归档被 GF-10 卡死任务阻断**。
证据：浏览器实时页面与接口响应。

## GF-09 区域候选预览

当前服务端预览返回：区域内 `0`、区域外 `1`、暂无位置 `1`；“绑定所选设备”按钮禁用，预览未改变绑定。

判定：**候选分类和无副作用预览通过；设备组/运营商权限隔离待多角色补测**。
证据：浏览器实时页面与接口响应。

## GF-10 批量 SN 预览与逐项结果

1. 混合输入有效 SN `1202000240194DP0005`、重复有效 SN、两个不存在 SN；分隔符覆盖空格、分号、逗号和换行。
2. 服务端预览结果为：可绑定 `1`、从其他围栏移入 `0`、已跳过 `2`，说明重复有效 SN 被去重，不存在 SN 未泄漏设备详情。
3. 操作原因为空时，“创建绑定任务”保持禁用；填写 `105820-UAT-20260806-0817` 后按钮启用。
4. 创建任务后，两个不存在 SN 均为“已跳过”，有效 SN 始终为“待处理”，汇总始终为“等待执行：已完成 2/3”。
5. 持续轮询约 38 秒仍未收敛；关闭弹窗、刷新 GIS、重新打开绑定列表后，服务端回查不到已绑定设备。

判定：**失败（阻断 GF-11/GF-12）**。批量解析、去重、跳过和原因必填通过，但有效项未被 worker 消费，作业与明细残留非终态，直接违反“作业总览与逐项结果终态一致、不残留待处理”的验收要求。当前仅有一个测试围栏，`move` 分类无法构造。
证据：浏览器实时页面、任务接口和数据库回查。

## GF-11～GF-12 单一归属与绑定管理

有效 SN 未产生 active 绑定，因此无法继续验证原子改绑、暂停、恢复、导出和移除。测试围栏当前也没有可管理的绑定记录。

判定：**阻断（根因前置为 GF-10 批量作业不进入终态）**。

## GF-13～GF-15 位置上报、来源隔离与 Observe

未执行。原因：

- 当前没有成功建立 active 绑定，GF-15 前置不成立；
- 未取得第三方位置接口的授权 JWT/API Key；
- 未获授权修改专用设备的 `location_source_mode`，且缺少数据库回查通道。

判定：**阻断**。未向现网设备写入位置，也未改变位置来源。

## GF-16～GF-17 真机控制与恢复

未执行。没有获得专用测试设备、原始 IPSec/RF 值与真实 SPV/GPV 控制授权；本轮围栏始终使用“仅通知”策略。

判定：**阻断（安全门禁生效）**。未产生任何设备控制结果，未改动 RF/IPSec。

## GF-18 审计日志

1. 操作日志页共回查到本轮 12 条 geofence 记录，覆盖系统/运营商设置、创建、发布、改名、绑定任务创建、启用、禁用与归档失败。
2. 记录包含 actor `admin`、来源 IP `172.19.1.152`、动作、结构化详情、成功/失败、原因和起止时间。
3. 归档门禁失败被记录为 `config_geofence_archive_failed`；绑定任务创建记录为成功，但不代表异步有效项已完成。
4. 页面直接显示 `config_geofence_*` 动作码和英文后端错误，用户可见文案未完整 i18n。

判定：**基础审计通过；i18n 失败；对象 ID/目标状态等完整字段仍需展开或数据库补证**。
证据：操作日志页面与接口响应。

## GF-19 权限、失败和重启恢复

当前仅有 `System Admin` 会话，且没有 104 的可用 SSH/容器执行通道，无法切换普通用户、空设备组用户、跨运营商用户，也无法安全重启 app/worker 验证续跑与幂等恢复。

判定：**阻断**。但本轮已观察到未重启情况下有效绑定项长期不被 worker 消费。

## 清理与现场恢复

| 检查项 | 验收前 | 验收后 | 结果 |
| --- | --- | --- | --- |
| 系统模式 | `off` | `off` | 已恢复 |
| CMCC 运营商模式 | `off` | `off` | 已恢复 |
| 临时围栏数量 | `0` | `1`（disabled） | **未恢复：活动批任务阻止归档** |
| 测试设备绑定 | 无 | 无 | 已恢复/未产生 |
| 位置来源模式 | 未改动 | 未改动 | 无变化 |
| IPSec/RF | 未采集、未授权 | 未改动 | 无变化 |

系统总开关关闭后，GIS 电子围栏入口再次隐藏。临时围栏 `105820-UAT-20260806-0817-EDIT` 保持 disabled，不能绕过服务端门禁强制归档；需要先修复或处置卡死批任务后完成最终清理。
证据：浏览器实时页面与接口响应。

## 当前验收结论

**不通过**。管理主路径可创建、查询、预览、启停和审计，但 GF-07 改名错误创建几何版本、重绘未完成，GF-10 有效绑定任务永久待处理并阻断 GF-11/GF-12 和现场完整清理；GF-13～GF-19 的权限、位置、重启及真机控制前置也未满足。生产/预发布环境应保持系统与运营商开关为 `off`。

## 重新部署后复验（2026-08-06 18:16～18:24 UTC+8）

> 复验关联 ID：`105820-UAT-20260806-1023`
> 复验范围：P0 页面/服务健康、GF-01～GF-03、GF-05、GF-07、GF-08 门禁、GF-10 新任务消费。
> 安全边界：仍未执行设备 IPSec/RF 写入。

| 项目 | 实际结果 | 判定 |
| --- | --- | --- |
| 设备列表 | 2 台设备正常加载，原 500 未复现 | 通过 |
| app 就绪探针 | `http://172.17.3.104:18081/readyz` 返回 200；Postgres、Timescale、Redis、NATS、MinIO、topology、alarm、device、PM 均 healthy | 通过 |
| 默认总开关/GIS 入口 | 初始模式为 `off`，GIS 不显示入口 | 通过 |
| 系统总开关 | 开启预览为围栏 0、绑定 0、新增观察设备 0；确认后保持开启，GIS 入口出现 | 通过；服务重启持久化待补 |
| CMCC 开关 | 开启预览正常，确认后新增围栏按钮可用 | 通过；跨运营商/失败回滚待补 |
| GIS 按钮选中态 | 新样式已随 104 Web 加载，点击态为浅蓝背景和单层品牌蓝边框 | 通过；键盘聚焦待补 |
| 129 字符名称 | 编辑表单即时显示“围栏名称不能超过 128 个字符”，未提交、未 loading 卡死 | 通过 |
| 128 字符名称 | 可保存，toast 为“电子围栏已保存”；随后已恢复原名称 | 通过 |
| 仅改名 | 保存后未出现“版本已发布”toast；列表名称正确回查 | 页面路径通过；版本号需数据库/API 证据 |
| 重绘入口 | 编辑弹窗关闭并进入“请在地图上绘制保护区域”状态；取消后恢复原编辑内容和原几何 | 入口与取消恢复通过；新草稿发布待补 |
| 归档门禁 | 遗留任务仍为活动任务 1，确认按钮禁用，中文提示“任务进入终态后才能归档” | 门禁/i18n 通过 |
| 改名审计 | 两次实际改名后刷新操作日志，总数仍为 16，未出现 18:21 左右的 `config_geofence_modify` 记录 | **失败** |
| 新批量绑定预览 | 重复有效 SN + 未知 SN 得到可绑定 1、跳过 1，重复 SN 被去重 | 通过 |
| 新批量绑定执行 | 新任务显示已完成 1/2；未知 SN 已跳过，有效 SN `12020007592481D0041` 6 秒后仍为待处理 | **失败** |
| worker 就绪探针 | `http://172.17.3.104:9092/readyz` 连接被拒绝；新任务创建后活动任务总数变为 2 | **阻断**：需确认 worker 容器和端口 |
| 复验现场恢复 | 测试围栏重新禁用，CMCC 和系统总开关均恢复 `off`；设备参数、位置来源未改动 | 通过；两个 pending 作业保留为故障证据 |

104 终端进一步确认 worker 启动失败原因为：

```text
tsdb.max_conns=40 is below concurrent PM budget=68
(ingest=4 finalize=32 aggregation=3*8 reserve=8)
```

这是 104 dev 配置的连接池容量门禁：`config.dev.yaml` 的 40 个 TSDB 连接只能覆盖
`finalize=4` 的预算，而 Compose 当前从环境注入了 `finalize=32`。104 验收环境采用本地 `.env`
覆盖 `PM_AGGREGATION_FINALIZE_CONCURRENCY=4`，不修改公共 Compose/配置文件；预算恢复为
`4 + 4 + 3*8 + 8 = 40`。重建 worker 不需要重新构建镜像或拉取基础镜像。

重新部署后当前判定仍为 **不通过/阻断**：前端名称边界、重绘入口、归档 i18n 和按钮选中态修复生效，
但 worker 没有消费历史或新建绑定任务，GF-10～GF-17 无法继续；另发现仅改名未产生可见业务审计记录。
在 worker `/readyz` 返回 200 且两个 pending 作业进入终态前，不执行位置判定和真机控制。

## worker 恢复后续验（2026-08-06 18:32～18:40 UTC+8）

> 复验关联 ID：`105820-UAT-20260806-1035`
> 环境修复：仅在 104 本机未跟踪的 `.env` 设置
> `PM_AGGREGATION_FINALIZE_CONCURRENCY=4`，未修改公共 Compose 或 YAML 配置。

| 项目 | 实际结果 | 判定 |
| --- | --- | --- |
| worker 启动 | 日志显示 PM、日志和基站日志留存 runner/cron 以及 PM 导出 worker 完成初始化；随后 `http://172.17.3.104:9092/readyz` 返回 200 | 通过；启动命令后首次 `curl` 曾遇到一次连接重置，后续探针稳定成功 |
| 历史 pending 作业恢复 | worker 启动后，先前两个 pending 批量绑定作业均进入终态；归档预览中的活动任务数从 2 降为 0 | 通过 |
| 新批量绑定 | 输入两台有效设备和 `UNKNOWN-SN-105820`；预览为可绑定 2、跳过 1；任务约 2.5 秒进入“执行成功，已完成 3/3”，有效设备均成功、未知 SN 跳过 | 通过 |
| active 绑定 | `1202000240194DP0005`、`12020007592481D0041` 均形成唯一 active 绑定 | 通过；当前没有构造跨围栏 `move` 场景 |
| Observe 防抖 | 首轮显示“正在确认越界（3 次/2 次）”但仍为未知；满足默认 60 秒最小持续时间并收到新位置后，两台设备均收敛为“已在围栏外”，位置版本分别推进到 601、2375 | 通过；连续次数和最小持续时间共同生效 |
| Observe 控制隔离 | 围栏策略为 `notify_only`，两台设备已确认 outside 后“设备控制结果”仍为 0 | 通过；未创建 RF/IPSec 控制动作 |
| 越界告警 | 当前告警总数仍为验收前 3 条，只包含设备既有告警；未出现 `GEOFENCE_LOCATION_OUTSIDE` | **失败**：状态边沿已确认，但告警投影未形成 |
| 暂停绑定 | 填写 `105820-UAT-20260806-1036 暂停绑定验收` 后暂停成功，审计包含 reason | 通过 |
| 恢复绑定 | 点击恢复后立即生效，页面没有原因输入步骤；审计 `config_geofence_binding_resume` 仅含 `target_status: active` | **失败**：不符合 GF-12“恢复并填写原因”及 GF-18 审计要求 |
| 操作审计 | 可见本轮 settings、enable、bind、suspend、resume 记录，包含 actor、来源 IP、结果和时间 | 部分通过；仅改名审计仍缺失，恢复审计缺少原因 |

当前结论更新为：worker 部署门禁和 GF-10 异步消费故障已解除，GF-12 的暂停路径及 GF-15
的连续位置判定可工作；但越界告警缺失、恢复绑定原因缺失、仅改名审计缺失仍使验收保持
**不通过**。真机 Enforce 前必须完成设备原始 IPSec/RF 参数只读采集并确认可恢复边界。

## 绑定管理、真机前置与现场恢复（2026-08-06 18:47～19:02 UTC+8）

### GF-12 绑定管理补测

| 项目 | 实际结果 | 判定 |
| --- | --- | --- |
| CSV 导出 | 点击“导出绑定设备”后页面提示“绑定设备已导出” | 下载触发通过；文件内容与经纬度排除规则仍需保留下载物复核 |
| 移除第二台设备 | `12020007592481D0041` 填写 `105820-UAT-20260806-1048 缩小真机控制验收范围` 后变为“已移除” | 通过；设备控制结果保持 0 |
| 移除主测试设备 | `1202000240194DP0005` 填写 `105820-UAT-20260806-1054 重置 outside 边沿后执行真机控制` 后变为“已移除” | 通过；设备控制结果保持 0 |
| 移除审计 | 两次均生成 `config_geofence_binding_remove`，审计包含 reason | 通过 |

### GF-16 真机控制前置

用户明确确认测试设备允许读写后，只选取 `1202000240194DP0005` 作为控制候选，未扩大到第二台设备。

| 前置项 | 实际结果 | 判定 |
| --- | --- | --- |
| 在线与话务 | 设备在线激活，UE 数为 0，最后在线时间持续更新 | 满足 |
| 产品与映射 | QRTB / `BLQ_5.1.11.9`；产品映射中 IPSec 与两路 RF 均为 READ_WRITE | 满足代码映射前置 |
| 最新 GPV 基线 | 手工参数同步任务 `manual:eebee49a-e051-46ea-93d6-4d4aad3baec4` 完成：517 条、跳过 23 条、43 次 GPV、约 10 秒 | 通过 |
| IPSec 原值 | 快速设置主开关为“关闭”；同步后可见两个 tunnel 实例均为 `TunnelEnable=true` | 已记录，主开关与 tunnel 状态语义需在控制计划中分别处理 |
| RF 原值 | 小区 1 和 2 均显示“激活 / 射频开” | 已记录 |
| 去激活策略入口 | 围栏编辑器“越界动作”实际只有“仅通知、人工复核”，没有 `deactivate/去激活` | **阻断**：无法从受支持 UI 配置 #105820 要求的真机策略 |

源码复核确认 `GeofenceEditorDrawer.tsx` 将选项硬编码为 `notify_only/manual_review`，虽然服务端模型、
控制 monitor 和 BLQ/MLN 产品映射支持 `deactivate`。因此本轮没有绕过 UI 修改数据库，也没有产生
SPV/GPV 控制动作；GF-16、GF-17 不能签署通过。

### GF-07/GF-11 几何与原子改绑阻断

为在不修改设备位置来源的条件下构造 `inside → outside → inside`，先后尝试：

1. 将原围栏重绘为包含设备的四边形；
2. 将原围栏重绘为包含设备的三角形；
3. 新建 `105820-UAT-20260806-INSIDE` 围栏并以不同终点双击结束绘制。

三次页面均显示“电子围栏操作失败”。操作日志回查到 `config_geofence_modify_failed` 和
`config_geofence_create_failed`，结构化详情的真实原因为
`polygon has adjacent duplicate vertices`；页面没有展示该稳定业务原因。说明地图绘图组件输出的
闭合环与服务端相邻重复顶点校验不兼容，直接阻断重绘发布、新围栏创建以及后续 GF-11 `move`
原子改绑。失败操作没有创建第二个可见围栏。

### 最终现场恢复

| 检查项 | 验收前 | 验收后 | 结果 |
| --- | --- | --- | --- |
| 系统模式 | `off` | `off` | 已恢复；GIS 电子围栏入口隐藏 |
| CMCC 模式 | `off` | `off` | 已恢复 |
| 临时围栏数量 | 0 | 0 | 原测试围栏先禁用再归档；影响预览绑定 0、设备 0、活动任务 0 |
| active 绑定 | 0 | 0 | 两台测试设备均已带原因移除 |
| 位置来源模式 | TR069 | TR069 | 未修改 |
| IPSec 主开关 | 关闭 | 关闭 | 未改变 |
| RF 小区 1/2 | 射频开 / 射频开 | 射频开 / 射频开 | 未改变 |
| 当前告警 | 3 条基线 | 3 条基线 | 未新增；同时再次证明越界告警投影缺失 |
| 设备控制结果 | 0 | 0 | 全程未执行真机 SPV |

## 本轮最终结论

**不通过，但现场已完整恢复。** worker 部署与批任务消费已修复，配置、创建首条围栏、查询、
启停、归档、混合 SN 批量绑定、暂停、移除以及连续位置防抖的主要路径可工作；但以下问题仍直接
阻断 #105820 完整验收：

1. confirmed outside 后没有 `GEOFENCE_LOCATION_OUTSIDE` 当前告警；
2. 恢复绑定无需原因，审计缺少 reason；
3. 仅改名缺少业务审计；
4. 重绘/新建闭合环被服务端以 adjacent duplicate vertices 拒绝，页面只显示通用错误；
5. 前端不提供 `deactivate` 策略，GF-16/GF-17 无法进入受支持的真机控制流程；
6. 源码复核发现 outside 状态下将动作级别升级为 `deactivate` 会发布
   `SubjectGeofenceDeviceEscalated`，但 `GeofenceControlMonitor.Subscribe` 当前没有订阅该主题；
   即使补出前端选项，也必须补齐升级边沿消费并做幂等控制测试；
7. 普通用户、空设备组、跨运营商、第三方位置接口和 worker/outbox 重启幂等仍缺专用凭据或角色证据。

生产或预发布应继续保持系统与运营商模式 `off`，完成上述修复并重新部署 104 后，再按 GF-07、
GF-11、GF-15、GF-16、GF-17、GF-18、GF-19 的顺序复验。

## 第三轮完整流程复验（关联 ID `105820-UAT-20260806-1234`）

| 范围 | 现场结果 | 判定 |
| --- | --- | --- |
| P0 / GF-01～GF-03 | 设备列表与设置接口正常；初始 off；系统与 CMCC 开关影响预览、保存和刷新持久化正常 | 通过 |
| GF-04～GF-07 | 双击闭合创建成功；空名称、129 拒绝、128 接受；搜索/定位/返回正常；重绘发布成功 | 主流程通过；编辑/重绘提示存在误导文案 |
| GF-08 | 禁用预览准确显示绑定 2、设备 2、任务 0；原因必填；取消归档未破坏测试现场；重新启用正常 | 通过 |
| GF-09～GF-12 | 候选内 2；混合两台有效设备、未知 SN 和重复 SN 后任务 3/3 终态；暂停原因正常；恢复无原因框 | GF-10 通过；GF-12 恢复审计失败 |
| GF-13～GF-14 | 主设备位置来源 `TR069 → 第三方平台 → TR069` 均保存成功 | UI 来源切换通过；批量第三方接口/幂等仍待凭据 |
| GF-15 | 重绘后两台设备从确认中收敛为 outside，`notify_only` 下控制动作保持 0；告警 ID 查询为 0 | 判定与 Observe 隔离通过；告警失败 |
| GF-16～GF-17 | 主设备原值再次确认：IPSec 主开关关闭，RF1/RF2 开启；页面无“去激活”选项 | 阻断，未执行 SPV |
| GF-18 | settings/create/publish/bind/suspend 等审计存在；resume 详情仅 `target_status=active` | 部分通过 |

### 现场版本一致性结论

当前 MR 第 4 个提交 `78f465859` 已包含“去激活选项、恢复原因、改名审计、告警防误清除”等修复及
回归测试，但 104 页面仍呈现对应旧行为。第三轮结果因此判定为“104 容器版本不一致/未完整重建后
启动”，不是对 `78f465859` 的有效复验。下轮部署需要同时更新 web、app、worker，并在启动后刷新
浏览器再验收。

### 本轮新增源码修复

1. 编辑说明改为“仅改名不创建版本；区域或策略变化才创建并发布版本”；
2. 重绘提示改为完成后返回当前编辑面板；
3. 字段校验错误不再叠加通用失败 toast。

验证：前端定向 `24/24`、电子围栏 Go、告警 Go、worker Go、Web 生产构建均通过；定向 ESLint
无 error。验收文档和本执行日志继续保持本地未跟踪，不纳入 MR。

### 第三轮当前现场

- 系统和 CMCC 模式：开启（测试继续中）；
- 测试围栏：`105820-UAT-20260806-1234-EDIT`，启用，策略 `notify_only`；
- 两台设备：active 绑定、confirmed outside、控制动作 0；
- 主设备位置来源：已恢复 TR069；
- 主设备 IPSec/RF：未改动，仍为关闭 / 开启 / 开启。

该现场仅用于下次部署后无缝续验；在最终签署前仍必须执行第 11 节完整清理。
