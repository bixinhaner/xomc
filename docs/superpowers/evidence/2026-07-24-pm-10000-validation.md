# PM 10,000-device deployment validation — 2026-07-25

## Status

**FINAL SNAPSHOT — PARTIAL PASS, NOT FULL TASK 10 ACCEPTANCE.**

Exact package/deployment/health, isolated regressions, Prometheus rule and
target checks, bounded warning search, the 2,500-device device-hourly
calibration, deadlock/lock-wait behavior, and OOM/restart gates passed. The
then-exact `2053` package exposed a P1 in the concurrent group hourly path:
the 02:00 group bucket failed on attempt 3 with SQLSTATE `22P02` because
`indicatorMetaSQL` did not include dynamically discovered
`pm_metric_dictionary` metadata for `Cqi.00`. Fix commit `e6894d924` is
deployed in exact package `2211`; the official online recalculation succeeded
on attempt 1 in 18m04.661439s with verified rows, `Cqi.00` output and watermark
advancement. The P1 is closed. The paced 30-minute load gate is explicitly not
claimed because external writes stopped at 04:33:01+08. The timed 10,000-file
burst, paced host evidence, guarded core plans, and legacy/sparse dual run were
not verified. Do not represent this document as all Task 10 thresholds
passing.

Target host: `172.24.224.197`
Branch: `codex/pm-sparse-storage-rollup`
Required implementation commit:
`e6894d924c64c85c8ea3c6f8b957bc79b97ba634`

Live validation first exposed release/PM defects. Their verified follow-up
commit is:
`cfbefdec5452a59abd738b23c5c38fdd92ea4744`
(`fix(pm): 修复压测发现的写入阻塞与元数据冲突`). Observation of that
clean release then exposed another P1: the formula-KPI hourly counter read did
not include its fixed bucket timestamp. The next accepted release must also
include that regression-tested fix. The fix commit is
`6915a7c47f90f58f0be0db0046c13e94547846e7`
(`fix(pm): 限定小时公式查询窗口`); local and remote branch now match this
commit, and deployed intermediate release `0.1.7-20260724-1833` embeds it.
That release exposed a further P1 because the bounded-hour query still loaded
every counter into Go. The final acceptance commit and package must also
include the dependency-path filter described below. That fix is committed and
pushed as `79a59763f1d4ffa39f7f454e668319eecee8a6cf`
(`fix(pm): 按公式依赖限定小时聚合读取`) and deployed as final release
`0.1.7-20260724-1904`. Its live gate failed: the dependency filter was active,
but the current transaction's formula path still re-read freshly inserted
hourly rows and its first formula statement exceeded eight minutes. The next
acceptance commit must read and aggregate the required counters directly from
the raw sparse tables. That fix is committed and pushed as
`def534142659914c28e5b2de6080b2a09ab9ff6f`
(`fix(pm): 避免事务内重读小时聚合`). A production-throughput follow-up
raises the consistent default batch size from 200 to 500 devices:
`c6d54c5f47d0778a2d0e13408c9919b147e1464d`
(`fix(pm): 提升小时聚合生产批次吞吐`). Local and remote branch match this
latest commit, deployed as final release `0.1.7-20260724-1954`.

Subsequent live calibration rejected both the committed 500-device default and
an experimental 1,000-device override for the hourly completion target. The
current calibration uses 2,500 devices/batch (four batches for 10,000
devices). The calibrated default and accurate post-formula batch completion
timestamp are committed and pushed as
`e2139d3972b8a9edbaa9d1f741cf7d478c07e8b3`
(`fix(pm): 校准万站小时聚合批次`), deployed as the then-exact release
`0.1.7-20260724-2053`.

That package exposed a group-hourly metadata gap. The follow-up implementation
commit is `e6894d924c64c85c8ea3c6f8b957bc79b97ba634`; it is pushed and deployed as
the current exact package `0.1.7-20260724-2211`.

The validation preserves the existing production data and load generator. It
does not purge JetStream, delete PM rows, remove indexes, reset volumes, or use
the bare-process restart scripts. Any replay uses a unique test identifier.
`EXPLAIN ANALYZE`, Task 1/2 PostgreSQL integration assertions, and legacy versus
sparse dual-run measurements run only in explicitly named isolated databases
or environments.

## Evidence clock and identities

| Item | Value |
|---|---|
| Validation window | 2026-07-24 16:56Z through exact `2211` deployment and successful official group-hourly recalculation at 2026-07-25 06:36:28+08 |
| Host clock / timezone | Host OS timezone `Etc/UTC`; NTP synchronized `yes`; database/application timestamps retain the explicit `+08` or `Z` offsets shown below |
| Pre-deployment application version | NOT CAPTURED |
| Deployed release version | Current exact release `0.1.7-20260724-2211`; supersedes P1-exposing `0.1.7-20260724-2053` |
| Deployed git commit | Package `VERSION` reports `git_commit=e6894d9`, matching `e6894d924c64c85c8ea3c6f8b957bc79b97ba634` |
| Release package checksum | SHA-256 `f4403837af06ce44fd768f02ac19645512318b2faba2220f8f8c2f0a571a9b65` |
| Release directory and Compose project | `/opt/omc/current -> /opt/omc/releases/0.1.7-20260724-2211`; Compose project `omcgo` |
| Workload identity / raw corpus manifest | NOT CAPTURED; therefore no exact 10,000-file burst claim is made |
| Isolated Task 1/2 database | `omcgo_task10_test`, production schema-only and no production data; removed after successful assertions |
| Isolated EXPLAIN clone | Must be exactly `omc_task8_sparse_clone` with the documented sentinel |
| Isolated legacy revision | `2732aa9a618fb6fdc9ce1e32ade85de08c33670b` (`origin/main`) |
| Isolated sparse revision | `11b24a3f369c04170e919f211f14e081fa6d858a` |

## 1. Pre-deployment baseline

All rows below must be captured before the release changes any server state.

### Host and containers

| Timestamp | Signal | Pre value | Notes |
|---|---|---|---|
| 2026-07-24 16:56–16:57Z | Load / CPU idle | Host was not CPU-saturated | Exact multi-sample CPU idle values were not captured |
| NOT CAPTURED | Memory / swap | NOT CAPTURED | Required pre-baseline gap |
| NOT CAPTURED | Filesystems | NOT CAPTURED | Required pre-baseline gap |
| NOT CAPTURED | Disk latency / IO pressure | NOT CAPTURED | Required pre-baseline gap |
| NOT CAPTURED | Container state / health / restarts / OOM | NOT CAPTURED | Final exact deployment state was captured separately |
| NOT CAPTURED | Container CPU / memory / block IO | NOT CAPTURED | Live calibration resource samples were captured separately |

### PostgreSQL and TimescaleDB

| Timestamp | Signal | Main PostgreSQL | TimescaleDB |
|---|---|---|---|
| NOT CAPTURED | Connections / active / idle-in-transaction | NOT CAPTURED | NOT CAPTURED |
| 2026-07-24 16:56–16:57Z | Lock waits / blockers | NOT CAPTURED | 29 waiters at baseline; blocker details not captured |
| 2026-07-24 16:56–16:57Z | Deadlocks / rollbacks | NOT CAPTURED | 5 cumulative deadlocks at baseline; rollback count not captured |
| NOT CAPTURED | Dead tuples / last autovacuum | NOT CAPTURED | NOT CAPTURED |
| 2026-07-24 16:56–16:57Z | Database / table / index / TOAST bytes | NOT CAPTURED | Metric dictionary approximately 440 MB; complete relation breakdown not captured |
| NOT CAPTURED | Long-running or blocked statements | NOT CAPTURED | NOT CAPTURED |

The metric dictionary had approximately 28.37 million cumulative tuple
updates at baseline. A later exact capture reported 2,043 rows and 30,511,364
cumulative updates before the dictionary-sync fix was exercised. These are
counter baselines, not post-fix deltas.

### Queue, async jobs, hourly state, logs

| Timestamp | Signal | Pre value |
|---|---|---|
| 2026-07-24 16:56–16:57Z | `PM` / `pm-workers` pending | 253,635 |
| 2026-07-24 16:56–16:57Z | Ack pending / configured max ack pending | 128 / configured limit not captured |
| NOT CAPTURED | Redelivered | NOT CAPTURED |
| 2026-07-24 16:56–16:57Z | Oldest relevant pending age | Approximately 6.5 hours |
| NOT CAPTURED | Stream last / delivered / ack stream and consumer sequences | NOT CAPTURED |
| NOT CAPTURED | Async jobs by type and state | NOT CAPTURED |
| NOT CAPTURED | Oldest pending async job | NOT CAPTURED |
| NOT CAPTURED | Failed hourly jobs, error and recovery metadata | NOT CAPTURED |
| NOT CAPTURED | Hourly versions by state / stale building rows | NOT CAPTURED |
| NOT CAPTURED | Active-version uniqueness | NOT CAPTURED at pre-baseline; final calibrated bucket was captured |
| NOT CAPTURED | Hourly completion watermark | NOT CAPTURED at pre-baseline; final calibrated watermark was captured |
| NOT CAPTURED | Recent app / ACS / worker / migration errors | NOT CAPTURED |
| NOT CAPTURED | Whether the 10,000-device workload is active | NOT CAPTURED |

## 2. Deployment record

| Timestamp | Action | Evidence / result |
|---|---|---|
| 2026-07-25 | Initial verified branch push | Initial branch point `11b24a3f3` was pushed before first deployment; later superseded by the live-fix commit below |
| NOT CAPTURED | Back up existing release configuration | No backup transcript/checksum was captured; persistent configuration was preserved |
| 2026-07-24 17:00Z | Build exact release artifact | Release `0.1.7-20260724-1700` built from `11b24a3f3`; artifact checksum pending transcription |
| 2026-07-24 17:11Z | Run repository release/Compose installer | Completed after diagnosing a cross-platform `stat` probe defect; see note below |
| Initial deployment | Main PostgreSQL migrations | Initial transcript not captured; final exact deployment completed migrations 5/3/1 |
| Initial deployment | TimescaleDB migrations | Initial transcript not captured; final exact deployment completed migrations 5/3/1 |
| 2026-07-24 17:11Z | Start complete app, infrastructure, web and monitoring profiles | Completed; release health check reported 25/25 |
| Then-exact `2053` deployment | Verify active release symlink / image tags / app version | PASS for `2053` / `e2139d3`; subsequently superseded by the P1 fix package `2211` |
| 2026-07-25 | Commit and push live fixes | `cfbefdec5452a59abd738b23c5c38fdd92ea4744`; local and remote branch point to the same commit |
| 2026-07-25 | Verify fix commit locally | `go build ./...`, `go test ./...`, frontend typecheck, monitoring profile test, storage Compose test (59/0), and `git diff --check` passed |
| 2026-07-25 | Build clean final artifact from fix commit | Release `0.1.7-20260724-1808`; `RELEASE.txt` embeds `git_commit=cfbefde`; package SHA-256 `1bdaaef4e57929a64c7a1f2882566a89f4a5d25cd62ac32f1708cd4a3198b1b0` |
| 2026-07-25 | Redeploy clean final artifact | Active release is `/opt/omc/releases/0.1.7-20260724-1808`; final deployment timestamp pending exact server-clock transcription |
| 2026-07-25 | Run final installer migrations | Main migrations 5, seed migrations 3 and TimescaleDB migration 1 all completed as successful no-ops |
| 2026-07-25 | Run final release health check | 25/25 passed |
| 2026-07-25 | Observe final-release hourly formula query | Found a missing fixed-window timestamp predicate; current release `cfbefdec5` is not accepted and a new clean build/redeploy is required |
| 2026-07-25 | Commit and push formula-query P1 fix | `6915a7c47f90f58f0be0db0046c13e94547846e7` (`fix(pm): 限定小时公式查询窗口`); local and remote branch match |
| 2026-07-25 | Run focused P1 verification | `go test ./internal/pm/aggregator -count=1` passed on `6915a7c47`; full repository verification remains pending |
| 2026-07-25 | Build and deploy formula-query fix | Release `0.1.7-20260724-1833`; package `VERSION` `git_commit=6915a7c`; SHA-256 `fd34c0105f1591308a8cafb53c5111abeb8f10a5355fb0b86732502ff8379730`; `current` symlink verified |
| 2026-07-25 | Run final-candidate release health check | 25/25 passed; app, ACS, worker and web use new release tags and have restart count zero |
| 2026-07-25 | Observe dependency-unbounded formula query | Even with the fixed hour predicate, a single formula read loaded all batch counters into Go and ran for more than 6 minutes; `6915a7c47` is an intermediate release, not the final accepted commit |
| 2026-07-25 | Commit and push dependency-filter fix | `79a59763f1d4ffa39f7f454e668319eecee8a6cf` (`fix(pm): 按公式依赖限定小时聚合读取`); local and remote branch match |
| 2026-07-25 | Build and deploy final dependency-filter release | Release `0.1.7-20260724-1904`; package `git_commit=79a5976`; SHA-256 `abf7d773f0ba4c5eb082aeaa38a11213dda1b4bbf23a00d8f1e1e3f79e09c967`; `current` symlink verified exact |
| 2026-07-25 | Run final release health check | 25/25 passed; app, ACS, worker and web restart count zero |
| 2026-07-25 | Run `1904` hourly live gate | FAIL/P1: the dependency-filtered formula statement was active for 8m19s with bucket version 26 still at 0/50; no lock wait or blocker explained the delay |
| 2026-07-25 | Commit and push raw-source formula fix | `def534142659914c28e5b2de6080b2a09ab9ff6f` (`fix(pm): 避免事务内重读小时聚合`); local and remote branch matched; `go test ./internal/pm/aggregator -count=1` passed; included in final commit below |
| 2026-07-25 | Commit and push production batch calibration | `c6d54c5f47d0778a2d0e13408c9919b147e1464d` (`fix(pm): 提升小时聚合生产批次吞吐`); default 500 devices/batch is consistent in Go, development Compose, release Compose and documentation; release/storage validation has 62/62 checks |
| 2026-07-25 | Build and deploy final release | Release `0.1.7-20260724-1954`; `VERSION` `git_commit=c6d54c5`; SHA-256 `0511d7df9a6800629e191f5312c9214b42498c8d35150dfd7bd0484b7ae771b1`; exact `current` symlink recorded above |
| 2026-07-25 | Run final installer health and runtime-parameter check | Health 25/25; app, ACS, worker and web restart count 0 and OOM false; worker environment has `PM_HOURLY_BATCH_DEVICES=500` and `PM_LATE_DATA_WINDOW=168h` |
| Approximately 2026-07-24 20:xxZ | Start 1,000-device/batch calibration run | Worker rebuilt/restarted with `PM_HOURLY_BATCH_DEVICES=1000`; restart count remained 0 and OOM false after recreation. This is a runtime calibration on the existing package, not final package identity evidence. |
| 2026-07-25 | Reject 1,000-device/batch calibration | Bucket version 34 completed two batches at approximately 7 minutes each, projecting approximately 70 minutes for ten batches. This misses one-hour production throughput and is not the final default. |
| 2026-07-25 04:29:47+08 | Start 2,500-device/batch calibration | Bucket version 35 for 02:00; job created/started at 04:29:47+08 with `expected_batches=4`. Complete progress and resource evidence pending. |
| 2026-07-25 04:50:17+08 | Complete 2,500-device/batch calibration | PASS: version 35 published active/clean at 4/4 in 20m29.694872s; exactly one active version and device hourly watermark advanced to 02:00 |
| 2026-07-25 | Commit and push 2,500-device default | `e2139d3972b8a9edbaa9d1f741cf7d478c07e8b3` (`fix(pm): 校准万站小时聚合批次`); default aligned across code/Compose/docs, and batch `finished_at` now records wall-clock completion after formula work; aggregator tests and release/storage validation 62/62 pass |
| 2026-07-25 | Check branch CI status | `glab ci status` returned `no pipeline found`; recorded as no pipeline, not as a CI pass |
| 2026-07-25 | Build and deploy then-exact package | Release `0.1.7-20260724-2053`; package/current `VERSION` `git_commit=e2139d3`; SHA-256 `4145444cd650d8ea3b473877674c1d2331d19ac160d98624f2a48708622c03d5`; exact `current` symlink recorded above |
| 2026-07-25 | Run `2053` migrations, health and runtime-parameter checks | Main migrations 5 / seed 3 / TimescaleDB 1 completed; health 25/25; app/ACS/worker/web tag `2053`, restart 0 and OOM false; worker batch 2500 / late window 168h |
| 2026-07-25 05:11:52 | Verify first version 38 batch on package `2053` | Batch 0 completed 2,500 devices: started `04:59:11.877263`, finished `05:11:52.016441`, duration `12m40.139178s`; version 38 reached 1/4 with 182,500 anchors and 4,094,997 values. Distinct start/finish timestamps prove the `finished_at` correction is active online. |
| 2026-07-25 05:25:59 | Verify version 38 half-way progress | Version 38 reached 2/4 with 365,000 anchors and 8,189,995 values, approximately 26m54s after rollup start at 04:59:05. Device and group jobs remained running with fresh heartbeats; lock waits 0; application restarts 0 and OOM false. The 53m48s half-way projection is below one hour but is not a completion result. |
| 2026-07-25 05:38:24 | Verify version 38 three-quarter progress | Version 38 reached 3/4 with 547,500 anchors and 12,284,995 values, approximately 39m19s after rollup start at 04:59:05. Device and group jobs continued concurrently. The approximately 52m25s projection is not a completion result. |
| 2026-07-25 05:41:53 | Observe end of device/group overlap | Group job was no longer running while the device job remained running and version 38 remained 3/4. The heaviest concurrent phase therefore ended after approximately 42 minutes; the fourth device batch continued without group-job competition. This was subsequently confirmed as group-job failure, not successful completion. |
| 2026-07-25, exact package `2053` | Confirm new online P1 in group hourly path | The 02:00 group-hourly bucket failed on attempt 3 with SQLSTATE `22P02`. `indicatorMetaSQL` queried only legacy `perf_indicators_*` metadata and omitted `pm_metric_dictionary`, so dynamically discovered `Cqi.00` metadata was absent. The following rows record the deployed fix and successful official rerun. |
| 2026-07-25 | Commit, push and review group metadata fix | Implementation `e6894d924c64c85c8ea3c6f8b957bc79b97ba634` pushed. A real PostgreSQL integration test covering dictionary-only, identical, conflicting, field-fallback and complete-legacy metadata cases passed on `172.24.224.197`; independent review was clean. |
| 2026-07-25 | Build and deploy current exact package | Release `0.1.7-20260724-2211`; `VERSION` `git_commit=e6894d9`; SHA-256 `f4403837af06ce44fd768f02ac19645512318b2faba2220f8f8c2f0a571a9b65`; health 25/25; app/ACS/worker/web restart 0 and OOM false; worker batch 2500 / late window 168h |
| 2026-07-25 06:18:23–06:36:28+08 | Complete official recalculation of failed group bucket | Official API job `92de46ab-93f5-433e-8f73-4d87df9d65bc` for `2026-07-24T18:00:00Z` through `2026-07-24T19:00:00Z` succeeded on attempt 1. Started `06:18:23.465165+08`, finished `06:36:28.126604+08`, duration `18m04.661439s`, result rows 1,254. |
| 2026-07-25 06:36:28+08 | Verify group-hourly publication and P1 closure | TSDB window contained 1,254 rows for one group and 1,254 metric paths; `Cqi.00` had one row with value min/max 0/0. Group-hourly watermark advanced to `2026-07-25 02:00:00+08` (`18:00Z`) and was updated at `06:36:28.123977+08`. P1 CLOSED. |
| Post-rerun | Verify final runtime health | Worker log matches for missing metadata, SQLSTATE `22P02`, and failed status were all 0; locks 0; cumulative deadlocks remained 6; app/ACS/worker/web restart 0 and OOM false; health 25/25; Prometheus targets 10/10; NATS messages / pending / ack pending / redelivered 0 / 0 / 0 / 0. |
| Post-rerun | Capture resource headroom | Worker 213.5 MiB / 1 GiB; TimescaleDB 4.469 / 7 GiB; main PostgreSQL 2.295 / 7 GiB; NATS 178.5 MiB; MinIO 2.093 / 4 GiB. |
| Package `2053`, active version 38 rollup | Check host and monitoring health | Host timezone `Etc/UTC`, NTP synchronized; Prometheus API reported 10/10 healthy targets and no unhealthy target; a bounded 15-minute app/ACS/worker log search matched zero OTEL endpoint/DNS failures and zero backup-bucket warnings |
| Package `2053`, active version 38 rollup | Sample host CPU and IO wait | `vmstat 1 5` CPU idle 78%, 75%, 83%, 79%, 76%; IO wait 2%, 0%, 0%, 1%, 0%. This is an active-rollup sample, not the required paced 11.1-files/s steady-state window. |

The deployment must preserve the existing `.env`, `resources.env`, external
data paths, credentials, and persistent volumes. The complete monitoring
profile is required; `--skip-monitoring` is not an accepted deployment for
this validation.

The first installer attempt exposed a GNU/BSD `stat` portability defect in
`monitoring-profile-lib.sh`: on Linux, the BSD probe could exit successfully
while returning an invalid mode string. A regression test was added locally,
the probe order was corrected and the Linux test passed. The server-side
installer copy was patched to allow the deployment to proceed. This defect and
its regression are included in follow-up commit `cfbefdec5`; the server-side
hotfix was superseded by the clean package and runtime identity recorded
above.

## 3. Release health

The `0.1.7-20260724-2211` final installer reported 25/25 health checks passing.
The four application images below were freshly reconciled to
new release tags and remained running with restart count zero and OOM false;
infrastructure health is from the same final check.

| Component | Runtime state | Health evidence | Result |
|---|---|---|---|
| app | Running, restart count 0, OOM false | New `0.1.7-20260724-2211` tag; final release health check | PASS; exact image ID and endpoint detail not captured |
| ACS | Running, restart count 0, OOM false | New `0.1.7-20260724-2211` tag; final release health check | PASS; exact image ID and endpoint detail not captured |
| worker | Running, restart count 0, OOM false | New `0.1.7-20260724-2211` tag; final release health check; batch 2500 / late window 168h | PASS; exact image ID and endpoint detail not captured |
| web | Running, restart count 0, OOM false | New `0.1.7-20260724-2211` tag; final release health check | PASS; exact image ID and endpoint detail not captured |
| PostgreSQL | Running | Final release health check | PASS; detailed SQL transcript not captured |
| TimescaleDB | Running | Final release health check | PASS; detailed extension/SQL transcript not captured |
| Redis | Running | Final release health check | PASS |
| NATS / JetStream | Running | Final release health check plus PM consumer | Runtime PASS; final `jsz` total messages 0 and PM consumer pending / ack pending / redelivered 0 / 0 / 0. Historical peak during validation was 261,458 / 128 / 128. |
| MinIO | Running | Final release health check | PASS |
| Prometheus | Running | Final release health check, in-container `promtool`, and API target query | Runtime PASS; `omc-rules.yml` parsed with 25 rules; 10/10 targets healthy, unhealthy targets `[]` |
| Alertmanager | Running | Final release health check | PASS |
| Grafana | Running | Final release health check | PASS |
| Loki | Running | Final release health check | PASS |
| Tempo | Running | Final release health check | PASS |
| OTEL Collector | Running | Final release health check | PASS |
| NATS / nginx / node exporters / cAdvisor | Running | Release health check and Prometheus API target query | Runtime PASS; all 10 discovered Prometheus targets healthy |

Prometheus rule validation must run through the deployed Prometheus image:

```bash
docker exec <prometheus-container> \
  promtool check config /etc/prometheus/prometheus.yml
```

This validates the referenced rule files without relying on host or
`docker exec` glob expansion. Record the actual mounted config path when it
differs. The final exact deployment also has a bounded 15-minute app/ACS/worker
log search with zero matches for recurring OTEL endpoint/DNS failures and zero
matches for invalid backup-bucket warnings.

## 4. Live PostgreSQL Task 1/2 assertions

These tests are permitted only against a freshly migrated, isolated
TimescaleDB database. They must not point at the production database:

```bash
OMCGO_DB_DSN='<isolated database DSN>' \
  go test -tags=integration ./internal/pm/metrics \
  -run TestIntegrationCopyIngestStoresSparseValuesAndLogicalMissingRows \
  -count=1 -v

OMCGO_DB_DSN='<isolated database DSN>' \
  go test -tags=integration ./internal/pm/aggregator \
  -run 'TestIntegrationVersionedHourly(PublishesSparseBucketAtomically|LockOrderAvoidsIngestDeadlock)' \
  -count=1 -v
```

| Assertion | Isolated database | Exit / key observation | Result |
|---|---|---|---|
| Existing dictionary and metric-set rows remain physically unchanged | `omcgo_task10_test` | `TestIntegrationCopyIngestStoresSparseValuesAndLogicalMissingRows` passed | PASS |
| Sparse values reconstruct logical missing rows | `omcgo_task10_test` | Same integration assertion passed | PASS |
| Versioned hourly publication is atomic | `omcgo_task10_test` | `TestIntegrationVersionedHourlyPublishesSparseBucketAtomically` passed | PASS |
| Concurrent ingest/rollup avoids the former reverse lock cycle | `omcgo_task10_test` | `TestIntegrationVersionedHourlyLockOrderAvoidsIngestDeadlock` passed | PASS |
| KPI report key cannot promote a discovered counter path | `omcgo_task10_test` | `TestIntegrationSyncMetricDictionaryDoesNotPromoteKPIReportKeyToKPI` passed, including positive counter enrichment | PASS |

The isolated database was created from the production schema only and
contained no production data. After the tests passed, the temporary
`omcgo_task10_test` database and `omcgo-task10-gomod` dependency volume were
removed; no production database or persistent volume was changed by cleanup.

## 5. Guarded isolated index analysis

The procedure and exact three-part guard are defined in
`2026-07-24-pm-index-analysis.md`. The required database identity is
`omc_task8_sparse_clone`; the client flag, exact database match, and matching
server-side sentinel must all pass before the harness can execute. The harness
runs in one transaction and ends with `ROLLBACK`.

| Item | Evidence |
|---|---|
| Clone creation method and source snapshot timestamp | NOT RUN |
| Exact database identity | NOT RUN |
| Sentinel row | NOT RUN |
| Guard-test result | NOT RUN |
| Harness exit code | NOT RUN |
| Cold and warm plan captures | NOT RUN |
| Candidate index statistics and physical bytes | NOT RUN |
| Decision | **Retain every index unless complete before/after evidence proves removal safe** |

No production index deletion is part of this task.

## 6. Same-input legacy versus sparse dual run

The raw corpus must be copied once to a neutral immutable directory. Its
SHA-256 manifest must be unchanged before, between, and after both runs. The
legacy and sparse environments must not share databases, MinIO, NATS, ports,
or data directories.

| Item | Legacy | Sparse |
|---|---|---|
| Exact revision | `2732aa9a618fb6fdc9ce1e32ade85de08c33670b` | `11b24a3f369c04170e919f211f14e081fa6d858a` |
| Environment identity | NOT RUN | NOT RUN |
| Database / MinIO / NATS isolation | NOT RUN | NOT RUN |
| Raw manifest before | NOT RUN | NOT RUN |
| Raw manifest after | NOT RUN | NOT RUN |
| Queue-drained timestamp | NOT RUN | NOT RUN |
| Checkpoint / analyze timestamp | NOT RUN | NOT RUN |
| Logical Counter / KPI / Dashboard / CSV / hourly exports | NOT RUN | NOT RUN |
| Table bytes | NOT RUN | NOT RUN |
| Index bytes | NOT RUN | NOT RUN |
| TOAST bytes | NOT RUN | NOT RUN |
| WAL bytes for identical ingest | NOT RUN | NOT RUN |
| Logical / anchor / value counts | NOT RUN | NOT RUN |

Comparator gate:

```text
logical_equal = NOT RUN
old_bytes = NOT RUN
sparse_bytes = NOT RUN
sparse_bytes / old_bytes = NOT RUN
```

The comparator must be run against independently exported legacy and sparse
logical views. A production sparse logical view cannot stand in for either
side.

## 7. 10,000-device workload observation

If the original load has ended, replay must use the existing repository load
tool with a new, isolated SN prefix and must not use `cleanup`, `purge`,
`-cleanup-after`, or any operation that deletes production PM or JetStream
state. Record whether the test is:

- a 10,000-file burst observed until drain, or
- a paced 11.1-file/s steady-state stream observed for a full 30 minutes.

Samples are required every 30–60 seconds. The raw sample table or attached
artifact must contain:

| Sample fields |
|---|
| timestamp; pending; ack pending; redelivered; oldest age; delivery/ack/publish rates; pending slope; pressure state |
| files processed/failed/retried and processing-duration deltas |
| host CPU idle; load; memory; swap; disk latency/utilization/IO pressure |
| container CPU/memory/restarts/OOM/health |
| PostgreSQL/TimescaleDB connections, waits, blockers, deadlocks, rollbacks, dead tuples and autovacuum |
| async hourly jobs; hourly version states; active-version count; watermark |
| application errors, retries, DLQ entries, OTEL and backup warnings |

### Observation summary

| Window | Start | End | Samples | Result |
|---|---|---|---|---|
| 10,000-file burst | NOT CAPTURED | NOT CAPTURED | NOT CAPTURED | NOT VERIFIED |
| 11.1 files/s for 30 minutes | N/A | N/A | 0 | NOT CLAIMED — producer stopped before a paced window |

First post-deployment sample:

| Timestamp | Pending | Ack pending | Oldest age | Backpressure | Rates | Worker observations |
|---|---:|---:|---:|---|---|---|
| Shortly after 17:11Z | 262,741 | 128 | 23,958 s | Active (`queue_pending`) | First-sample rate gauges were `NaN`; no slope conclusion is possible yet | `failed` file counter 248; hourly recovery counters present, exact values and deltas under investigation |

Compared with the pre-deployment point, pending was already 9,106 messages
higher. These two points do not form an acceptance window: deployment elapsed
time and producer activity must be accounted for, rate gauges require at least
two fresh monotonic samples, and the pre-existing backlog was more than six
hours old. The value 248 was only an intermediate process/window snapshot and
is not the final historical failure total.

Follow-up diagnosis:

| Timestamp | Finding | Evidence / action |
|---|---|---|
| 2026-07-24 17:35Z | Restart-window queue sampler errors | Nine `consumer not found` samples occurred only while `pm-workers` was being recreated. No such error recurred after 17:35:24Z; final worker consumer creation was logged at 17:35:38Z. |
| After final consumer creation | Queue state | Pending 261,458; ack pending 128; redelivered 128. |
| Post-fix dictionary sync probe | Stable-row rewrite eliminated | `pm_metric_dictionary` contained 2,043 rows and had 30,511,364 cumulative updates. After the runner fix, the delta was only 8 targeted/startup updates rather than a full-table rewrite every cycle. |
| Before second rebuild | P0 lock queue reproduced | 32 ingest sessions blocked on `UPDATE pm_hourly_bucket_versions SET dirty=true`; target building version 18 was already `dirty=true`, so the repeated level-triggered update had no semantic effect but serialized every writer. |
| Recent three-hour historical DLQ grouping | Strict optional-metadata mismatch | 2,166 entries for `Cqi.00`: an empty optional statis/unit input from backlog data was incorrectly treated as conflicting with an already-enriched dictionary row. |
| Recent three-hour historical DLQ grouping | KPI/report-key type collision | 1,533 entries for `ERAB.RelEnbNbr.Sum`: KPI report-key enrichment incorrectly promoted a dynamically discovered counter path to KPI. |
| After metadata and dirty-row fixes | Worker / lock / queue recovery | Worker processed 7,074 files successfully with no new error or DLQ entry; TimescaleDB waiters fell from 32 to 0; pending fell from 261,458 to 255,327; redelivered was 0. |
| Later online sample | Continued drain / resources | Worker success reached 14,496; pending fell to 246,990; waiters remained 0. Worker used 618.86% CPU / 211.7 MiB, TimescaleDB 866.60% / 3.213 GiB, main PostgreSQL 23.53% / 787.9 MiB, ACS 29.9% / 55.69 MiB, and NATS 6.01% / 149.4 MiB. |
| Later online sample | Deadlocks | TimescaleDB cumulative deadlocks remained at 6. The first pre-deployment baseline was 5, so the superseded diagnostic deployment observed one new deadlock; the final clean-release window must rebaseline at 6 and remain unchanged. |
| Final `cfbefdec5` release | New P1 in hourly formula read | `insertVersionedHourlyFormulaKPIs` queried hourly anchors/values by bucket version and device list but omitted the fixed `ha."time"` predicate. The old online SQL had run for 11 minutes. |
| Isolated plan after P1 fix | Bounded formula read | Adding `ha."time"=$3` allowed `EXPLAIN ANALYZE` over 200 actual device IDs and 303,800 output rows to complete in 2.821 seconds while using the `(time,bucket_version,anchor_id,...)` prefix of `uq_pm_hourly_values`. |
| `6915a7c47` live release | Dependency-unbounded formula read | The fixed-hour query still returned every counter for the batch and a single online statement exceeded 6 minutes. |
| Dependency-filter fix `79a59763f` | Reduce Go input cardinality | First query distinct `(device, object_type, object_ldn)` anchors to preserve object enumeration, including constant formulas. A second query loads only the de-duplicated, sorted union of formula dependencies through `d.metric_path=ANY($4::text[])`; constant-only batches skip the counter-value query. Deployed successfully, but the following live gate exposed the read-own-writes P1. |
| `79a59763f` live gate | Read-own-writes performance P1 | The new predicate was present, yet formula-query PID 63993 remained active for 8m19s and bucket version 26 stayed at 0/50. There were no lock waits or blockers. The platform route had 73 KPIs / 154 dependency paths versus 1,333 counters. |
| Historical committed-version control | Same filtered query outside current write path | `EXPLAIN ANALYZE` against historical version 13 produced 56,600 rows in 3.044 seconds, isolating the abnormal behavior to the current transaction's read of freshly written hourly hypertable rows rather than the dependency predicate itself. |
| Raw-source fix `def534142` | Avoid current-transaction hourly reread | Read required counters from raw `pm_measurement_anchors` / `pm_metric_values` and aggregate with the same statis/normalization semantics as the base hourly SQL; do not re-read newly inserted hourly rows in the batch transaction. Focused tests pass; package and live proof pending. |

The P0 lock root cause was fixed locally by adding `AND dirty=false` to the
hourly-version update. Its SQL-shape regression passed. The dictionary sync
query was also made change-sensitive with `IS DISTINCT FROM`, and only
configured counter report keys may enrich dynamically discovered counter
paths; KPI report keys cannot promote an existing counter path to KPI. Existing
mirror-merge tests and the positive counter-enrichment integration case were
retained after review. Sparse dictionary comparison now treats an absent
optional `statis_type` or `unit` as unknown while still rejecting two explicit,
different values and metric-type conflicts.

The successful recovery figures above came from a diagnostic rebuild of the
working-tree fixes. `build-release.sh` embeds `git rev-parse --short HEAD` and
does not identify dirty content, so that artifact cannot be used as exact
commit evidence. That diagnostic artifact was superseded by clean release
`0.1.7-20260724-1808`, whose `RELEASE.txt`, package checksum and active release
path are recorded above. The formula-query P1 then superseded that release;
workload thresholds are evaluated only from `0.1.7-20260724-1833`.

The superseded `cfbefdec5` clean-deployment baseline had zero TimescaleDB
waiters, 6 cumulative deadlocks, 30,511,430 cumulative metric-dictionary
updates, and queue state 159,014 pending / 128 ack pending / 0 redelivered. Its
first worker sample recorded 3,183 successes and no new worker error or DLQ
entry. Those values led to the P1 discovery and are not reused as the final
candidate's acceptance window.

That observation also found the formula-query P1 recorded above. The
regression test was proven to fail before the fix, and the implementation now
passes `w.Start` as the third query argument with `ha."time"=$3`. The 2.821
second plan is diagnostic evidence for the fix, not final runtime acceptance.

Release `0.1.7-20260724-1833` contains the hour predicate. In its first live
sample, the
former 11-minute statement was absent. Bucket 21 had a new active job whose
base-batch SQL age was 12 seconds, while buckets 19 and 20 remained pending.
Queue state was 60,358 pending / 128 ack pending / 0 redelivered. Resource
usage was worker 480% CPU / 181 MiB, TimescaleDB 993% CPU / 2.756 GiB of a
7-GiB limit, main PostgreSQL 75% CPU / 1.443 GiB, and NATS 4.6% CPU / 149 MiB.
This initial improvement did not survive the next observation: the query still
fed all batch counters into Go and one statement exceeded 6 minutes. Commit
`79a59763f` now separates object enumeration from counter loading. A distinct
anchor query enumerates `(device, object_type, object_ldn)` independently, then
the counter query uses the sorted, de-duplicated union of formula dependencies
through `d.metric_path=ANY($4::text[])`. Dependency-free/constant formulas
therefore retain object semantics without disabling filtering; a
constant-only batch skips the counter-value query. Focused tests passed, but
hourly completion and the full resource/queue window remain pending.

Final release `0.1.7-20260724-1904` contains the two-query implementation. At
2026-07-24 19:10:30Z its first snapshot showed queue pending 0, ack pending 0,
redelivered 0, no new DLQ row, zero TimescaleDB waiters, and cumulative
deadlocks unchanged at 6. Worker memory was 29.85 MiB, TimescaleDB used
3.986/7 GiB, main PostgreSQL 1.386/7 GiB, and MinIO 3.009/4 GiB. Hour 23:00
was being processed as bucket version 26 at 0/50 completed batches; the active
base-anchor query age was 6 seconds. This is a clean initial sample, while
hourly advancement/completion and the continuous window remain pending.

The next sample failed the live gate. Although the dependency predicate was
active and narrowed a platform route from 1,333 counters to 154 dependency
paths for 73 KPIs, formula-query PID 63993 remained active for 8m19s and
bucket version 26 was still 0/50. TimescaleDB showed no lock wait or blocker.
The same filtered query against committed historical version 13 returned
56,600 rows in 3.044 seconds under `EXPLAIN ANALYZE`, pointing to the batch
transaction's read-own-writes path over newly inserted hourly hypertable rows.
Release `1904` is therefore not accepted. Commit `def534142` aggregates
required dependency counters directly from raw anchors/values using the base
hourly statis/normalization semantics, and final commit `c6d54c5f4` adds the
500-device production calibration. Final release `1954` includes both; its
complete hourly observation is still required.

Before the then-exact `2053` package was deployed, the same raw-source code and
500-device configuration ran in the `def534142` image. Bucket version 30 for
02:00 reached 11/20 batches in 14m06s, producing 401,500 anchors and 8,995,214
values, before it was interrupted only by deployment of the `2053`
package. Resource usage at that point was worker 220.7 MiB of 1 GiB,
TimescaleDB 4.309/7 GiB, main PostgreSQL 2.267/7 GiB, NATS 160.7 MiB, and
MinIO 2.76/4 GiB. This proves sustained progress and memory headroom but not a
completed bucket.

Final release `0.1.7-20260724-1954` then started bucket version 32 for 22:00.
At approximately 2m33s it remained at 0/20 while its base SQL was actively
running with no lock wait. The complete 20/20 transition, active-version
uniqueness and watermark advancement remain pending.

The server was subsequently switched to an explicit 1,000-device batch
override. Bucket version 34 completed two batches at approximately 7 minutes
each, projecting roughly 70 minutes for ten batches, so this calibration was
rejected. During that stage worker memory ranged from 57 to 196 MiB of 1 GiB,
TimescaleDB peaked at 5.828/7 GiB, and main PostgreSQL used approximately
2.3/7 GiB. Locks and blockers remained zero. NATS had zero total messages and
pending/ack/redelivery were all zero. The DLQ retained 3,699 historical rows
but gained no new row after the 1,000-device switch.

The current experimental override is 2,500 devices/batch. Bucket version 35
for 02:00 was created/started at 2026-07-25 04:29:47+08 with four expected
batches. At 04:32:49+08 it was 0/4 with its base SQL active for 34 seconds.
Worker used 547% CPU / 278.6 MiB of 1 GiB; TimescaleDB 1,298% CPU / 4.525 GiB
of 7 GiB; main PostgreSQL 57% CPU / 2.02 GiB of 7 GiB; NATS 172.8 MiB; and
MinIO 2.92/4 GiB. At 04:33:34+08 worker used 190.8 MiB and TimescaleDB
4.719 GiB, with no lock wait. At 04:36:09+08 version 35 reached 1/4, with
182,500 anchors and 4,088,609 values. The first batch became visibly committed
after approximately 5–6 minutes; the second batch's active SQL age was 66
seconds. A linear projection is approximately 22–26 minutes, but only actual
4/4 can satisfy the gate. At 04:41:17+08 it reached 2/4, with 365,000 anchors
and 8,177,459 values after 11m30s total. The measured linear projection was
approximately 23 minutes. Restart count remained zero and OOM false; worker
peak was 278.6 MiB and the current TimescaleDB peak approximately 5.8/7 GiB.
At 04:45:39+08 it reached 3/4, with 547,500 anchors and 12,266,225 values
after 15m52s; the fourth batch's base aggregation was active. Worker used 607%
CPU / 212.1 MiB, TimescaleDB 955% CPU / 4.437 GiB, and locks/blockers remained
zero. Source/config validation reports 62/62, focused Go tests pass and review
is clean. The source change is commit `e2139d397`; the actual 4/4 publication
and watermark result is recorded below. The then-exact package identity was
confirmed by release `2053` as recorded above.

`pm_files.max(parsed_at)` was 2026-07-25 04:33:01+08, proving that the external
load producer had stopped during this hourly calibration. This run therefore
cannot be used as a 30-minute 11.1-file/s steady-state proof.

Version 35 completed successfully. It was created at 04:29:47.492+08 and
published at 04:50:17.148+08 as `active`, `dirty=false`, with 4/4 batches,
730,000 anchors and 16,355,079 values. The async job succeeded on attempt 2
with duration 20m29.694872s. The device hourly watermark advanced to 02:00 and
was updated at 04:50:17.158+08. There was exactly one active version for the
bucket; older version 30 was `failed` and invisible. Locks and blockers
remained zero and cumulative deadlocks remained 6, with no increment.

At completion worker used 102.1 MiB, TimescaleDB 5.726/7 GiB, main PostgreSQL
2.246/7 GiB, NATS 169.8 MiB, and MinIO 2.867/4 GiB. This passes the hourly
behavior gate for the explicit 2,500-device runtime calibration. The
then-exact release `0.1.7-20260724-2053` embedded `e2139d397`, made 2,500 the
package default, and passed the identity/health/runtime-parameter checks
recorded above before its group-hourly P1 was discovered.

The same path was then exercised on package `2053` with bucket
version 38. Its first batch processed 2,500 devices, started at
`04:59:11.877263`, and finished at `05:11:52.016441`, an observed duration of
`12m40.139178s`. Version 38 then reported 1/4 batches, 182,500 anchors, and
4,094,997 values. The real finish timestamp no longer equals the start
timestamp, proving the `e2139d397` batch-completion timestamp fix is active in
the deployed package. Concurrent `pm_aggregate_hourly_group` and
`pm_aggregate_hourly_device` work had run for approximately 13 minutes with no
lock wait. A linear projection from this first batch is 50m40s, below one
hour, but this is not a substitute for actual 4/4 publication; final exact
package completion remains in progress.

At 05:25:59 version 38 reached 2/4, with 365,000 anchors and 8,189,995 values,
approximately 26m54s after the rollup began at 04:59:05. Both device and group
jobs remained running with fresh heartbeats. Lock waits remained zero, and
application restart and OOM counts remained zero. The half-way linear
projection is approximately 53m48s, below one hour, but actual 4/4 completion
is still required.

At 05:38:24 version 38 reached 3/4, with 547,500 anchors and 12,284,995
values, approximately 39m19s after the 04:59:05 rollup start. Device and group
work continued concurrently. The current approximately 52m25s projection
remained below one hour, but this run did not close exact-package acceptance
because the concurrent group-hourly path later failed.

At 05:41:53 the group job was no longer running while the device job remained
running and version 38 remained 3/4. This bounds the heaviest device/group
overlap to approximately 42 minutes; the final device batch then continued
without group-job competition. This status transition was subsequently
confirmed to be a failure, not successful group completion: the exact `2053`
package's 02:00 group-hourly bucket failed on attempt 3 with SQLSTATE `22P02`.
`indicatorMetaSQL` queried only legacy `perf_indicators_*` metadata and did
not include dynamically discovered metadata from `pm_metric_dictionary`, so
metadata for `Cqi.00` was absent.

The fix was deployed in exact package `2211`. Official API recalculation job
`92de46ab-93f5-433e-8f73-4d87df9d65bc` for the original
`2026-07-24T18:00:00Z` through `19:00:00Z` window succeeded on attempt 1. It
started at `06:18:23.465165+08`, finished at `06:36:28.126604+08`, and ran for
`18m04.661439s`, reporting 1,254 result rows. The TSDB window contained 1,254
rows for one group and 1,254 metric paths. `Cqi.00` was present as one row
with value min/max 0/0. The group-hourly watermark advanced to
`2026-07-25 02:00:00+08` (`18:00Z`) and was updated at
`06:36:28.123977+08`.

Post-rerun worker log matches for missing metadata, SQLSTATE `22P02`, and
failed status were all zero. Locks remained zero and cumulative deadlocks
remained 6. App, ACS, worker and web remained at restart count 0 and OOM false;
health was 25/25, Prometheus targets were 10/10 healthy, and NATS messages /
pending / ack pending / redelivered were 0 / 0 / 0 / 0. Resource usage was
worker 213.5 MiB / 1 GiB, TimescaleDB 4.469 / 7 GiB, main PostgreSQL 2.295 /
7 GiB, NATS 178.5 MiB, and MinIO 2.093 / 4 GiB. The group-hourly metadata P1
is closed.

## 8. Acceptance matrix

| Acceptance threshold | Required | Actual evidence | Result |
|---|---|---|---|
| Exact deployed commit | `e6894d924c64c85c8ea3c6f8b957bc79b97ba634` | Release `0.1.7-20260724-2211`; package `git_commit=e6894d9`; SHA-256 recorded above; `/opt/omc/current` resolves to the `2211` release; package deployed and health-checked | PASS |
| Final package / deploy / installer health | Exact checksum, migrations, active release and 25/25 health | SHA-256 `f4403837af06ce44fd768f02ac19645512318b2faba2220f8f8c2f0a571a9b65`; release `2211` deployed; last explicit migration capture 5/3/1; health 25/25; app/ACS/worker/web tag `2211` | PASS |
| 10,000-file burst drain | At most 15 minutes | Existing backlog drained, but no immutable, exactly 10,000-file burst identity and start/end timing were captured | NOT VERIFIED |
| 30-minute steady-state pending slope | `<= 0` at 11.1 files/s | External writes had stopped by 04:33:01+08 during the hourly calibration; no paced 30-minute producer window was run | N/A — NOT CLAIMED |
| Oldest pending age after warm-up | `< 5 minutes` | Queue later reached zero, but the required warm-up/producer window and oldest-age series were not captured | NOT VERIFIED |
| Ack pending | Not continuously saturated | Queue/ack/redelivery later reached 0/0/0, but no continuous under-load window was captured | NOT VERIFIED |
| New PostgreSQL deadlocks | `0` | Cumulative deadlocks remained 6 across the complete device calibration and exact-package group-hourly rerun | PASS |
| Persistent lock wait queue | None | Locks/blockers remained zero through the device calibration and exact-package group-hourly rerun | PASS |
| Hourly bucket | Device and group paths succeed, exactly one active version, watermarks advance | Device version 35 published clean at 4/4 in 20m29.694872s with exactly one active version and its watermark advanced. Fix `e6894d924` passed the real PostgreSQL metadata matrix; exact package `2211` official group recalculation succeeded attempt 1 in 18m04.661439s with 1,254 rows, `Cqi.00` present, and group watermark advanced to 18:00Z. P1 CLOSED. | PASS |
| Host CPU idle in steady state | `>= 20%` | During package `2053` version 38 rollup, `vmstat 1 5` sampled idle 78%, 75%, 83%, 79%, 76% and IO wait 2%, 0%, 0%, 1%, 0%; the required paced 11.1-files/s window was not run | NOT VERIFIED |
| OOM / restarts / unhealthy dependencies | None | Device calibration stayed within memory limits; after exact `2211` group rerun app/ACS/worker/web were restart 0 and OOM false, health 25/25, with worker 213.5 MiB / 1 GiB and both databases below their 7-GiB limits | PASS |
| Complete monitoring and exporters | Healthy / Prometheus targets up | Release health 25/25 with monitoring and OTEL running; Prometheus API reported 10 targets healthy out of 10, unhealthy `[]` | PASS |
| Prometheus rules | `promtool` passes | In-container `promtool` passed `omc-rules.yml` with 25 rules | PASS |
| Recurring OTEL / backup-bucket warning | None | Final-deployment bounded 15-minute search across app/ACS/worker matched 0 OTEL endpoint/DNS failures and 0 backup-bucket warnings | PASS |
| Task 1/2 live PostgreSQL assertions | All pass in isolated database | Five assertions across metrics, aggregator and tsdbsync passed in schema-only `omcgo_task10_test` | PASS |
| Guarded core-query plans | Captured in exact isolated clone; indexes retained by default | Guarded `omc_task8_sparse_clone` harness was not run; all indexes remain retained | NOT VERIFIED |
| Sparse logical output | `logical_equal=true` for required scopes | Faithful isolated legacy/sparse dual run was not executed | NOT VERIFIED |
| Sparse physical storage | `sparse_bytes / old_bytes <= 0.20` | Faithful same-input physical measurement was not executed | NOT VERIFIED |

## 9. Conclusion

**NOT FULLY ACCEPTED.** Exact fix package `2211` / `e6894d924` is deployed and
healthy, and its metadata regression matrix passed against real PostgreSQL.
The official recalculation of the group-hourly bucket that exposed
SQLSTATE `22P02` succeeded with verified output and watermark advancement.
That P1 and the exact-package hourly gate are closed. The matrix still contains
one explicitly unclaimed load scenario and several not-verified thresholds,
so Task 10 remains incomplete unless those remaining rows are replaced by
fresh, timestamped PASS evidence. No queue data, production PM data, index,
volume, or workload result was deleted or fabricated to manufacture a pass.
