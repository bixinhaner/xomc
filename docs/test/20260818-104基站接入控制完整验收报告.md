# 104 基站接入控制完整验收报告

## 1. 结论

- 验收时间：2026-08-18 12:34—12:48（UTC，104 页面系统时间）
- 环境：`http://172.17.3.104:8081`
- MR：!556，分支 `codex/base-station-access-control-design`
- MR 单一提交：`c461ce607c005c19419881f9a6b3031d71ba3b18`
- 部署记录：最新 main `416ebc074`，104 HEAD `5cd7ffec3`
- 结论：**QRTB 参数解析缺陷修复通过；接入控制核心闭环通过。正式放行仍缺受限账号 403、运行镜像 digest、数据库/CWMP 原始记录三类审计证据。**

本次重点复验此前唯一代码阻塞项：mBS31001/QRTB 的 `Device.DeviceInfo.SAS.RadioEnable` 解析、写入、设备回执和动作终态。旧错误 `RF control requires writable ... invalid input` 未再出现。

## 2. 部署与页面

| 检查项 | 证据 | 结果 |
|---|---|---|
| 版本 | 104 HEAD `5cd7ffec3`，已整合 MR `c461ce607` | 通过 |
| 数据库 | schema/seed reconcile 均退出 0 | 通过 |
| 服务 | app、ACS、worker、web 运行；9091/9092/9095/Web 返回 200 | 通过 |
| 配置边界 | 未恢复 `device_access` YAML；由 CMCC 页面业务总开关控制 | 通过 |
| 管理员 UI | 菜单、五页签、CMCC 作用域、策略/名单/候选/决定/证据/动作、中文页面 | 通过 |
| 运行镜像摘要 | 部署截图未包含四个镜像 digest | 阻塞 |

## 3. 两台设备业务结果

| 项目 | QRTB | MLN |
|---|---|---|
| SN/IP | `1202000240194DP0005` / `172.19.3.54` | `12020007592481D0041` / `172.19.3.182` |
| ProductClass | `FAP/mBS31001/DC` | `FAP/MLN/SC` |
| 初始状态 | 在线、未激活、RF 关 | 在线、未激活、RF 关 |
| 名单流程 | 临时黑名单命中 `reject/denylist_matched`；停用后进入普通规则 | 白名单放行、黑名单优先、依次停用后回普通规则均通过 |
| 普通规则最终决定 | `reject/rule_mismatch`、冻结，v460 | `reject/no_applicable_rule`、冻结，v40 |
| 决定依据 | TAC、ECGI、IP 通过；GPS 不在已发布 geofence，预期拒绝 | 无适用已发布规则，预期拒绝 |
| 最终设备状态 | 在线、未激活、RF 关 | 在线、未激活、RF 关 |

QRTB 最新证据：TAC `1`；双小区 ECGI `314-030-135755833`、`441-90-192192`；IP `172.19.3.54`；GPS `30.5363,104.068`。最新 `rule_mismatch` 明确由 GPS geofence 条件失败产生，不是 RF 参数解析回归。

## 4. QRTB RF 闭环复验

1. 新增临时 QRTB 黑名单后，决定变为 `reject/denylist_matched`，普通任务冻结。
2. 对此前失败动作执行重试，动作变为 `RF 隔离/成功/尝试 2 次`；旧 `invalid input` 未再出现。
3. 停用临时黑名单后，决定恢复普通规则路径，并产生 `RF 恢复/成功/尝试 1 次`。
4. 完整参数同步完成：517 条、跳过 23 条、43 次 GPV；精确路径 `Device.DeviceInfo.SAS.RadioEnable=true`，证明恢复动作的设备回执与 GPV 目标一致。
5. 为恢复验收前状态，手工写回 `Device.DeviceInfo.SAS.RadioEnable=false`；消息中心显示设备参数设置完成。随后新 Inform/证据重评得到 `reject/rule_mismatch`，并生成新的 `RF 隔离/成功/尝试 1 次`（12:46:58）。
6. 最终根参数和两个实例均保持 RF 关闭语义；临时名单已停用。

结果：**通过。** 已分别证明接入决定、普通任务冻结、设备动作、设备回执与 GPV，未使用页面接入状态替代设备回执。

## 5. 需求追溯矩阵

| Redmine | 核心要求 | 结果 |
|---|---|---|
| #31672 | SN/IP/TAC/ECGI/GPS 判定；不满足时 OMC 主动去激活 | 通过 |
| #51449 | 名单展示设备信息、停用确认与审计保留 | 通过 |
| #45094 | 管理入口、五页签、作用域、i18n、RBAC | 部分通过：管理员与未认证 401 已验；缺受限账号 403 |
| #31732 | 总开关、规则、黑白名单、接入状态完整流程 | 通过 |
| #31675 | 非 accepted 设备冻结普通任务 | 通过 |
| #21156 | GPS 接入控制及拒绝后主动去激活 | 通过 |
| #21155 | GPS 前端配置与证据展示 | 通过；本轮未重复导入导出 |

## 6. 恢复记录与遗留项

- CMCC 业务总开关保持开启。
- QRTB、MLN 本轮临时黑/白名单均已停用。
- 两台设备均恢复为在线、未激活、RF 关。
- 未修改服务器、数据库、网络及无关设备参数。
- 未覆盖的正式审计证据：受限账号登录后 403；四个运行镜像 digest；数据库动作/任务记录与 CWMP 请求响应原文。

**灰度意见：核心业务与真实 QRTB RF 闭环已通过，可进入受控灰度；在正式发布签字前补齐上述三类审计证据。**
