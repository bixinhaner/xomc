# OMC 后端开源组件与基础设施分析报告

> **版本**：1.0.0
> **日期**：2026-03-12
> **适用系统**：OMC（Operations, Management and Control）小基站综合管理系统
> **后端模块**：`omcgo/`（Go 1.25，Gin + PostgreSQL + TimescaleDB + Redis + MinIO + NATS）

本报告对 OMC 后端所有开源依赖进行系统性分析，涵盖许可证合规、技术风险、国产化替代路径及水平扩展能力，供技术决策与采购评审使用。

---

## 目录

1. [开源库全景](#1-开源库全景)
2. [Redis 风险分析](#2-redis-风险分析)
3. [数据库国产替代方案](#3-数据库国产替代方案)
4. [MinIO 用途与替代方案](#4-minio-用途与替代方案)
5. [NATS JetStream 风险分析](#5-nats-jetstream-风险分析)
6. [PostgreSQL 分析](#6-postgresql-分析)
7. [TimescaleDB 时序数据库分析](#7-timescaledb-时序数据库分析)
8. [水平扩展能力分析](#8-水平扩展能力分析)
9. [综合风险汇总与行动计划](#9-综合风险汇总与行动计划)

---

## 1. 开源库全景

### 1.1 直接依赖（21 个）

基于 `omcgo/go.mod` 快照，共 21 条直接依赖，全部为宽松型许可证：

| # | 模块路径 | 版本 | 许可证 | 用途 |
|---|---------|------|--------|------|
| 1 | `github.com/gin-gonic/gin` | v1.12.0 | MIT | REST API HTTP 框架 |
| 2 | `github.com/jackc/pgx/v5` | v5.8.0 | MIT | PostgreSQL/TimescaleDB 驱动 + pgxpool 连接池 |
| 3 | `github.com/redis/go-redis/v9` | v9.18.0 | BSD-2 | Redis 客户端（支持 Standalone/Cluster 自动切换）|
| 4 | `github.com/nats-io/nats.go` | v1.31.0 | Apache-2.0 | NATS JetStream 消息队列客户端 |
| 5 | `github.com/minio/minio-go/v7` | v7.0.98 | Apache-2.0 | MinIO/S3 对象存储客户端 |
| 6 | `github.com/spf13/viper` | v1.18.2 | MIT | YAML 配置加载 + 环境变量绑定 |
| 7 | `github.com/spf13/cobra` | v1.8.0 | Apache-2.0 | CLI 框架（acs/app/worker/migrate/omcctl 5 个入口）|
| 8 | `go.uber.org/zap` | v1.21.0 | MIT | 结构化高性能日志 |
| 9 | `github.com/prometheus/client_golang` | v1.23.2 | Apache-2.0 | Prometheus 指标暴露（3 个进程各自 /metrics）|
| 10 | `go.opentelemetry.io/otel` | v1.41.0 | Apache-2.0 | 分布式链路追踪 API |
| 11 | `go.opentelemetry.io/otel/sdk` | v1.41.0 | Apache-2.0 | OpenTelemetry SDK |
| 12 | `go.opentelemetry.io/otel/exporters/otlp/otlptrace/otlptracegrpc` | v1.41.0 | Apache-2.0 | OTLP gRPC 链路数据导出 |
| 13 | `github.com/golang-jwt/jwt/v5` | v5.3.1 | MIT | JWT 认证（RBAC 管理面）|
| 14 | `github.com/golang-migrate/migrate/v4` | v4.19.1 | MIT | 数据库 Schema 迁移（33+ 迁移文件）|
| 15 | `github.com/Masterminds/squirrel` | v1.5.4 | MIT | 动态 SQL 构建（15+ 仓储文件使用）|
| 16 | `github.com/beevik/etree` | v1.6.0 | BSD-2 | TR069 参数树 XML 动态遍历 |
| 17 | `github.com/google/uuid` | v1.6.0 | BSD-3 | UUID 生成（所有表主键）|
| 18 | `github.com/robfig/cron/v3` | v3.0.1 | MIT | 定时任务（心跳检测、KPI 聚合）|
| 19 | `github.com/stretchr/testify` | v1.11.1 | MIT | 测试断言（仅测试构建）|
| 20 | `golang.org/x/crypto` | v0.48.0 | BSD-3 | bcrypt 密码哈希 |
| 21 | `golang.org/x/time` | v0.12.0 | BSD-3 | 设备级/全局限流器（rate.Limiter）|

### 1.2 间接依赖（~89 个）

`go.mod` 记录约 89 条 `// indirect` 依赖。按来源分类：

| 依赖链 | 关键间接依赖 | 许可证 |
|--------|------------|--------|
| Gin 框架链 | bytedance/sonic, go-playground/validator, goccy/go-json | MIT |
| pgx 驱动链 | jackc/pgpassfile, jackc/puddle/v2, lib/pq | MIT |
| MinIO 客户端链 | klauspost/compress, dustin/go-humanize, tinylib/msgp | MIT/BSD |
| NATS 客户端链 | nats-io/nkeys, nats-io/nuid | Apache-2.0 |
| OpenTelemetry 链 | google.golang.org/grpc, google.golang.org/protobuf | Apache-2.0 |
| Viper/Cobra 链 | fsnotify/fsnotify, hashicorp/hcl, mitchellh/mapstructure | MIT |
| Prometheus 链 | beorn7/perks, cespare/xxhash/v2 | Apache-2.0/MIT |
| 测试依赖 | alicebob/miniredis/v2（内存 Redis 模拟）| MIT |
| 传递依赖 | go.mongodb.org/mongo-driver/v2（MinIO 间接引入，未直接使用）| Apache-2.0 |

### 1.3 许可证分布汇总

| 许可证类型 | 数量（估算）| 商业使用 | 传染性 |
|-----------|-----------|---------|--------|
| MIT | ~55 | 允许 | 无 |
| Apache-2.0 | ~25 | 允许 | 无（含专利授权条款）|
| BSD-2/3-Clause | ~12 | 允许 | 无 |
| **合计** | **~92** | **全部允许** | **无** |

> **结论：** 所有 Go 依赖均为宽松型许可证，无 GPL/LGPL/AGPL 传染性代码进入产品二进制。
> **风险评级：无**

---

## 2. Redis 风险分析

### 2.1 当前使用情况

| 项目 | 详情 |
|------|------|
| 客户端 | `go-redis/v9 v9.18.0`（BSD-2-Clause）|
| 服务端镜像 | `redis:7-alpine`（docker-compose.yml）|
| 连接模式 | `redis.UniversalClient` 自动探测 Cluster/Standalone |
| 连接池 | ACS: 500, App: 200, Worker: 100 |
| 生产建议 | 6 节点 Redis Cluster（3 主 + 3 从）|

**Redis Key 使用模式（7 种）：**

| Key 前缀 | 数据结构 | 用途 | TTL |
|---------|---------|------|-----|
| `acs:session:{serial}` | Hash | TR069 会话状态 | 5 分钟 |
| `acs:cmdq:{serial}` | Sorted Set | 设备命令队列（优先级排序）| 无 |
| `acs:heartbeat:{serial}` | String | 设备心跳时间戳 | 2× Inform 间隔 |
| `acs:connreq:pending:{serial}` | String | Connection Request 去重 | 30 秒 |
| `alarm:active:{serial}` | Hash | 活跃告警（alarm_code → alarm_id）| 无 |
| `datamodel:*` | String | 数据模型三级缓存 | 1h/24h |
| `datamodel:cache_version` | String | 跨实例缓存失效计数器 | 无 |

### 2.2 风险一：许可证变更

| 版本范围 | 许可证 | 影响 |
|---------|--------|------|
| Redis ≤ 7.2 | BSD-3-Clause | 无限制，完全开源 |
| Redis 7.4 ~ 7.8 | RSALv2 / SSPLv1 双授权 | **非 OSI 开源许可证** |
| Redis 8.0+ | RSALv2 / SSPLv1 / AGPLv3 三选一 | 新增 AGPL 选项 |

**RSALv2 限制：** 不得将 Redis 作为托管服务（Managed Service）提供给第三方，不得去除许可标识。

**SSPLv1 限制：** 若将 Redis 作为服务提供，必须公开整个服务栈的源代码（比 AGPL 更严格）。

**对 OMC 的影响：**
- OMC 作为自托管部署的私有软件，不对外提供 Redis 服务，**当前使用不直接违反 RSALv2/SSPL**
- 但许可证不确定性增加了法律审查成本和供应链风险
- 客户端 `go-redis/v9` 仍为 BSD-2，不受服务端许可证变更影响

### 2.3 风险二：单线程命令执行瓶颈

Redis 核心命令处理采用**单线程事件循环**模型：

- **优势：** 无锁设计，单次操作延迟极低（< 1μs）
- **瓶颈：** 单核 CPU 吞吐上限约 10-20 万 QPS（取决于命令复杂度）
- **当前影响估算：**
  - 10 万基站 × 每站每 5 分钟 1 次 Inform = ~333 QPS（会话读写）
  - 加上命令队列、心跳更新、缓存查询 ≈ ~2000 QPS
  - 单节点能力远超当前需求
- **Cluster 模式缓解：** Redis Cluster 将数据分散到 16384 个 slot，每分片独立执行，线性扩展吞吐
- **残留瓶颈：** 单个热点 Key（如 `datamodel:cache_version`）仍受单分片限制

### 2.4 风险三：信创合规

Redis 由 Redis Ltd.（以色列/美国）开发，不在中国信创产品目录中。金融、政府等强合规场景可能面临采购限制。

### 2.5 替代方案：Valkey

| 对比项 | Redis 7.x | Valkey 9.0 |
|--------|----------|------------|
| 发起方 | Redis Ltd. | Linux 基金会 |
| 许可证 | RSALv2/SSPL | **BSD-3-Clause** |
| 协议兼容 | — | 100% 兼容 Redis 协议 |
| 性能 | 单线程命令执行 | 多线程 I/O，吞吐提升最高 **230%** |
| 支持方 | Redis Ltd. | AWS、Google Cloud、阿里云、华为、腾讯 |
| 最新版本 | 8.0（2025） | 9.0（2025.10，10 亿 RPS 集群验证）|
| 客户端兼容 | — | `go-redis/v9` 直接使用，**零代码改动** |

**迁移方式：** 仅替换 Docker 镜像 `redis:7-alpine` → `valkey/valkey:8-alpine`，代码零改动。

> **风险评级：中**（许可证变更 + 信创合规）
> **建议：**
> - 短期：锁定镜像为 `redis:7.2-alpine`（最后的 BSD 版本）
> - 中期：评估迁移 Valkey 8.x/9.x
> - 信创场景：直接采用 Valkey（阿里云、华为均已适配）

---

## 3. 数据库国产替代方案

### 3.1 当前 PostgreSQL 使用情况

| 项目 | 详情 |
|------|------|
| 服务端 | PostgreSQL 16（`timescale/timescaledb:latest-pg16` 镜像）|
| 驱动 | `jackc/pgx/v5 v5.8.0`（MIT）+ `pgxpool` 连接池 |
| SQL 构建 | `Masterminds/squirrel v1.5.4`（动态 SQL，无 ORM）|
| 迁移工具 | `golang-migrate/migrate/v4 v4.19.1`（33+ 迁移文件）|
| 连接池 | 主库 max_conns: 50, TimescaleDB max_conns: 30 |
| 生产建议 | Patroni HA 3 节点 + 流复制 |
| 核心特性依赖 | UUID (`gen_random_uuid()`)、JSONB、窗口函数、CTE |

### 3.2 国产 PostgreSQL 兼容数据库

| 产品 | 厂商 | 技术路线 | PG 兼容度 | 许可证 | 信创认证 | pgx 可用性 |
|------|------|---------|----------|--------|---------|-----------|
| **openGauss** | 华为 | PG 9.2 内核 + 深度改造 | 高 | 木兰 PSL v2（开源）| 是 | 协议兼容，需测试 |
| **KingbaseES** | 人大金仓 | PG 兼容模式 | 高（PG 14 级）| 商业 | 是（国家队）| 兼容模式下可用 |
| **GaussDB** | 华为云 | PG 12 兼容 | 中高 | 商业云服务 | 是 | 基本兼容 |
| **达梦 (DM)** | 武汉达梦 | Oracle 兼容路线 | 低（部分 PG 语法）| 商业 | 是 | 需适配层 |
| **PolarDB** | 阿里云 | PG 14 兼容 | 高 | 商业云服务 | 是 | 高度兼容 |

### 3.3 迁移可行性评估

**有利因素：**
- `pgx/v5` 标准 PostgreSQL 驱动，无厂商绑定 ORM
- `squirrel` 生成标准 SQL，兼容性好
- 33 个迁移文件以标准 DDL 为主

**不利因素与关注点：**

| 特性 | openGauss | KingbaseES | 达梦 |
|------|-----------|------------|------|
| `gen_random_uuid()` | 需安装 uuid-ossp 扩展 | 支持 | 需 `SYS_GUID()` 适配 |
| JSONB 类型 | 支持 | 支持 | 部分支持 |
| TimescaleDB 扩展 | **不支持**（需替代方案） | **不支持** | **不支持** |
| CTE (WITH 子句) | 支持 | 支持 | 支持 |
| 窗口函数 | 支持 | 支持 | 支持 |

**最大挑战：** TimescaleDB 超表（4 个）在任何国产库中均无原生支持，需要：
1. 改用 PostgreSQL 原生分区表（range partition by time），或
2. 分离时序数据到独立时序库（IoTDB / TDengine），或
3. 保留 TimescaleDB 仅用于时序数据，管理面迁移国产库

> **风险评级：低**（PostgreSQL 本体）/ **中**（含 TimescaleDB 时序部分）
> **建议：**
> - 信创场景优先评估 **openGauss**（开源 + 华为鲲鹏深度适配）或 **KingbaseES**（政务/金融首选）
> - 管理面数据（设备/用户/告警/配置）先行迁移，时序数据单独处理
> - 迁移前执行完整 DDL 兼容性验证（33 个迁移文件逐条测试）

---

## 4. MinIO 用途与替代方案

### 4.1 当前使用情况

| 项目 | 详情 |
|------|------|
| 客户端 | `minio-go/v7 v7.0.98`（Apache-2.0）|
| 服务端镜像 | `minio/minio:latest`（docker-compose.yml）|
| 接入方式 | S3 API（HTTP），通过 `minio.New()` 静态凭证连接 |
| 控制台端口 | 9001 |
| 生产建议 | 分布式 4+ 节点 Erasure Coding |

### 4.2 用途：6 个 Bucket

| Bucket | 配置字段 | 存储内容 | 写入方 | 读取方 |
|--------|---------|---------|--------|--------|
| `pm-files` | PMFiles | TR069 性能管理文件（XML）| Worker (TransferBridge) | Worker (PMCollector) |
| `mr-files` | MRFiles | 测量报告（MRO/MRS/MRE）| Worker (TransferBridge) | Worker (MRCollector) |
| `firmware` | Firmware | 基站固件升级镜像 | App (SoftwareService) | ACS (DownloadHandler) |
| `config-backup` | ConfigBackup | 设备配置基线备份 | Worker (BackupExecutor) | App (FileManager) |
| `logs` | Logs | 系统/设备日志归档 | Worker (TransferBridge) | App (LogHandler) |
| `reports` | Reports | 生成的报表文件（JSON）| Worker (ReportGenerator) | App (ReportHandler) |

**对象路径约定：** `YYYY/MM/DD/{device_serial}/{filename}`

### 4.3 许可证风险分析

| 组件 | 许可证 | 传染性 | 说明 |
|------|--------|--------|------|
| MinIO 服务端（社区版）| **AGPL-3.0** | **有** | 网络交互即触发（比 GPL 更严格）|
| MinIO Go 客户端 | Apache-2.0 | **无** | 进入 OMC 二进制无传染性 |

**AGPL-3.0 传染性分析：**
- OMC 后端通过标准 S3 HTTP API 与 MinIO 通信，**不存在代码级链接（linking）**
- MinIO 以独立容器进程部署，与 OMC 应用进程完全隔离
- 根据 FSF 和 AGPL 条款解释，通过标准网络 API 调用独立服务一般**不触发**传染条款
- **但：** 若向客户交付包含 MinIO 的整体解决方案，需在合同中明确 AGPL 条款

**2025 年 MinIO 重大变更：**
- MinIO 社区版移除了 Web 管理界面（2025.03），仅企业版 AIStor 保留
- MinIO GitHub 仓库宣布进入维护模式（Maintenance Mode），不再接受新功能和 PR
- 企业版许可证起步价 $96,000/年

### 4.4 替代方案

| 产品 | 许可证 | 语言 | S3 兼容 | 信创 | 特点 |
|------|--------|------|---------|------|------|
| **RustFS** | Apache-2.0 | Rust | 100% | 国产团队 | 专为替代 MinIO 开发，IOPS 提升 43%，延迟降低 38% |
| **SeaweedFS** | Apache-2.0 | Go | 是 | 否 | 成熟项目，超大文件优化 |
| **Ceph (RadosGW)** | LGPL-2.1 | C++ | 是 | 否 | 企业级分布式存储，运维复杂 |
| **CubeFS** | Apache-2.0 | Go | 是 | 否 | CNCF 孵化项目，云原生 |
| **XEOS** | 商业 | — | 是 | 是（金融信创认证）| 星辰天合，1000+ 客户 |

**客户端零改动：** `minio-go/v7` 实现标准 S3 API，替换存储后端仅需修改 Endpoint 地址，代码无变更。

> **风险评级：低**（进程隔离消除 AGPL 传染；但 MinIO 进入维护模式增加了长期风险）
> **建议：**
> - 短期：继续使用，明确合同中的 AGPL 声明
> - 中期：评估 **RustFS**（Apache-2.0，国产，性能更优）
> - 信创场景：采用 **XEOS** 商业版

---

## 5. NATS JetStream 风险分析

### 5.1 当前使用情况

| 项目 | 详情 |
|------|------|
| 客户端 | `nats.go v1.31.0`（Apache-2.0）|
| 服务端 | `nats:2.10-alpine`（Apache-2.0）|
| 重连策略 | `max_reconnect: -1`（无限重连），`reconnect_wait: 2s` |
| JetStream 存储 | FileStorage，`--store_dir /data` |

**6 个 JetStream Stream：**

| Stream | Subjects | 保留策略 | MaxAge | 副本数 |
|--------|---------|---------|--------|--------|
| DEVICE | `device.>` | WorkQueue | 72h | 1（开发）/ 3（生产）|
| COMMAND | `command.>` | WorkQueue | 72h | 1 / 3 |
| PM | `pm.>` | WorkQueue | 72h | 1 / 3 |
| MR | `mr.>` | WorkQueue | 72h | 1 / 3 |
| ALARM | `alarm.>` | WorkQueue | 72h | 1 / 3 |
| OSS | `oss.>` | WorkQueue | 72h | 1 / 3 |

**消费模式：** `QueueSubscribe` + `AckExplicit`，指数退避重试（1s/2s/4s/8s），最大 5 次投递后 `Term()`。

### 5.2 风险一：Jepsen 测试报告 — 数据持久性问题（严重）

2025 年 Jepsen 对 NATS 2.12.1 的测试报告指出：

- **默认 `sync_interval` 为 2 分钟**，即 JetStream 每 2 分钟才将数据 `fsync` 到磁盘
- 在此窗口内，节点崩溃（OS crash、断电）**可能导致已确认（Acked）的消息丢失**
- 少数节点的文件损坏可能导致**持久性脑裂（persistent split-brain）**和副本分歧
- NATS 2.10.22 版本还存在崩溃后 JetStream 忘记整个 Stream 的 Bug（2.10.23 修复）

**对 OMC 的影响：**
- PM/MR 文件处理队列中的消息可能在节点故障后丢失
- 设备命令响应事件可能丢失，导致命令状态不一致
- 当前配置未显式设置 `sync_interval`（使用默认 2 分钟）

**缓解措施：**
```yaml
# nats-server.conf
jetstream {
  sync_interval: "1s"   # 或 sync_always: true（性能略有下降）
}
```

### 5.3 风险二：商业实体与生态

- NATS 核心由 **Synadia** 商业公司主导开发
- 商业版 NGS（Synadia Cloud）与开源版功能逐步分化
- **不在中国信创目录**
- 国内使用案例较少，社区知名度低

### 5.4 风险三：运维复杂性

- JetStream 集群最少需要 3 节点（RAFT 共识）
- 消费者 Lag 监控需 KEDA 或自定义 Prometheus 指标
- 当前生产配置 Replicas=1（单点），需升级为 3 副本

### 5.5 替代方案

| 对比项 | NATS JetStream 2.10 | Apache RocketMQ 5.x |
|--------|--------------------|--------------------|
| 许可证 | Apache-2.0 | Apache-2.0 |
| 主导方 | Synadia（美国商业公司）| Apache 基金会（阿里巴巴贡献）|
| 信创 | 否 | **是**（进入信创目录）|
| Go 客户端 | `nats.go`（官方）| `rocketmq-client-go`（Apache-2.0）|
| 持久化 | FileStorage + Raft | CommitLog + 同步刷盘 |
| 数据可靠性 | Jepsen 发现缺陷 | 金融级，经过大规模生产验证 |
| 延迟 | 极低（< 1ms）| 低（1-5ms）|
| 事务消息 | 不支持 | 支持 |
| 国内生态 | 弱 | **强**（阿里/腾讯/华为均适配）|

**迁移工作量评估：**
- `event.EventBus` 接口已完成抽象（`internal/event/bus.go`）
- 当前有 `NATSEventBus` 和 `ChannelEventBus` 两个实现
- 新增 `RocketMQEventBus` 实现即可无缝替换，**预计工作量 1-2 人周**

> **风险评级：中**（数据持久性隐患 + 信创合规）
> **建议：**
> - **P0（立即）：** 配置 `sync_interval: 1s` 消除数据丢失风险
> - **P0（立即）：** 生产环境 Replicas 设为 3
> - 信创场景：迁移至 RocketMQ 5.x（EventBus 接口已抽象，工作量可控）

---

## 6. PostgreSQL 分析

### 6.1 当前使用情况

| 项目 | 详情 |
|------|------|
| 服务端版本 | PostgreSQL 16 |
| Docker 镜像 | `timescale/timescaledb:latest-pg16` |
| 驱动 | `jackc/pgx/v5 v5.8.0` + `pgxpool`（MIT）|
| 迁移工具 | `golang-migrate/migrate/v4 v4.19.1`（MIT）|
| SQL 构建 | `Masterminds/squirrel v1.5.4`（MIT）|
| 迁移文件 | 33+ 个（000001 ~ 000033）|
| 表特征 | UUID 主键、created_at/updated_at 时间戳、JSONB 列 |

### 6.2 许可证分析

| 组件 | 许可证 | 说明 |
|------|--------|------|
| PostgreSQL 服务端 | PostgreSQL License | 类 BSD，OSI 认证，极宽松 |
| pgx/v5 驱动 | MIT | 无限制 |
| golang-migrate | MIT | 无限制 |
| squirrel | MIT | 无限制 |

**PostgreSQL License** 允许任意商业使用、修改和分发，无传染性条款，是目前最宽松的数据库许可证之一。社区版（Community Edition）完全免费，无企业版功能限制。

### 6.3 技术风险

- PostgreSQL 16 为当前长期支持版本（LTS），社区极度活跃
- `pgx/v5` 是 Go 生态中性能最佳的 PostgreSQL 驱动，接口稳定
- 无已知安全漏洞或重大 Bug
- 连接池配置合理（max 50，min 10，30 分钟生命周期）

### 6.4 扩展性

- 单实例可支撑数千 TPS（取决于硬件和查询复杂度）
- 可通过 Patroni HA + 流复制实现读写分离
- 超大规模可引入 PgBouncer 连接池或 Citus 分片

> **风险评级：无**
> **建议：** 保持现有方案。关注 PostgreSQL 17 升级机会（并行查询、增量备份改进）。

---

## 7. TimescaleDB 时序数据库分析

### 7.1 当前使用情况

TimescaleDB 作为 PostgreSQL **扩展（Extension）** 运行于同一数据库实例。

**4 个超表（Hypertable）：**

| 超表 | 分区列 | Chunk 间隔 | 压缩策略 | 保留策略 | 用途 |
|------|--------|----------|---------|---------|------|
| `pm_counters` | time | 1 天 | 7 天后压缩 | 90 天 | PM 文件计数器原始数据 |
| `kpi_values` | time | 1 天 | 无 | 无 | KPI 计算结果 |
| `mr_records` | time | 1 天 | 7 天后压缩 | 90 天 | MR 测量报告记录 |
| `alarms_history` | time | 7 天 | 无 | 365 天 | 历史告警 |

**1 个连续聚合（Continuous Aggregate）：**
- `pm_counters_hourly` — 按 `time_bucket('1 hour')` 聚合 PM 计数器（SUM/AVG/MIN/MAX/COUNT）

**使用的 TimescaleDB 特性：**
- `create_hypertable()` — 超表创建
- `add_compression_policy()` — 自动压缩
- `add_retention_policy()` — 自动数据保留
- `time_bucket()` — 时间桶查询
- 连续聚合（Continuous Aggregate）

### 7.2 许可证分析

| 功能层 | 许可证 | 是否使用 | 说明 |
|--------|--------|---------|------|
| 核心引擎 | Apache-2.0 | 是 | `create_hypertable`、基础时间查询 |
| 社区特性 | TSL（Timescale License）| 是 | 连续聚合、压缩策略、保留策略 |
| 企业特性 | 商业 | **否** | 多节点分布式超表、数据分层到 S3 |

**TSL 关键条款：**
- **自托管（self-hosted）完全免费**，功能无限制
- **禁止：** 将 TimescaleDB 作为 DBaaS/PaaS 托管数据库服务提供给第三方
- OMC 为自托管部署，**不触发 TSL 限制**

**优秀的防御性设计：**
所有迁移 SQL 均包含 `IF EXISTS (SELECT 1 FROM pg_extension WHERE extname = 'timescaledb')` 检查，若无 TimescaleDB 扩展则自动降级为普通 PostgreSQL 表。

### 7.3 国产替代路径（如需要）

| 替代方案 | 许可证 | 信创 | 迁移成本 | 说明 |
|---------|--------|------|---------|------|
| PG 原生分区表 | PostgreSQL License | 随主库 | 中 | `PARTITION BY RANGE (time)`，需重写 4 张超表 DDL |
| Apache IoTDB | Apache-2.0 | 是（进入信创目录）| 高 | 专用时序库，需独立部署 + 数据管道 |
| TDengine 社区版 | AGPL-3.0 | 国产 | 高 | 国产时序库，有 AGPL 传染性问题 |

> **风险评级：低**（许可证无风险；国产替代有路径但成本较高）
> **建议：**
> - 默认方案：继续使用 TimescaleDB（自托管免费，功能完备）
> - 信创场景：切换至 PG 原生分区表（改动最小）或分离时序数据到 IoTDB

---

## 8. 水平扩展能力分析

### 8.1 架构概览：三个无状态部署单元

| 进程 | 职责 | 无状态 | 状态存储位置 |
|------|------|--------|-------------|
| `omcgo-acs` | TR069 ACS 引擎 | **是** | Redis（会话/命令队列/心跳）|
| `omcgo-app` | 管理面 REST API | **是** | PostgreSQL（业务数据）+ JWT（无服务端 Session）|
| `omcgo-worker` | PM/MR/报表/备份后台处理 | **是** | NATS（任务分发）+ MinIO（文件存储）|

### 8.2 自动扩缩容配置

**omcgo-acs HPA：**
- 触发条件：CPU > 70% **或** `acs_active_sessions` > 2000/Pod
- 副本范围：**2 ~ 20**
- 扩容速率：每 60 秒增加 2 Pod
- 准入控制：每实例 `max_concurrent: 5000` 会话

**omcgo-app HPA：**
- 触发条件：CPU > 70%
- 副本范围：**2 ~ 5**
- 缩容稳定窗口：300 秒

**omcgo-worker KEDA：**
- 触发条件：NATS JetStream 消费者 Lag > 1000 条
- 副本范围：**2 ~ 10**
- 轮询间隔：15 秒
- 激活阈值：Lag > 100 条

### 8.3 各基础设施组件扩展能力

| 组件 | 扩展方式 | 当前状态 | 扩展方向 |
|------|---------|---------|---------|
| PostgreSQL | Patroni HA 读写分离 | 单实例（开发）| PgBouncer + 读副本 |
| TimescaleDB | 与 PG 共扩展 | 单实例 | 同上 |
| Redis | `UniversalClient` 自动 Cluster | **Cluster-Ready** | 线性加分片 |
| NATS | 3 节点 JetStream Cluster | Queue Group 负载均衡 | 线性加节点 |
| MinIO | 分布式 Erasure Coding | 单节点（开发）| 4+ 节点线性扩展 |

### 8.4 无状态验证

逐项排查后确认 **无水平扩展障碍**：

| 检查项 | 结果 | 说明 |
|--------|------|------|
| 服务端 Session | **无** | JWT 认证，Token 签名验证即可 |
| 本地文件依赖 | **无** | 所有文件存储均在 MinIO |
| 进程间共享内存 | **无** | 三个进程独立部署 |
| 分布式锁 | **无需** | 命令队列用 Redis `ZRem` 原子操作竞争 Pop |
| 单例定时任务 | **可改进** | HeartbeatMonitor cron 每实例独立运行（幂等但冗余）|
| 缓存一致性 | **10 秒延迟** | DataModel L1 缓存通过 Redis `cache_version` 轮询同步 |

### 8.5 规模估算

| 场景 | ACS 副本 | 并发会话 | 基站数 |
|------|---------|---------|--------|
| 当前默认 | 2 | 4,000 | ~1 万 |
| HPA 满载 | 20 | 40,000 | ~10 万 |
| 扩展 HPA | 50 | 100,000 | ~25 万 |
| 极限（需评估 PG）| 100+ | 200,000+ | ~50 万+ |

**扩展至 100 万基站的路径：**
1. ACS HPA `maxReplicas` 提升至 50-100（无代码变更）
2. PostgreSQL 引入 PgBouncer + 读写分离
3. Redis Cluster 扩展分片数
4. NATS 增加节点数 + Stream Replicas=5
5. MinIO 扩展至 8+ 节点

> **风险评级：无**（三个部署单元均为无状态，已具备完整水平扩展能力）
> **建议：** 当前架构满足 10 万基站目标；100 万级需扩大 HPA 上限并评估 PG 分片方案。

---

## 9. 综合风险汇总与行动计划

### 9.1 风险矩阵

| 组件 | 版本 | 许可证 | 许可证风险 | 技术风险 | 信创合规 | **综合评级** |
|------|------|--------|-----------|---------|---------|------------|
| Go 全部依赖（92 个）| 多版本 | MIT/Apache/BSD | 无 | 无 | — | **无** |
| PostgreSQL | 16 | PostgreSQL License | 无 | 无 | 有国产替代 | **无** |
| 水平扩展架构 | — | — | — | 无 | — | **无** |
| TimescaleDB | 2.x-pg16 | Apache-2.0 + TSL | 低（自托管免费）| 低 | 有替代路径 | **低** |
| MinIO 服务端 | latest | **AGPL-3.0** | 低（进程隔离）| 中（进入维护模式）| XEOS/RustFS | **低** |
| Redis 服务端 | 7.x | **RSALv2/SSPL** | 中 | 中（单线程）| Valkey 可替 | **中** |
| NATS JetStream | 2.10 | Apache-2.0 | 无 | **中（fsync 问题）**| 非信创 | **中** |

### 9.2 行动计划

| 优先级 | 操作项 | 组件 | 工作量 | 影响 |
|--------|--------|------|--------|------|
| **P0（立即）** | 配置 NATS `sync_interval: 1s` | NATS | 极小 | 消除数据丢失风险 |
| **P0（立即）** | NATS 生产环境 Replicas 设为 3 | NATS | 极小 | 消除单点故障 |
| **P0（立即）** | 锁定 Redis 镜像 `redis:7.2-alpine` | Redis | 极小 | 确保使用 BSD 许可证版本 |
| **P1（近期）** | 评估 Valkey 8.x/9.x 替换 Redis | Redis | 小 | 彻底消除许可证风险 |
| **P1（近期）** | 压测 Redis Cluster 单分片吞吐上限 | Redis | 中 | 验证性能瓶颈 |
| **P2（中期）** | 评估 RustFS 替换 MinIO | MinIO | 中 | 消除 AGPL + 维护模式风险 |
| **P2（中期）** | 信创场景：NATS → RocketMQ 5.x | NATS | 中（1-2 人周）| EventBus 接口已抽象 |
| **P3（远期）** | 信创场景：PG → openGauss/KingbaseES | PostgreSQL | 高 | TimescaleDB 需独立处理 |
| **P3（远期）** | 生成 SBOM（软件材料清单）| 全部 | 小 | 合规审计用 |

### 9.3 许可证合规声明

OMC 后端二进制（`omcgo-acs`, `omcgo-app`, `omcgo-worker`）内嵌的所有代码均为 **MIT/Apache-2.0/BSD 宽松许可证**，不存在 GPL/LGPL/AGPL 传染性代码进入产品二进制。

AGPL-3.0 组件（MinIO 服务端）以独立进程部署，通过 HTTP S3 API 访问，不触发传染条款。交付时需在软件材料清单（SBOM）中注明。

---

## 附录

### A. 许可证扫描工具推荐

| 工具 | 用途 |
|------|------|
| `go-licenses` | 自动扫描 Go 依赖许可证 |
| `trivy` | 容器镜像安全漏洞扫描 |
| `nancy` | Go 依赖 CVE 漏洞扫描 |
| `syft` + `grype` | 生成 SBOM + 漏洞匹配 |

### B. 参考链接

- [Redis 许可证变更公告](https://redis.io/blog/redis-adopts-dual-source-available-licensing/)
- [Valkey 官网（Linux 基金会）](https://valkey.io/)
- [Jepsen NATS 2.12.1 测试报告](https://jepsen.io/analyses/nats-2.12.1)
- [MinIO AGPL-3.0 声明](https://www.min.io/blog/from-open-source-to-free-and-open-source-minio-is-now-fully-licensed-under-gnu-agplv3)
- [MinIO 进入维护模式（InfoQ）](https://www.infoq.com/news/2025/12/minio-s3-api-alternatives/)
- [RustFS 项目](https://github.com/rustfs/rustfs)
- [TimescaleDB 许可证条款](https://github.com/timescale/timescaledb/blob/main/tsl/LICENSE-TIMESCALE)
- [openGauss 官网](https://opengauss.org/)
- [KingbaseES 官网](https://www.kingbase.com.cn/)
- [Apache RocketMQ](https://rocketmq.apache.org/)

### C. 文件索引

| 文件 | 说明 |
|------|------|
| `omcgo/go.mod` | 依赖版本唯一来源 |
| `omcgo/internal/components/redis/redis.go` | Redis UniversalClient 配置 |
| `omcgo/internal/components/nats/nats.go` | 6 个 JetStream Stream 定义 |
| `omcgo/internal/components/minio/minio.go` | 6 个 Bucket 定义 |
| `omcgo/internal/event/bus.go` | EventBus 接口抽象 |
| `omcgo/internal/components/postgres/timescale.go` | TimescaleDB 扩展检查 |
| `omcgo/deployments/docker/docker-compose.yml` | 基础设施镜像版本 |
| `omcgo/deployments/k8s/acs/hpa.yaml` | ACS HPA 扩缩容配置 |
| `omcgo/deployments/k8s/worker/keda-scaledobject.yaml` | Worker KEDA 配置 |
