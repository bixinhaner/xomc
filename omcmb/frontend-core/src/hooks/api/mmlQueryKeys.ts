export const MML_CUSTOM_COMMAND_PATHS_QUERY_KEY = [
  'mml',
  'console',
  'custom-command-paths',
] as const;

/** Console 与 Catalog 按 productId 派生的自定义命令列表共用此前缀。 */
export const MML_CUSTOM_COMMANDS_QUERY_KEY = ['mml', 'custom-commands'] as const;
