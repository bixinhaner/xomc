# 生产构建（vite build 默认 mode=production 加载本文件，覆盖 .env 的 mock 默认）。
# 与 v1(webcode) 一致：打真实后端，不用 mock（否则 system_license 等无 mock 实现的 API 会报错，
# 且全部页面展示的是假数据而非真实业务数据）。
VITE_USE_MOCK=false
VITE_API_BASE_URL=/api/v1

# 动态菜单门禁（与 v1 对齐）：admin/超管恒放行，非 admin 按模块级菜单门禁。
VITE_DYNAMIC_MENU=true
