# Code Review: MML 多实例查询与结果展示修复

- 日期：2026-08-18
- 基线：`main` / `8989c1391`
- 分支：`fix/mml-multi-instance-results`
- 作者：wangyong
- 范围：参数模型映射、MML 查询任务元数据、前端结果解析与宽表展示
- 结论：PASS

## 变更摘要

- 修正 BaiBNQ NR 小区带宽、开关和多实例参数映射，移除固定实例别名与重复映射，并同步 XML 元数据计数。
- LST 查询在实例留空、叶子路径折叠为对象前缀时保留用户原始勾选路径，供任务历史重建和结果筛选使用。
- 多实例结果按实例号数值排序，列名携带实例上下文；取消 80 列截断，完整展示 QOS 等大对象的所有返回参数。
- 命令弹框重新打开时重新校验参数模型，避免缓存的空 sub-fields 造成命令没有可选 PATH。
- MML 粘性表头和表体使用相同固定数值宽度，避免大量长表头时横向滚动产生列错位。

## 审查结果

### CRITICAL

无。

### WARNING

无。

### INFO

- QOS 10 个实例、每实例 35 个参数时会生成 350 个动态列；页面通过横向滚动完整展示，未再静默截断。
- 本次未新增数据库迁移、权限逻辑、SQL、外部接口或不安全 HTML 渲染。
- 新增的 `selected_standard_paths` 是任务 commands JSONB 内的兼容扩展字段；历史任务缺失该字段时继续按显式对象查询语义展示后代参数。

## 重点检查

- `standard-model.xml` 的对象、参数和总路径计数与实际 XML 条目一致；BaiBNQ `totalEntries` 与对象、参数总数一致。
- 标准路径到私有路径的占位符层级不会减少，内置参数模型不存在重复私有路径或重复映射对。
- 多层实例号采用数值比较，实例 `10` 排在实例 `9` 之后，并保持同实例内用户选择顺序稳定。
- 叶子查询折叠为对象 GPV 后只接纳匹配原始模板的返回参数；显式对象查询仍展示全部后代。
- 动态表格的表头和表体共享相同列宽、总宽度及固定布局，时间列不会落入 QOS 参数表头下。

## 验证

- `cd omcgo && go build ./...`：通过。
- `cd omcgo && go test ./...`：通过，包含 E2E 与 integration。
- `cd omcmb && npm run typecheck`：通过。
- 相关前端 Vitest：6 个文件、81 个用例通过。
- 相关前端 ESLint：通过。
- `cd omcmb/webcode && npm run build`：通过，仅有既有 Vite 警告。
- `git diff --cached --check`：通过。
- 本地 Docker Compose 热部署：Web/app/acs 启动正常，`http://localhost:8081/` 返回 200。
- 可见浏览器真实 QOS 任务：356 个表头与 356 个单元格逐列坐标偏差为 0；350 个动态参数和 QOS.10 的 35 个参数完整存在；下发时间、响应时间列和值正确对齐；无 console/page error。

## 风险评估

低至中。参数数量很大时 DOM 与横向滚动成本会增加，但这是替代静默截断后的显式产品行为；固定列宽保证粘性表头和表体在该规模下保持一致。
